{"unit": "u1", "section": "5a Design Direction (NOT YET APPROVED) / FR-80", "question": "How does an inline entity reference show it is a link? The product has no inline text-link idiom today — every existing link is a shadcn Button or a nav item — so EntityRef needed one and §5a is unapproved.", "best_guess": "Underline on hover only, at the existing underline-offset-2, with no colour change and no new token. The global :focus-visible ring in globals.css supplies the keyboard affordance. Nothing new is spent from the palette.", "confidence": "med"}
{"unit": "u1", "section": "FR-80 adoption across the eleven existing screens", "question": "Once Wave B adopts <EntityRef>, does the existing <Ref>/<RefList> in answer-chips.tsx get deleted, or do both stay? They emit different contracts (data-verify-unit=\"ref\" vs \"entity-ref\") and the e2e gate only counts the latter, so a screen that keeps Ref reads as un-adopted.", "best_guess": "Both stay for now; EntityRef supersedes Ref wherever a reference resolves to a real entity, and Ref remains only for identifier tokens that name no entity at all (if any survive). This unit changes no existing screen either way, so the decision costs nothing here and belongs to the adopting units.", "confidence": "med"}
