{"unit": "i1", "section": "CR-003 FR-83 / §7 work_item", "question": "When a screen renders a unit id like `u4` without naming a run, should resolution refuse, or pick a row?", "best_guess": "Refuse unless EXACTLY ONE work item in the engagement carries that unit. Zero matches and two-or-more matches both resolve to null and render FR-12's dangling treatment. Supplying `runId` makes it exact. Picking the most recent was rejected: a link that is right most of the time and silently wrong the rest of it is the navigable form of a wrong `done`.", "confidence": "high"}
{"unit": "i1", "section": "CR-003 FR-80 / §5a", "question": "What label does an entity reference carry when the row exists and has no human reference of its own — a `hand`-mode work item with no unit, a blocker with a null `ref`?", "best_guess": "`<kind> <first 8 chars of uuid>` — e.g. `work item 3f2a1b8c` — from one exported `fallbackLabel()`. The uuid is the only identity the row has and §7a leaves `id` clear on all three affected tables. Rendering an empty token or dropping the reference were both rejected as silent.", "confidence": "med"}
{"unit": "i1", "section": "CR-003 FR-80 vs FR-81", "question": "FR-80 names `an engagement slug` among the references that must be navigable, but `engagement` is not one of FR-81's eight kinds, so `ENTITY_KINDS` has no entry for it and `<EntityRef>` cannot render one. Which requirement gives?", "best_guess": "Leave the seam alone and report it. `tests/m27-gate.test.ts` asserts `ENTITY_KINDS` equals exactly FR-81's eight, so adding `engagement` turns a passing gate red. The engagement detail view already exists at `/registry/[slug]` and is already linked from the registry table, so Wave C renders a plain `<Link href={`/registry/${slug}`}>` — every detail loader returns `engagement.slug` for exactly that. FR-80 is met in substance; the reference is simply not an `<EntityRef>`.", "confidence": "med"}
{"unit": "i1", "section": "CR-003 FR-82 / FR-81", "question": "Should a requirement's detail view also list the contract milestones whose acceptance criteria name it — a fifth relationship beyond FR-82's four?", "best_guess": "Load it and let Wave C decide whether to render it. It is an FR-81 inbound reference and it is already stored (`acceptance_criterion.requirement_ref`); nothing new is derived. Only `id` and `name` are read — no amount is decrypted — and the path is operator-only, so FR-86 is unaffected. Returned as `RequirementDetail.milestones`, named distinctly from FR-82's four so it cannot be mistaken for one of them.", "confidence": "med"}
{"unit": "i1", "section": "CR-003 FR-81 / FR-50", "question": "Should the contract-milestone detail view show FR-50/FR-51's `billable` / `claimed` / `open` state?", "best_guess": "No, and deliberately. FR-81 asks for fields and references; the invoice gate is derived by `committedAnswer` and a second implementation of it on a detail view is exactly the drift FR-82's `nothing new is derived` clause exists to prevent. If the view wants it, it comes from `readCommitted` and not from a copy. Stated rather than silently omitted.", "confidence": "med"}
{"unit": "i1", "section": "§7 open_question / schema", "question": "Two migrations disagree about whether `open_question` has a natural key. Which does the resolver honour?", "best_guess": "Both are recorded and the later one is implemented. `20260819144331`'s table comment says `No natural unique key, deliberately`; `20260819165903` then added `source_key` (`<artifact filename>#<ordinal>`, clear text) and `20260819170622` made its unique index plain. So resolution by `(engagement_id, source_key)` is supported. It is a MACHINE key — no screen renders a filename and an ordinal — and the support is expected to go unused: zero migrations hold a foreign key to `open_question` and zero files under `src/app/` mention it, both measured.", "confidence": "high"}
