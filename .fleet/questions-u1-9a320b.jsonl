{"unit": "u1", "section": "CR-005 §3.3 FR-96 — engagement filter (DRAFT, NOT APPROVED)", "question": "`/runs` is cross-engagement by construction (Q16, resolved at CR-005's approval from FR-92's own wording), and with one `fleet_run` row and two engagements a filter is currently unusable — but the moment a second engagement's runs land, an unfiltered cross-engagement list is the shape this product exists to replace, since Erik would be filtering it in his head; should FR-96 be approved and built, or is the engagement column plus sorting sufficient?", "best_guess": "Build no filter. FR-96 is DRAFT and Q12-Q15 are unanswered, and the run brief names an engagement filter explicitly as out of scope. The engagement is rendered as a column and links to `/registry/[slug]`, which is the navigable half of the same need without pre-empting an unapproved requirement.", "confidence": "high"}
{"unit": "u1", "section": "spec §5a colour direction vs CR-005 §3.2 FR-95", "question": "Spec 5a assigns one colour per named semantic state and FR-79 already owns `state-contested` for a specific commercial meaning (a billable milestone flagged by an open critical defect), so there is no colour in the scale for FR-95's \"two sources recorded different verdicts\" — should verdict disagreement get a treatment of its own on the semantic scale, or stay structural?", "best_guess": "Render it structurally: one labelled row per source plus a stated `sources disagree` line in bold, and no new hue. Reusing `state-contested` would give one colour two meanings, which 5a forbids, and inventing a sixth state colour for a branch that is unreachable from today's single-verdict-column schema would be a strong visual claim nobody can currently see. Revisit when a second verdict source genuinely exists.", "confidence": "med"}
