# devops report — d1

**Status:** DONE
**Run:** d4000f
**Live status:** NOT LIVE

## Summary

Migration `20260824110601_i1_planned_work_schema` is applied to the live Supabase project
`onpvolboecjpdkvurjaf`, the local filename matches the version Supabase recorded, and every
schema claim in the file was read back from the database and confirmed — including the two
this repo has been burned by before: the upsert conflict target is a PLAIN unique index, and
the trigger function carries a per-name REVOKE from PUBLIC with a matching GRANT to
`service_role`. Nothing was applied, re-applied or repaired.

The finding that matters is not in the schema. **The live database has moved ahead of every
deployed artifact.** The migration is applied to the production database, but the commit that
introduces it is on no remote branch and in no deployment: branch
`agent-build/2026-08-24-d4000f` was never pushed, production serves `e3de4be`, and there is no
preview deployment for this branch to report. Production is not broken by the gap — every
change is additive or widening and I checked the read paths — but the gap is real and this
report is where it is recorded.

## Step zero — the two SHAs

| | SHA | Commit |
|---|---|---|
| HEAD before reset | `75f070a1b6137185cd11e999679cb90e0da3091a` | Merge pull request #3 (b46 command palette) |
| HEAD after reset | `6f91fa2a30f2235e705645a1eee7b5b6d182658d` | fleet d4000f: Phase 1 checkpoint, prod.md writeback, learnings |

Both preconditions were proven empty as separate commands before resetting: `git status
--porcelain` printed nothing, and `git log --oneline agent-build/2026-08-24-d4000f..HEAD`
printed nothing. No `git merge` was used.

Phase 1 confirmed: `git merge-base --is-ancestor 7be6949 HEAD` returned 0 (`HAVE_PHASE1`), and
`supabase/migrations/20260824110601_i1_planned_work_schema.sql` exists in this worktree
(13156 bytes).

Worktree: `/Users/erikmeltzer/Projects/project-tracker/.claude/worktrees/agent-ad1c7f2c2aa3ddb71`
(established with `git rev-parse --show-toplevel`). All reads and writes were confined to it.

## Branch and merge facts

- Working branch: `agent-build/2026-08-24-d4000f` @ `6f91fa2a30f2235e705645a1eee7b5b6d182658d`
- Production branch: `master` @ `e3de4bed86d46a65872295aca50828aa075d6fee` (after `git fetch origin`, which moved `origin/master` from `75f070a` to `e3de4be`)
- Is the working commit on production? **NO** — `git merge-base --is-ancestor 6f91fa2 origin/master` returned non-zero (`NOT_ON_PRODUCTION`)
- Is the branch even on the remote? **NO** — `git ls-remote --heads origin agent-build/2026-08-24-d4000f` returned zero lines
- Merge base: `0e17a6dced48b7e0933f14d00941401edd32af82`
- Five commits sit on the branch and not on production, including the migration commit `7be6949`

Basis for the live status: the commit is **not** on the production branch, **and** the changed
behaviour was **not** observed at a live URL because no deployment of this branch exists. Both
required facts are absent, so the status is **NOT LIVE**. It is not even
`DEPLOYED (preview, not merged)` — there is no preview.

## Migration version vs local filename — side by side

`list_migrations` on `onpvolboecjpdkvurjaf` reports 13 migrations. All 13 match the local
filenames exactly, in order. The unit under verification:

| Source | String |
|---|---|
| `list_migrations` version + name | `20260824110601` + `i1_planned_work_schema` |
| Local file in this worktree | `20260824110601_i1_planned_work_schema.sql` |

**Match.** No rename is needed and no re-run risk exists: because every applied version has a
byte-matching local filename, a later `supabase db push` reads zero files as pending and cannot
re-run `create type plan_reconciliation`. This is the exact bookkeeping failure the repo rule
warns about, and it is not present.

## Schema read back from the database, not from the migration file

Source: `information_schema.columns`, `pg_indexes`, `pg_trigger`, `pg_constraint`, `pg_enum`,
`pg_proc`, `pg_policies`. The migration file was read only to know what to look for.

### FR-87 — nullable `execution_mode`

| Claim | Observed | Verdict |
|---|---|---|
| `work_item.execution_mode` is nullable | `is_nullable = YES`, `udt_name = execution_mode` | CONFIRMED |
| NULL is not a fourth enum member | `execution_mode` enum has exactly 3 labels: `fleet`, `hand`, `external` | CONFIRMED — no `planned` member was added |
| `work_status` unchanged, `pending` present | 7 labels, `pending` at sort order 1 | CONFIRMED |

### FR-91 — staleness timestamp and its trigger

| Claim | Observed | Verdict |
|---|---|---|
| `created_at timestamptz not null default now()` | present, ordinal 22, `NOT NULL`, default `now()` | CONFIRMED |
| `updated_at timestamptz not null default now()` | present, ordinal 23, `NOT NULL`, default `now()` | CONFIRMED |
| BEFORE UPDATE trigger maintains it | `CREATE TRIGGER work_item_touch_updated_at BEFORE UPDATE ON public.work_item FOR EACH ROW EXECUTE FUNCTION app.touch_updated_at()`, `tgenabled = O` (enabled) | CONFIRMED |
| Partial read index for the staleness scan | `CREATE INDEX work_item_planned_updated_idx ON public.work_item USING btree (updated_at) WHERE (execution_mode IS NULL)` | CONFIRMED, and partial **by design** — it is not unique, so it can never be an ON CONFLICT target |

### FR-90 — plan reference, collision mark, and the unique index

| Claim | Observed | Verdict |
|---|---|---|
| `plan_ref text` nullable, clear (not encrypted) | ordinal 24, `text`, nullable | CONFIRMED |
| `plan_reconciliation` NOT NULL default `unreconciled` | ordinal 25, `NOT NULL`, default `'unreconciled'::plan_reconciliation` | CONFIRMED |
| Enum has exactly 3 members | `unreconciled`, `keyed`, `collision` | CONFIRMED |
| CHECK: keyed requires a key | `CHECK (((plan_reconciliation <> 'keyed') OR (plan_ref IS NOT NULL)))` | CONFIRMED, and it contains **no function call**, so it cannot hit the caller-role grant trap |
| Unique index is PLAIN, not partial | see below | CONFIRMED |

**The `pg_indexes.indexdef` for the new unique index, verbatim:**

`CREATE UNIQUE INDEX work_item_engagement_plan_ref_key ON public.work_item USING btree (engagement_id, plan_ref)`

**No `WHERE` clause.** It is a plain unique index and is therefore usable as a PostgREST
`on_conflict` target.

The detector is not blind: the same query flagged `is_partial = true` for
`work_item_planned_updated_idx` and `work_item_unparsed_idx` in the same result set, so a
`WHERE` clause would have been seen if one were there.

### Tables and RLS

No table was created by this migration. Schema `public` holds exactly 21 tables and **every one
has a row in spec 7a** — so the "an entity with no row in 7a is a blocker" rule is not
triggered. `work_item` RLS state, read back:

- `relrowsecurity = true`, `relforcerowsecurity = false`, policy count 1
- Policy `work_item_select`, cmd `SELECT`, roles `{authenticated}`, qual `app.is_operator()`, no WITH CHECK
- Table grants: `authenticated` SELECT only; `service_role` and `postgres` full; **no `anon` grant of any kind**

Asserting the policy rather than the `relrowsecurity` flag was deliberate — this project's
`rls_auto_enable` event trigger sets that flag on every new public table, so the flag is not
evidence. (Vault note below, CONFIRMED.)

Encryption per 7a is intact: `description` and `raw_status` are both `bytea`. `plan_ref` is
clear `text`, which matches 7a as written — 7a names exactly two encrypted columns on
`work_item` and leaves identifiers clear, the same call it makes for `requirement.ref` beside
an encrypted `requirement.text`.

## Grants on the function this migration created

This migration created **no function in schema `public`**. It created one in schema `app`:
`app.touch_updated_at()`. The standing per-name REVOKE rule is scoped to `public`, but i1 wrote
the revoke and the grant anyway, and both are live. Read back from `pg_proc`:

| Property | Observed |
|---|---|
| `prosecdef` | `true` (SECURITY DEFINER) |
| `proconfig` | `search_path=""` |
| owner | `postgres` |
| `proacl` | `postgres=X/postgres || service_role=X/postgres` |

There is **no bare `=X/postgres` entry**, which is the ACL form a grant to PUBLIC takes, and no
`anon` or `authenticated` entry. So PUBLIC has no EXECUTE and `service_role` does.

The ACL reader is not blind: in the very same result set, `public.rls_auto_enable` shows
`=X/postgres || postgres=X || anon=X || authenticated=X || service_role=X` — a live PUBLIC
grant. A PUBLIC grant would have been visible if present. See Issues below; that one is
pre-existing.

A `SECURITY DEFINER` trigger is the case that does not strictly need the grant, so the
`service_role` GRANT here is redundant belt-and-braces rather than load-bearing. It is still the
right call: a redundant grant costs nothing and a missing one cost this project a total Mode-1
ingest outage on run b0952e.

## Verification — observed, not asserted

Every mutating probe ran inside an explicit transaction that was rolled back. **Live data was
left untouched** — proven after the fact, not assumed.

| Probe | Command | Observed |
|---|---|---|
| Applied version present | `list_migrations` on `onpvolboecjpdkvurjaf` | 13 migrations, `20260824110601 i1_planned_work_schema` present |
| Filename vs version | `ls supabase/migrations` vs the above | all 13 match, in order |
| FR-87 storable | insert with `execution_mode` omitted, `status = 'pending'` | 1 row written, `execution_mode` stored NULL, `plan_reconciliation` defaulted `unreconciled` |
| Upsert idempotency, the SECOND post | same insert twice with `on conflict (engagement_id, plan_ref) do nothing` | both statements succeeded, **no `42P10`**, exactly 1 row left |
| Trigger fires and cannot be defeated | `update ... set updated_at = '2000-01-01'` then read it back | came back `2026-08-24 13:28:28+00` — the trigger **overrode an explicitly written stale value** |
| Same, as the role the app writes with | `set local role service_role`, insert then the stale-value update | `ran_as = service_role`, insert and update both succeeded (**no `42501`**), `updated_at` overridden to now |
| Negative control — CHECK must refuse | insert `plan_reconciliation='keyed'` with `plan_ref` NULL | **`23514`** violating `work_item_keyed_requires_plan_ref` |
| Positive control — CHECK must admit | insert `plan_reconciliation='collision'` with `plan_ref` NULL | succeeded, `no_key = true`, `no_mode = true` |
| Rollback actually rolled back | re-read the touched row and count probe rows | `updated_at` restored to `2026-08-24 11:06:01.744111+00`, 0 probe rows, 20 rows total, `max(updated_at)` unchanged |
| Production root | `vercel curl https://project-tracker-mu-livid.vercel.app/` | `200` |
| Production auth boundary, no auth | `vercel curl .../api/answer/next` | `401` |
| Production auth boundary, bad auth | same with `Authorization: Bearer not-a-real-token` | `401` |
| Negative control — 404 | `vercel curl .../api/does-not-exist-d1` | `404` |
| Preview for this branch | `vercel ls --meta githubCommitRef=agent-build/2026-08-24-d4000f` | **no deployments found** |
| Negative control — the filter can return rows | `vercel ls --meta githubCommitRef=docs/b46-verified-in-production` | 1 Ready preview returned |

**Negative control discriminates: YES.** Three separate ones fired. The CHECK refused `keyed`
and admitted `collision`, so it is discriminating on the right axis rather than refusing
everything. Production answered `200` / `401` / `404` to three different requests, so it is not
a catch-all that would make any probe look green. And the Vercel branch filter returned a row
for a branch that has deployed while returning none for ours, so "no preview" is a measured
absence rather than a broken query.

**One probe of mine produced a false negative and I caught it.** The first `service_role` run
reported `trigger_fired_under_service_role: false`. That was an artifact of my own test, not a
defect: inside one transaction `now()` is frozen at transaction start, so the trigger set
`updated_at` to exactly `created_at` and my `updated_at > created_at` comparison could not fire.
Re-measured with the stale-value technique, which discriminates regardless of `now()` semantics,
and it came back true. Recording it because the discarded reading would have been reported as a
broken trigger.

### The backfill was a no-op, and that is correct

The migration claims it moves `created_at` back to the run timestamp where one exists. Measured:
`created_at < updated_at` on **0 of 20** rows, so nothing moved. Cause, measured rather than
guessed: `started_at IS NOT NULL` on 0 rows and `ended_at IS NOT NULL` on 0 rows, so the
backfill's `WHERE` matched nothing. All 20 rows carry
`created_at = updated_at = 2026-08-24 11:06:01.744111+00`, the migration time.

Not a defect — the guard did its job and there was no better value available. It does mean all
20 pre-existing rows assert they were created at migration time, which the migration's own
comment says it wanted to avoid. No FR-91 answer depends on it: FR-91 reads only planned rows
(`execution_mode IS NULL`) and there are **0** of those.

### NULLS DISTINCT, observed

All 20 rows carry `plan_ref IS NULL` and the plain unique index on `(engagement_id, plan_ref)`
exists over them without conflict. That only holds under NULLS DISTINCT, which is what the
partial predicate would otherwise have bought. Confirmed by the coexistence, not inferred.

### Scope limit on the upsert proof, stated plainly

My probes ran as `current_user = postgres` and then as `service_role`; neither is PostgREST. The
`42P10` risk is settled regardless of role — it is raised by the planner from the column list,
and PostgREST emits the same `ON CONFLICT (cols)` form. What is **NOT VERIFIED** is the end-to-end
product write path through PostgREST, because i3's planned-work writer does not exist yet
(deferred by CR-005 3.1a) and no agent token is obtainable this run.

## Advisors

`get_advisors` was run for both types on `onpvolboecjpdkvurjaf`. Every finding, with its level.

### Security — 4 findings, 0 introduced by this migration

| Level | Finding | Introduced by this migration? |
|---|---|---|
| WARN | `public.rls_auto_enable()` executable by `anon` as SECURITY DEFINER via `/rest/v1/rpc/rls_auto_enable` | NO — created in `20260819144540_rls_and_grants.sql`, 2026-08-19 |
| WARN | `public.rls_auto_enable()` executable by `authenticated`, same function | NO — same origin |
| WARN | Leaked-password protection disabled (HaveIBeenPwned check) | NO — auth setting, untouched |
| INFO | `app.rate_limit_counters` has RLS enabled but no policies | NO — pre-existing |

Attribution was measured, not assumed: `grep -rln rls_auto_enable supabase/migrations/` returns
only `20260819144540_rls_and_grants.sql`, and the i1 migration mentions it 0 times. Control on
that grep: the same file mentions `work_item` 29 times, so the grep is not blind.

### Performance — 10 findings, all INFO, all `unused_index`, 1 introduced by this migration

Nine are pre-existing: `contract_milestone_due_date_idx`, `blocker_open_idx`,
`work_item_executor_kind_idx`, `work_item_requirement_ref_idx`, `test_case_covers_idx`,
`open_question_open_idx`, `audit_log_created_at_idx`, `audit_log_target_idx`,
`defect_open_idx`.

**One is new and this run owns it:** `work_item_planned_updated_idx` on `public.work_item`,
level INFO, "has not been used". Reported as introduced rather than waved past. It is expected
and I judge it benign: the index exists for FR-91's staleness scan, whose reader is i4 and is
deferred, so nothing has queried it yet — an index built ahead of its reader cannot show usage.
It should be re-checked once the staleness reader ships; if it is still unused then, it is a
real finding.

Notably `work_item_engagement_plan_ref_key` is **not** flagged, consistent with it having been
exercised by the upsert probe above.

## Transit and header observations

**NOT VERIFIED for this unit's branch — there is no deployment of it to read headers from.**
Recorded as absent rather than omitted, and not substituted with a reading from a different
tree's deployment, which would describe code this unit did not verify.

Headers were not read off production either: production serves `e3de4be`, which is not this
run's code, so a header reading there says nothing about d1's work. Per the repo rule, any such
reading must be taken with `vercel curl` rather than plain `curl` — deployment protection
answers first with its own `302` to `vercel.com/sso-api` carrying an HSTS header of its own, and
the tell is `preload`, which Vercel's HSTS has and `next.config.ts` deliberately omits. The
`vercel curl` probes above were used for exactly that reason.

| Header | Observed value | Present? |
|---|---|---|
| Strict-Transport-Security | not read | NOT VERIFIED — no deployment of this branch |
| Content-Security-Policy | not read | NOT VERIFIED — no deployment of this branch |
| X-Content-Type-Options | not read | NOT VERIFIED — no deployment of this branch |
| Referrer-Policy | not read | NOT VERIFIED — no deployment of this branch |
| Set-Cookie flags | not read | NOT VERIFIED — no deployment of this branch |

`NEXT_PUBLIC_`-prefixed and secret-shaped: none observed. No environment variable was set,
read, or changed by this unit — it is a verification unit and touched no Vercel configuration.
Credential expiries recorded: not applicable, no credential was set this run. No value of any
kind was printed, echoed, truncated or written to any file, including this report.

## Auth posture carried forward

- `disable_signup`: **verified by observation on 2026-08-17** with the three-part instrument
  recorded in `prod.md` B8 — settings flag, the discriminating error code (`signup_disabled`,
  not `weak_password`; both open and closed states answer `422`, so the code discriminates and
  the status does not), and `auth.users` row count. Not re-litigated here and **not changed**.
  This migration cannot have affected it: it references `auth.` 0 times (control: it contains 4
  `alter table` statements, so the grep is not blind).
- Current state, observed this run: `auth.users` = 1, `public.operator` = 1,
  `auth.mfa_factors` verified = 1 — consistent with B20 resolved and one MFA-enrolled operator.
- Agent-token positive path: **NOT VERIFIED**, carried forward verbatim from 7c. The boundary is
  **proven to refuse and NOT proven to admit** — negative controls returned `401` no-auth and
  `401` bad-auth against production. No token was sought.
- `pnpm gate:m27:e2e`: **NOT VERIFIED — e2e gate is orchestrator-run post-merge.** Not run. It
  needs `M27_STORAGE_STATE`, which is gitignored and structurally unavailable from a worktree,
  and its unset-variable failure signature (10 failed / 10, all "rendered the operator gate") is
  byte-identical to a revoked session and would establish nothing.

## Vercel

- Project: `project-tracker` (`prj_bekdkkVtS2DhP9t67QSyuBxp6kd7`), team `erik-capital-ready-advisors`, authenticated as `erik-capital-ready-advisors`
- This worktree is **not** linked (no `.vercel/`); the shared checkout is, and read-only CLI queries were run from there
- Preview URL for this branch: **none exists.** The branch was never pushed, so Git integration had nothing to build.
- Current production deployment: `dpl_CW9W9JLPNhtEMwww9J9XszyFRzUX`, target `production`, `readyState: READY`, aliased to `project-tracker-mu-livid.vercel.app` and `project-tracker-git-master-...`
- Its commit, read from the Vercel API rather than inferred from timestamps: `githubCommitSha = e3de4bed86d46a65872295aca50828aa075d6fee`, `githubCommitRef = master`

I checked the SHA explicitly because the timestamp evidence alone was a 3-second coincidence
between the commit and the deployment, and a coincidence is not an identification.

## Is production harmed by the database being ahead of it?

Assessed, because a migration applied to a production database ahead of the code is the shape of
a live incident. My judgement is **no**, on this evidence:

- Every change is additive or widening. Dropping `NOT NULL` cannot break a reader, and all 20 rows are still non-null.
- The four new columns all have defaults or are nullable, so existing INSERTs with explicit column lists are unaffected.
- The new unique index spans all-NULL keys under NULLS DISTINCT, so it cannot collide.
- The CHECK only bites `plan_reconciliation = 'keyed'`, which no shipped code writes.
- The trigger only sets a column production does not read.
- Production reads `work_item` through **explicit column lists**. The only `select("*")` match in the production tree is inside a comment in `src/lib/server/releases/persist.ts` saying it is *not* used there — per the repo lesson, a match in prose is evidence of the opposite.
- The project imports **no zod** at all, so no strict schema can reject the four extra columns.
- Observed: production root `200`, auth boundary `401`, unknown route `404`, all after the migration.

Grep discipline on that check: my first pass returned zero for both the pattern and its control,
which per the repo rule means blind, not clean. I re-ran with a control that must match
(`export`, 166 files) and confirmed the tooling worked and that zod genuinely is absent. The
original "control" was itself a zod pattern, which is why it was worthless.

## Issues

1. **`public.rls_auto_enable()` is EXECUTE-able by `anon` and `authenticated`** (2 WARN advisors, and a live `=X/postgres` PUBLIC entry in its ACL). This violates the repo's own standing per-name REVOKE rule. **Pre-existing since 2026-08-19; not introduced by this run** and not fixed here, because this unit is verify-only and grants are a schema write. Queued.
2. **One new INFO advisor introduced by this migration** — `work_item_planned_updated_idx` unused. Expected, benign now, re-check when the FR-91 reader ships.
3. **No preview deployment exists for this branch** because the branch is unpushed. Queued.

## Prior fleet learnings used

- `Knowledge/PostgREST upsert cannot use a partial unique index, and it fails only on the second write.md` (Evidence line present, run b0952e, 2026-08-19) — told me to check `indexdef` for a `WHERE` on anything upserted against, and predicted the exact remedy i1 used. **CONFIRMED**: the new conflict target is plain, and the second post succeeded with no `42P10`.
- `Knowledge/Rename migration files to the version Supabase recorded when applying through the API.md` — **CONFIRMED**: all 13 filenames match their recorded versions, so no `db push` re-run risk exists.
- `Knowledge/New Supabase projects auto-enable RLS by event trigger, so RLS-on is not evidence a migration did it.md` — **CONFIRMED and applied**: I asserted the policy, roles and grants rather than the `relrowsecurity` flag.
- `Knowledge/A Postgres CHECK constraint runs as the caller, so its function needs a grant to the writing role.md` — **CONFIRMED as avoided**: the new CHECK is a plain column predicate with no function call, and the `service_role` write path was exercised with no `42501`.
- `Knowledge/A redeploy does nothing until the branch is merged - verify deploy state, not the claim.md` — the rule this whole report turns on. **CONFIRMED**: the branch is unpushed, production serves a different commit, and the status is NOT LIVE.
- `Knowledge/Run the probe under the conditions the question is about - a lucky right answer feels identical to a verified one.md` — **CONFIRMED**: it is why I re-measured the frozen-`now()` false negative instead of reporting a broken trigger.
- Repo `CLAUDE.md` lessons on blind scans and on prose matches — both fired for real this run (the zod control, and the `select("*")` comment).

## Files created / modified

No product code, schema, configuration or environment variable was created or modified by this
unit. Stating that explicitly rather than omitting the section. The only files written are this
unit's own artifacts:

- `.fleet/specialist-reports/d4000f/d1.md` (this report)
- `.fleet/questions-d1-d4000f.jsonl` (5 queued questions)

No migration was applied, re-applied, repaired or pushed. No deployment was created, promoted or
aliased. No domain or DNS record was touched. No auth setting was changed.

## Questions queued

5 questions, written to `.fleet/questions-d1-d4000f.jsonl` in this worktree:

1. `public.rls_auto_enable()` carries a live PUBLIC EXECUTE grant, against the repo's standing rule — pre-existing, should a follow-up unit revoke it?
2. Leaked-password protection disabled — is provider default the intended 7a posture?
3. `app.rate_limit_counters` has no 7a row and RLS with zero policies — is schema `app` deliberately outside the roster?
4. Who owns pushing this branch and producing its preview deployment?
5. Is migrate-first-then-merge the intended ordering for M2.9, given the live database is now ahead of production?

## Best-guess decisions

1. **I did not create a preview deployment, though my role definition normally deploys one.** Confidence: high. The branch is unpushed, so a `vercel deploy` from the worktree would produce a deployment with no git metadata — not "the preview deploy for this branch", and misleading in the record. It would also publish code that has not passed the run's gates. My brief scopes this unit to verify-and-report, and manufacturing the artifact I was asked to verify is the wrong move. Reported the absence with its cause instead. Reversible: the orchestrator pushes and Git integration builds it.
2. **I did not treat the one new INFO advisor as a unit failure.** Confidence: high. It is `unused_index` on an index whose reader CR-005 3.1a explicitly defers. Reported as introduced-by-this-migration rather than filtered out, so the orchestrator can overrule me.
3. **I did not treat `app.rate_limit_counters` as a 7a blocker.** Confidence: medium. The "no row in 7a is a blocker" rule is scoped in my brief to tables *this migration created*, and i1 created none. It is pre-existing, in schema `app` rather than `public`, and unreachable via PostgREST. Queued rather than blocked.
4. **I ran mutating probes against the live production database inside rolled-back transactions.** Confidence: high that this was correct and safe. A trigger, a CHECK and an upsert conflict target cannot be verified by reading catalogs — "a control you did not observe is not a control", and the `42P10` and `42501` failures this repo has paid for are invisible to a schema read. I proved the rollback afterwards rather than trusting it: 20 rows, 0 probe rows, `max(updated_at)` unchanged at the migration timestamp.
5. **I read `prod.md` for the `disable_signup` verdict instead of re-measuring it.** Confidence: high. It was verified by observation on 2026-08-17 with the discriminating error code, the migration touches `auth.` 0 times, and re-measuring would have meant handling an API key for no new information.
