# api-integrator report — i5

**Status:** DONE
**Run:** d4000f
**Branch:** agent-build/2026-08-24-d4000f
**Worktree:** `/Users/erikmeltzer/Projects/project-tracker/.claude/worktrees/agent-abae78e8e7b569b68`

## Summary

Fixed the one `important` QA finding: `POST /api/ingest/run` could tell an agent "Nothing else was
changed" after `persistPlan` had already committed the whole run. The sentence is now scoped to the
FR-90 reconciliation and says positively what survived, matching the shape the partial-write branch
two lines below it already used. One test, written and run red before the fix. No schema change, no
migration, no route file touched.

## Step zero — the branch guard, and a pushed-commit case

- HEAD **before** reset: `e3de4be` ("Merge pull request #4 from
  erik-capital-ready-advisors/docs/b46-verified-in-production")
- `git status --porcelain`: **empty**
- `git log --oneline agent-build/2026-08-24-d4000f..HEAD` **printed one commit**, so I did not reset
  blindly. `git branch -a --contains e3de4be` returned `remotes/origin/master` — **the commit is
  pushed, a reset could not destroy it**, which is the case the brief said to flag prominently.
  Flagging it: this worktree was cut from `origin/master`, not from a local-only commit.
- HEAD **after** reset: `64628b2` ("fleet d4000f: Wave D fan-in — manual gate PASS, B-P3 transcript
  exposure, B-P4 stale-evidence gap")
- `git merge-base --is-ancestor 64628b2 HEAD` printed **HAVE_BRANCH**.

## Correction to the brief — the QA report is NOT committed

The brief states the QA report "is committed on the branch, so it resolves inside your worktree
after the reset." It does not. Verified from three vantages:

- `git ls-tree -r --name-only HEAD | grep qa-report` returns only `29b583`, `b0952e`, `eb2490`.
- `git log --all --oneline -- '.fleet/qa-report-d4000f.md'` returns **nothing** — no commit on any
  ref has ever carried that path.
- The file exists **untracked in the shared checkout** at
  `/Users/erikmeltzer/Projects/project-tracker/.fleet/qa-report-d4000f.md`, 30605 bytes, dated
  Aug 24 11:51.

I read it read-only from that path because it exists nowhere else, and wrote nothing into the shared
checkout. Worth the orchestrator's attention: a `git worktree prune` or a clean checkout loses this
run's QA report, which is the same trap the report gate's own header documents for queued questions.

## Files created / modified

- `src/lib/server/planned-work/mark-collisions.ts` (modified) — read-failure message scoped to the
  reconciliation; comment records why the claim was false in both call directions
- `src/lib/server/planned-work/mark-collisions.test.ts` (modified) — one test added, run red first
- `.fleet/questions-i5-d4000f.jsonl` (created, previously untracked) — 3 queued questions
- `.fleet/specialist-reports/d4000f/i5.md` (created, previously untracked) — this report

`git diff --stat` against `64628b2` for the source change: **2 files, +50 / -1**.

## The exact response shape, before and after

Both strings observed from the thrown `ApiError`, printed by vitest's `Received:` line — not
transcribed from source. `withAgentRoute` carries the message verbatim into
`{"error":{"code":"internal_error","message": ...}}` at HTTP 500.

**Before:**

> The FR-90 reconciliation could not read this engagement's work items, so no collision mark was
> written. Nothing else was changed.

**After:**

> The FR-90 reconciliation could not read this engagement's work items, so no collision mark was
> written. Everything this request wrote before this step stands and was not rolled back. Re-post to
> complete the marking; the writes are upserts on a natural key, so nothing is duplicated.

The two outcomes the caller must distinguish are now distinguishable: a 200 carrying
`reconciliation.marked` means persisted **and** marked; this 500 means persisted **and** marking
failed, and it says so. Neither reads as "nothing was changed".

**The re-post advice is verified, not hoped.** `persist.ts` is upsert-on-natural-key throughout
(`onConflict: "engagement_id,run_id"`, `"engagement_id,fleet_run_id,unit"`, and nine more), and the
plan path is `ON CONFLICT DO NOTHING` on `(engagement_id, plan_ref)`. Re-posting does not duplicate.

## Why the wording is caller-agnostic rather than QA's literal suggestion

QA suggested "The run was persisted." `markPlanCollisions` is shared: `POST /api/ingest/run` reaches
it after `persistPlan`, and `POST /api/ingest/plan` reaches it after `ingestPlanDocument`. Naming
"the run" would have made the message false on the plan path — the same class of error, relocated.
The wording chosen is true in both directions, needs no signature change, and left the plan route
byte-identical. Recorded as a decision below and queued for Erik.

## Endpoints and actions added

None. No route file was modified. `src/app/api/ingest/run/route.ts` is unchanged at `64628b2`.

## Schema changes

**None.** No migration, no table, no column, no grant, no policy. The unit is a message change plus
a test.

## Security posture applied

| Table | Class | Source | At rest | Change by this unit |
|---|---|---|---|---|
| `work_item` | sensitive | spec §7a | pgcrypto on `description`, `raw_status` | none — untouched |

Controls from §7a: 1 reasserted · from baseline: 0 · NOT VERIFIED: 0 new controls introduced.

The §7a rule that bound this unit was "an error message must not echo back row prose it just read."
The corrected message is a fixed string. It interpolates nothing, carries no Postgres text, no table
values and no `work_item` prose, and it does not pass through the driver's `error.message`. The new
test asserts the negative directly: with the fake db failing as `relation "work_item" does not
exist`, the caller-facing message does **not** contain `does not exist`. That assertion passes. i3's
`Omit<..., "inputs">` trap was not re-introduced — no object carrying row prose is constructed here.

## Env vars required (hand to devops)

None. This unit adds no configuration.

## DATA slots filled

None — no `ui_reports` were supplied and this unit touches no UI.

## Verification

Ordering is stated honestly: **the test was written first and run red before the fix existed.**

- **RED (before the fix, test written first):** `pnpm vitest run
  src/lib/server/planned-work/mark-collisions.test.ts` → **1 failed | 10 passed (11)**. The failure
  was the intended assertion, not a compile error:
  `AssertionError: expected 'The FR-90 reconciliation could not re…' not to contain 'Nothing else
  was changed'`, with vitest printing the real current sentence on its `Received:` line.
- **GREEN (after the fix):** same command → **11 passed (11)**.
- Build: **PASS**, `BUILD_EXIT=0`
- Type-check: **PASS** — `tsc --noEmit`, no output
- Lint: **PASS** — `oxlint`, `LINT_EXIT=0`
- Tests: **1815 passed | 6 skipped (1821)**, 120 files passed / 1 skipped. Baseline was 1814 passed
  / 6 skipped, so the count rose by **exactly 1**, the one test added. Nothing else moved.
- `pnpm gate:m27`: **5 passed (5)** — matches the 5/5 baseline
- Served page routes: **31** non-API routes in the build output — matches the baseline of 31
- `manual-gate.sh`: **NOT RUN** — the script is not present in this worktree, the repo, or
  `~/.claude`; it is an orchestrator tool. The page-route count it keys on is unchanged at 31.
- `pnpm gate:m27:e2e`: **NOT VERIFIED** — e2e gate is orchestrator-run post-merge. It needs
  `M27_BASE_URL` and `M27_STORAGE_STATE`, and the storage-state file is gitignored, so it is
  structurally unrunnable from a worktree. Not attempted, and no dev server was pointed at the
  shared checkout.
- QA suite in `e2e-review/`: **not run**, per the brief — separate config, needs a session, and one
  suite is committed red on purpose.
- Authenticated round-trip against the live route: **NOT VERIFIED** — §7c records the agent token as
  DEGRADED, negative controls only. No valid token is obtainable by this run and `.env.local` is
  read-denied by design. I did not go looking for one. The boundary is proven to refuse and **not**
  proven to admit; the changed sentence is therefore verified at the unit level and has not been
  observed coming off the wire.
- RLS probe: **N/A** — no schema change.
- Column encryption / deletion path / rate limit / security headers / new-account role: **N/A for
  this unit** — none introduced or altered. No control was claimed that was not observed.

**Probe proven able to fail: YES.** The red run is the proof — the assertion failed against the
unfixed code and named the exact string, so it is not a test that passes vacuously. Separately, to
quote the post-fix message from observation rather than from source, I temporarily inserted a
deliberately failing `toContain("ZZZ_PROBE_PRINT_ACTUAL")` to make vitest print the actual value,
then removed it. Removal confirmed: `grep -c ZZZ_PROBE` returns **0** and `git diff --stat` is back
to 2 files / +50 / -1.

## Scope confirmation

I touched **nothing** outside the FR-90 error path. Specifically **not** modified, verified by
`git status --porcelain` showing exactly two modified files:

- `src/app/api/ingest/run/route.ts` — unchanged; i3's collision marking at `:77` is **kept**, not
  reverted, as QA directed
- `src/app/api/ingest/plan/route.ts`, `persist.ts`, `persistPlan`, `reconcile.ts` — all unchanged
- No migration, no schema change
- **No `minor` finding was touched.** Not the axe/contrast findings, not the `MainNav` /
  `MobileNav` / `ThemeToggle` mock findings, not the stale comment at `database.types.ts:66`. All
  nine remain Erik's to triage.

## Questions queued

Three, written to `.fleet/questions-i5-d4000f.jsonl` in this worktree (validated as JSON, every row
carrying `"unit": "i5"`):

1. Should the run route answer 200 with a `reconciliation.failed` marker instead of a 500, so the
   persisted counts and FR-58's `unparsed` count survive a marking failure? The current 500
   discards them.
2. Is caller-agnostic wording the right call given `markPlanCollisions` is shared, or should each
   caller name what it committed?
3. Adjacent and deliberately not fixed: an unexpected non-`ApiError` thrown after the commit still
   yields the generic "The request could not be completed.", which is the same wrong "not done"
   shape. Unraised by QA, so flagged rather than widened.

## Prior fleet learnings used

- `Knowledge/PostgREST silently truncates unbounded reads at 1000 rows.md` — **CONFIRMED, and
  already applied in the code I read.** The note's claim (HTTP 206 with `error === null` past
  `max-rows`, so a truncated read is indistinguishable from a complete one) matches
  `mark-collisions.ts` exactly: it reads through `fetchAllRows` with a stable order and an exact
  count, and its docstring cites the same two incidents the note does (Shoe Swap Studio 2026-07-24,
  Cully Bet 2026-08-11). Nothing to change; it told me the read path was already correct so I did
  not touch it while working on the failure branch immediately below it.
- No vault note covers error-message honesty or partial-commit reporting, which is this unit's
  actual surface. Searched for `false statement`, `misleading error`, `wrong not done`, `idempot`,
  `partial success`. Candidate for a new note, since this defect class has now appeared once here.

## Best-guess decisions

1. **Fixed the message rather than restructuring the response.** Confidence: **high**. QA prescribed
   exactly this, the brief said not to invent a new error contract where the route already has a
   partial-success shape, and the sibling partial-write branch nine lines below is that shape. The
   200-with-a-failure-marker alternative is the better long-term contract and is queued as question
   1 rather than taken unilaterally, because it changes status-code semantics.
2. **Caller-agnostic wording instead of QA's "The run was persisted."** Confidence: **high**. The
   function is shared with the plan path, where QA's literal wording would be false. Queued as
   question 2.
3. **Kept the existing test's `toContain("no collision mark was written")` substring intact** so the
   sibling assertion at `mark-collisions.test.ts:197` stays green on its own terms rather than being
   edited to fit my change. Confidence: **high**. Editing a passing assertion to accommodate a new
   message is the move this repo forbids for fixtures, and the same logic applies.
4. **Did not broaden the catch to non-`ApiError` throws.** Confidence: **med**. Swallowing unknown
   errors into a partial-success report would hide real bugs, and QA named the `ApiError` path
   specifically. Queued as question 3.
5. **Read the QA report from the shared checkout.** Confidence: **high**, and stated rather than
   quiet. The worktree rule exists because two trees can hold different content at one path; here
   the path exists in exactly one tree, so there is no ambiguity about provenance. I wrote nothing
   there.
