# api-integrator report - i2

**Status:** DONE
**Run:** b0952e
**Unit:** i2 (`i-core`) - M1.4 Mode 1 ingest (FR-14 to FR-23), plus the pure rules inside M1.6, M1.7 and M1.8
**Branch:** worktree of `agent-build/2026-08-19-b0952e`
**Final code SHA:** `b09378a` (this report and the questions file are committed on top of it)

## Summary

All 13 tasks of `plan.md` are implemented test-first as `src/lib/ingest/` - 13 pure
modules, 93 colocated tests, every title naming the requirement it covers. The package
touches no filesystem, no network, no database and no framework; the single exception is
the Tier-2 corpus test, and it is a test.

**87 tests pass, 6 skip.** The 6 that skip are the entire Tier-2 corpus suite, because
`fixtures-local/` is absent on this machine. That is reported as SKIPPED, never as
passing.

## Files Created / Modified

Created, all under `src/lib/ingest/`:

| File | What it is |
|---|---|
| `types.ts` + `.test.ts` | Domain types, the six runtime enums, `validateWorkItem` |
| `markdown.ts` + `.test.ts` | `tableRows` - the markdown table reader |
| `status.ts` + `.test.ts` | `classifyStatus` - the unparsed discipline |
| `refs.ts` + `.test.ts` | `requirementRefs` - range expansion, three separator forms |
| `workUnits.ts` + `.test.ts` | `parseWorkUnits` |
| `blocked.ts` + `.test.ts` | `parseBlocked` |
| `questions.ts` + `.test.ts` | `normalizeQuestions` - six record shapes to one |
| `requirements.ts` + `.test.ts` | `parseRequirements` |
| `testTags.ts` + `.test.ts` | `parseTestTags` - FR-45 traceability |
| `coverage.ts` + `.test.ts` | `indexCoverage`, `untestedReport` - the certifier rule |
| `billing.ts` + `.test.ts` | `milestoneState` - the invoice gate |
| `waits.ts` + `.test.ts` | `daysWaiting`, `isOverdue`, `projectMilestone` |
| `ingestRun.ts` + `.test.ts` | The orchestrator, still pure |
| `ingestRun.corpus.test.ts` | Tier-2, skips when `fixtures-local/` is absent |
| `__fixtures__/manifest.ts`, `__fixtures__/questions.ts` | Tier-1 shape fixtures, no client detail |

Modified: `.gitignore` (added `fixtures-local/`, per Task 13, with a comment naming why).

**Nothing outside `src/lib/ingest/` and `.gitignore` was touched.** No `supabase/`, no
`src/app/`, no `src/components/`, no `package.json`, no `tsconfig.json`, no
`vitest.config.ts` - the scaffold's `include` already covered `src/**/*.test.ts`, so no
config change was needed.

## Endpoints and actions added

None. This unit is pure functions only - no route handlers, no server actions, no schema.
The consumers `plan.md` names (`POST /api/ingest/fleet-run`, the Untested and Committed
screens, milestone dates) are other units' work and call into this package.

## Schema changes

None. `supabase/` was not touched - unit `i1` is building the schema in parallel.

## Security posture applied

No tables, no columns, no endpoints, no credentials in this unit. The one place §7a bears
on it is the fixture tiering, and it is honoured:

| Control | Source | What was built | Verified |
|---|---|---|---|
| No client prose committed | §7a (`work_item`, `blocker`, `open_question` = `sensitive`) + open spec Q2 | Tier-1 fixtures hand-authored with tooling detail only; Tier-2 real artifacts confined to gitignored `fixtures-local/` | YES - `git ls-files \| grep fixtures-local` returns nothing; the directory never appeared in `git status` even while populated |
| No secrets in code | CLAUDE.md credentials rule | Package reads no `process.env` at all | YES - grep below |

Controls from §7a: 1 · from baseline: 1 · **NOT VERIFIED: 0**
Unclassified tables I refused to guess at: none - this unit creates no tables.

## Env vars required (hand to devops)

None. This package reads no environment.

## DATA slots filled

None assigned - no `ui_reports` were passed to this unit, and this unit writes no components.

## Verification

- **Build:** PASS - `pnpm build`, exit 0, 12 static routes emitted.
- **Type-check:** PASS - `pnpm tsc --noEmit`, exit 0.
- **Lint:** PASS - `pnpm lint` (oxlint) clean. It was **not** clean on first run; see
  Best-guess Decisions.
- **Tests (this package):** `pnpm vitest run src/lib/ingest` -
  `Test Files 13 passed | 1 skipped (14)`, `Tests 87 passed | 6 skipped (93)`.
- **Tests (whole repo):** `Test Files 15 passed | 1 skipped (16)`,
  `Tests 98 passed | 6 skipped (104)` - `u1`'s component tests still pass.

### Tier-2 corpus suite: SKIPPED, not passed

`fixtures-local/` does not exist on this machine, so all six corpus assertions - 31 work
items, 0 unparsed, 45 questions, 3 answered, 61 requirements, 0 invalid records - **did
not run.** The measured Tier-2 ground truths in `plan.md` are therefore **unverified by
this unit.** Queued as a question for Erik.

**The skip was proved to be a real skip.** A `skipIf` that always skips is
indistinguishable from one that skips correctly, so I populated `fixtures-local/` with
deliberately wrong data and re-ran: the suite executed and 4 of 6 assertions failed
(`Tests 4 failed | 2 passed (6)`). Directory then removed. The predicate flips.

### Unparsed count produced by my own suite against the Tier-1 fixtures

**0 unparsed**, asserted by `ingestRun.test.ts` -
`it("FR-15 reports how many rows it could not classify")` expects `run().unparsed` to be
`0` across 14 work items from two manifests plus a blocked table. Every status shape in
the Tier-1 fixtures classifies. Two further tests pin the discipline from the other side:
`classifyStatus("**frobnicated** - see the note above")` is `unparsed`, and a row with the
wrong column count is `unparsed` rather than silently misaligned.

### Purity - proved by grep, output shown

```
$ grep -rn "node:fs\|node:path\|@supabase\|process\.env\|new Date(\|fetch(" \
    src/lib/ingest/ --include=*.ts | grep -v "\.test\.ts"
(no matches - package is pure)
```

The only `node:fs` / `node:path` anywhere in the package:

```
src/lib/ingest/ingestRun.corpus.test.ts:1:import { existsSync, readdirSync, readFileSync } from "node:fs";
src/lib/ingest/ingestRun.corpus.test.ts:2:import { join } from "node:path";
```

That is the corpus test, and `plan.md` exempts it explicitly.

### No client prose committed - proved

```
$ git ls-files | grep fixtures-local
(nothing)
$ git check-ignore -v fixtures-local
.gitignore:27:fixtures-local/	fixtures-local
```

### `any` and non-null assertions

```
$ grep -rn ": any\|<any>\|as any" src/lib/ingest/
(none)
$ grep -rn ")!" src/lib/ingest/ --include=*.ts | grep -v "\.test\.ts"
src/lib/ingest/requirements.ts:25:  return [...seen.keys()].sort((a, b) => a - b).map((n) => seen.get(n)!);
```

Exactly one non-null assertion, and it is the one `plan.md` Task 8 sanctions.

### Every test title names its requirement

The only `it("...")` in the package not starting with `FR-` is
`it("renders a PDF", () => {});` at `testTags.test.ts:9` - and that is **fixture data
inside a template literal**, the deliberately untagged input `parseTestTags` must read as
covering nothing. Every actual test title carries its requirement.

### Mutation proofs - every task, and one the plan did not ask for

`plan.md` requires proving each new test can go red. Done for all 13 tasks. Named
example, the load-bearing one:

> **Task 3, `status.ts`:** changed the classifier's initial `status` from `"unparsed"` to
> `"done"` - the exact failure the product exists to prevent. Result:
> `× FR-15 returns unparsed for prose it does not recognize`, `Tests 1 failed | 7 passed (8)`.
> Reverted; back to 8 passed.

Full set: T1 `validateWorkItem` returns `[]` → 3 failed. T2 heading-break removed → 2
failed. T3 above → 1 failed. T4 lexical sort → 1 failed. T5 run scope dropped from id → 1
failed. T6 `BLOCKER_ID` loosened to `/\b(\d+)\b/` → 1 failed. T7 status always `"open"` →
1 failed. T8 first-mention guard removed → 1 failed. T9 `title.match` → `file.source.match`
→ 3 failed. T10 self-certification branch removed → 2 failed. T11 `covered` → `claimed` in
the missing filter → 1 failed (this one would have made a self-certified milestone read
`billable` - the invoice gate failing open). T12 resolved-wait filter removed → 1 failed.
T13 `parseBlocked` items dropped → 1 failed.

**One honest correction to the plan:** Task 13 Step 7 predicts *"the 14-item count and the
collision test"* both fail. Only the 14-item count failed. Dropping blocked rows removes
ids rather than duplicating them, so the collision assertion cannot detect it. The code is
right; the plan's prediction was one test too optimistic.

**Runner-level control** (from the vault note on mutation-proving): confirmed
`pnpm vitest run <nonexistent>` exits `1`, not `0`, so a bad path glob cannot make this
suite decorative while reporting green. Measured directly rather than through a pipe -
the first attempt read `tail`'s exit code and printed a false `0`.

### Controls not applicable to this unit

RLS probe, column encryption, deletion path, rate limit, security headers, new-account
role, TLS: **N/A** - this unit creates no schema, no endpoint and no network call. Nothing
here is an unverified claim; there is no control to verify.

## Questions Queued

Written to `/Users/erikmeltzer/Projects/project-tracker/.claude/worktrees/agent-af9a7210a66a86493/.fleet/questions-i2-b0952e.jsonl` (2 lines, both carrying `"unit": "i2"`).

1. **`parseBlocked` hardcodes every blocker's `owner` to `"client"`** (confidence: low).
   FR-52 and Flow A1 group the Blocked screen by owner *specifically* to separate the rows
   Erik owns from the rows a client or vendor owns - and in the reference corpus B1 is a
   hosting/provisioning blocker, which `CLAUDE.md` states is Erik's decision, not a
   client's. Shipping `owner: "client"` for every manifest-derived blocker sends Erik's own
   blockers into the client bucket on the screen that answers *"what is Erik the bottleneck
   on."* I followed `plan.md` verbatim rather than guessing a different default, and
   flagged it here. **This is the highest-value item in this report for `i3` /
   `project-lead` to resolve.**
2. **Tier-2 corpus verification needs Erik** (confidence: high). `fixtures-local/` is
   absent; the six measured ground truths are unverified. One `cp` and one `vitest run`
   settles it.

## Prior Fleet Learnings Used

- `Knowledge/A certifier check must join through the requirement to the work item that implemented it.md`
  — **CONFIRMED, and it changed the shipped test suite.** The note documents a real prior
  bug: comparing `certified_by` against *every executor in the engagement* rather than
  against the executor of the work implementing *that requirement*. On a real run
  `qa-reviewer` is itself a work-unit executor, so the widened form rejects every
  legitimate QA certification and leaves **every milestone permanently un-billable.**
  `plan.md`'s `coverage.ts` has the correct per-requirement join - but its fixture has only
  one work item, so **none of its seven tests can detect the widened form.** I verified
  this by writing the widened version: all seven of the plan's assertions still passed.
  I added one test beyond the plan,
  `it("FR-47 still accepts qa-reviewer when qa-reviewer executed some other unit")`, and
  confirmed it fails under the widened join and passes under the correct one. This is the
  single deviation-by-addition from `plan.md` in the unit, and the note is why.
- `Knowledge/A green test suite proves the tests ran, not that they can fail - mutate the code to prove the assertions bite.md`
  — **CONFIRMED.** Drove the 13 mutation proofs above, the runner-level no-match control,
  and the decision to prove the corpus `skipIf` predicate can flip rather than trusting a
  reported skip.
- `Knowledge/TypeScript 7 removed baseUrl outright and no longer auto-includes node_modules types.md`
  — **CONFIRMED, no action needed.** The `u1` scaffold already carries both fixes
  (`paths` without `baseUrl`, explicit `types: ["node"]`), which is why
  `node:fs` in the corpus test resolves and `tsc --noEmit` is clean. Recorded so the next
  unit does not rediscover it.

## Best-guess Decisions

1. **Followed `plan.md` verbatim on `blocker.owner = "client"`** despite believing it
   conflicts with FR-52. `plan.md` wins on details, and this is a semantic default that
   changes which bucket a row lands in on a screen - not mine to flip in a worktree.
   Queued instead.
2. **Fixed two `oxlint` errors that arrived with `plan.md`'s verbatim code**
   (`no-unused-vars` on a destructured `executorKind` in `types.test.ts`, and on the
   `today` parameter of `projectMilestone`). Pre-existing code lints clean, so I introduced
   them and a red `pnpm lint` would land on the shared branch. Fixed **inside my own files
   only** - I did not touch `.oxlintrc.json`, which is not my lane. `today` was renamed to
   `_today` rather than removed: it is unused on purpose, and deleting it invites a caller
   to reach for `new Date()` to fill a gap that does not exist. Arity and the plan's stated
   signature are unchanged. Committed separately as `b09378a` so the change is legible.
3. **Kept the one non-null assertion** in `requirements.ts` rather than the alternative
   `plan.md` offers, because the plan's "Done when" is written to expect exactly one.
4. **Did not add a `db-probe` or `manual` harness path** to `parseTestTags` even though
   `TestCase["harness"]` allows both. `plan.md` specifies only the vitest/playwright split
   and no fixture exercises the others; inventing a detection rule would be guessing at a
   shape no artifact uses.

## Where CR-001 (`i3`) attaches

`i3` builds the QA-findings parser (FR-64), `D-nn` tag reading, the defect verification
join (FR-66), regression derivation (FR-69), shipped-state derivation (FR-74) and the
`contested` rule (FR-79). Module boundaries were left so none of that reshapes my types:

- **FR-64 QA findings** — a new `qaReports.ts` beside `blocked.ts`. It should reuse
  `tableRows(text, heading)` unchanged; the reader is heading-agnostic and already stops at
  the next `##`.
- **`D-nn` tags** — mirror `refs.ts`. `requirementRefs` is deliberately FR-only; add a
  sibling `defectRefs` rather than widening the existing regex, since widening a pattern is
  the failure the unparsed rule exists to prevent.
- **FR-66 defect verification join** — belongs in `coverage.ts`, and should reuse the
  `implementers()` map rather than rebuilding one. **Read the vault note above first:** the
  same widened-join bug is available in the defect join, and my new control test is the
  pattern to copy.
- **FR-69 / FR-74 derivations** — new modules; `ingestRun.ts` is the seam. `RunResult` is a
  flat record-array shape, so adding `defects` / `regressions` fields is additive and
  breaks no existing consumer.
- **FR-79 `contested`** — this is the "two artifacts disagree, record both" rule
  (`manifest-cd414c` says `u4` is `pending`, `checkpoint-cd414c` says it merged). Nothing
  in my code reconciles duplicate units today: `ingestRun` scopes ids by
  `engagement:run:unit`, so two artifacts describing the same unit in the same run would
  collide on id. `i3` should decide that at the `ingestRun` level. **I deliberately did not
  build reconciliation** - it is CR-001's, and guessing at it would have destroyed the
  disagreement the spec calls data.

## Git state

- 14 code commits, one per task plus the lint fix. Last code SHA `b09378a`; this report
  and `questions-i2-b0952e.jsonl` land in one commit on top.
- `git status --porcelain` is **empty**. Nothing uncommitted.
- Reset at start: `c835cf9` → `6cb60fa` (both preconditions proved empty first).

**Nothing here is deployed.** This is code on a worktree branch.
