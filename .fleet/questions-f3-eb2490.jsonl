{"unit": "f3", "section": "FR-81 — EntityDetail identity row", "question": "On external_wait, release and contract_milestone the entity's human reference IS its title (the wait label, the release identifier, the milestone name), so <EntityDetail title> and <EntityDetail identifier> render the same string twice. Should identifier be omitted when it equals the title?", "best_guess": "Pass it anyway. `identifier` has no omit state — undefined renders `Absent` with 'this row carries no reference of its own', which would be a false claim about a row whose label IS its reference. The duplication is cosmetic; the false absence is not.", "confidence": "med"}
{"unit": "f3", "section": "§5a typography / money", "question": "This repo has TWO money formatters that disagree on shape: `formatAmount` in registry-display.ts renders `$111.00` (Intl, locale pinned to en-US) and `money` in money-display.ts renders `111.00 USD` (fixed format, ISO code after the figure). /registry/[slug] uses the first and /committed the second, so the same milestone amount reads two ways on two screens. Which one is the product's answer?", "best_guess": "Used `formatAmount`, because the f3 brief names it explicitly and because /milestones/[id] is entered from /registry/[slug]. Flagged rather than resolved: money-display.ts is in this run's no-edit list, and unifying them is a decision about a screen f3 does not own.", "confidence": "high"}
{"unit": "f3", "section": "FR-73 — release.deployed_at", "question": "`deployedAt` is a timestamp and this view renders it at day resolution via `formatDate`. Deployments happen several times a day, so a day is sometimes not enough to order two releases. Should it render at minute resolution instead?", "best_guess": "Day resolution. The brief says to use `formatDate` and not to format dates by hand, and `isoMinute` — the only minute-resolution helper — carries the zone-less-timestamp bug measured in this unit's report. Ordering releases within a day is not a requirement M2.7 states.", "confidence": "med"}
