{"run":"b0952e","unit":"i6","section":"§7a engagement row vs FR-24/FR-26","question":"§7a says agent tokens may read `engagement` name and slug ONLY, and i4's AGENT_ENGAGEMENT_COLUMNS excludes `repo_path` — but FR-24/FR-26 require the ingest endpoint (agent-authenticated) to resolve a posted working directory to an engagement, which needs `repo_path`. May an agent-authenticated request read `engagement.repo_path` server-side for resolution only, never returning it?","best_guess":"Took the restrictive reading and did NOT widen the grant. Resolution now works only from an explicit engagement slug (allowed); a session that names none is stored against `unassigned` and queued for one-click attribution, which is FR-26's own stated behaviour. Adding `repo_path` to AGENT_ENGAGEMENT_COLUMNS restores full directory resolution with no other change.","confidence":"low"}
{"run":"b0952e","unit":"i6","section":"FR-36 external wait resolution","question":"When a wait resolves, what status should its blocked work items take? I move `blocked` -> `pending` and leave every other status untouched.","best_guess":"blocked -> pending, and `external_wait_id` is retained as the record of what blocked the item. An item that reached `done` while the wait was open is not dragged backwards.","confidence":"med"}
{"run":"b0952e","unit":"i6","section":"FR-30 disposition default for mode-2 work items","question":"A session that says nothing about disposition gets `carried`. Is `carried` the right default, or should an unstated disposition be null until Erik decides?","best_guess":"Defaulted to `carried` — FR-30 says every work item carries one, and `carried` means Erik still owns the gap, which is the conservative reading. `closed` is a decision and is never inferred.","confidence":"med"}
{"run":"b0952e","unit":"i6","section":"FR-24 / B4 session hook install scope","question":"Should scripts/claude-session-capture.sh install globally in ~/.claude/settings.json with an allowlist of studio project roots, or per project? This blocks the install documentation, not the code.","best_guess":"Left undecided as instructed. The script works under either scope because it reads the working directory at run time. doc1 writes the install instructions once Erik settles B4.","confidence":"high"}
{"run":"b0952e","unit":"i6","section":"FR-31 stack attribution","question":"The hook does not infer a stack from the file tree; it only reports one when DELIVERY_LEDGER_STACK is set. Should the stack be inferred from the repository's manifest files instead?","best_guess":"No inference. A guessed stack becomes hours attributed to the wrong stack, and FR-31 feeds those hours to the decision about which stack earns its own fleet agent. Absent is honest; a guess is not.","confidence":"med"}
