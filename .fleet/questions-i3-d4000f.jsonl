{"unit": "i3", "section": "CR-005 §3.1 FR-88 — plan-document write path", "question": "Should a planned row created from a plan document carry `work_item.unit`, and if so what? The plan's only per-task handle is the positional `### Task N:` heading, which shifts the moment a task is inserted.", "best_guess": "Write `unit = null`. A positional number in a column named `unit` would look like a stable key and is not one — the same hazard Q13 rules against for the reconciliation key, arriving through a different column. `plan_ref` is the only key this path writes.", "confidence": "med"}
{"unit": "i3", "section": "CR-005 §3.1a FR-90 — where a reconciliation is attempted", "question": "FR-90's trigger is 'an ingested run reports a work unit that a planned row already describes'. That meeting can happen in either order — plan first then run, or run first then plan. Should the collision mark be written on BOTH paths, or only when a plan document is ingested?", "best_guess": "Both. I mark on the plan-document path and also after `persistPlan` in `POST /api/ingest/run`, through one shared function. Marking on only one path leaves a real collision unmarked whenever the run lands second, and the mark's stated meaning ('a reconciliation was attempted and no key existed') is true in both orders. This needs no change to `persist.ts` — `persistPlan` already returns `engagementId`.", "confidence": "med"}
{"unit": "i3", "section": "CR-005 §3.1 FR-89 — plan tasks the parser did not classify", "question": "A `### Task` heading that i2's parser emits as `status: unparsed` has `title: null`. Should it become a planned row anyway (description from the raw heading), or be withheld?", "best_guess": "Withheld and counted, never written. u2's primitive fixes `status = 'pending'`, so writing an unparsed task would assert this product understood a line it did not — a wrong `done`. The response reports the count and the source line numbers so nothing is dropped in silence.", "confidence": "med"}
{"unit": "i3", "section": "CR-005 §3.1 FR-88 — plan step texts", "question": "A plan task carries `- [ ]` steps. §7a models no child entity under `work_item`, so should step texts be folded into the planned row's description, dropped, or persisted somewhere?", "best_guess": "Not persisted in M2.9, and the count reported. One planned row per Task; the steps stay in the plan document. Folding them into `description` makes a paragraph out of a one-line answer to 'what is next', and §7a has no entity for them.", "confidence": "med"}
{"unit": "i3", "section": "spec §7a — agent capability for the plan route", "question": "Should `POST /api/ingest/plan` require a new capability (e.g. `plan:write`) rather than reusing `ingest:write`?", "best_guess": "Reuse `ingest:write`. It grants no new authority: a token holding `ingest:write` can already insert `work_item` rows through `POST /api/ingest/run`, and this route writes the same table under the same §7a class. A new capability would need a new member of the stored capability enum — a migration this unit was told it should not need. Flagged rather than assumed because it is a capability boundary.", "confidence": "high"}
{"unit": "i3", "section": "CR-005 §3.1 FR-89 — a document that is not a plan", "question": "Q12 says a document not of the plan shape is 'rejected whole'. Is that a 400 to the caller, or a 200 carrying the rejection reason and zero rows?", "best_guess": "400 `invalid_request`, naming i2's `NotAPlanReason`. The caller posted something this endpoint cannot act on, and a 200 reporting zero rows written is the quiet answer this product exists to stop being given.", "confidence": "med"}
