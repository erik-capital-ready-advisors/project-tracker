# Production Plan: Delivery Ledger

**Project:** Delivery Ledger — single-operator delivery tracker for Capital Ready Advisors
**Last updated:** 2026-08-24 — spec session, **zero code**. **Q12–Q15 ALL RULED by Erik and CR-005 §3.1 APPROVED: planned work is M2.9, FR-87–FR-91, approved and dispatchable.** This closes the one ask of Erik's three that was a real gap — between "I wrote a plan" and "the fleet ran", the ledger is blind, and nothing the operator can click makes anything `pending`. **The Q13 ruling was checked against the artifacts before it was recorded and the check cut M2.9's scope:** plan and manifest share **zero** keys today (`plan.md` numbers tasks positionally; the manifest has no plan-id field) and both templates live outside this repo, so **M2.9 builds the read side of FR-90 only — it marks every collision and merges nothing**, which makes a false `done` impossible by construction. **Then §3.3 was approved too, later the same day, on Erik's rulings on Q17–Q19: FR-96 rejoins M2.9 with FR-96a/b/c, and §2's bar is ADOPTED AS WRITTEN (Q20).** M2.9's final scope is **FR-87–FR-91 plus FR-96 and its three sub-clauses**. The sharp one was Q17 — the FR-58 unparsed badge is mounted in `app-shell.tsx` on purpose, so a filter would have hung a ledger-wide count above a scoped list on every screen at once; it now stays ledger-wide **and states its scope**. **CR-005 has no open questions left — Q12 through Q20 all resolved.** **M2.9 was NOT dispatched, and the reason is measured:** `fleet-preflight.sh` **FAILS** from this session — launch cwd derives from `$CLAUDE_CODE_SESSION_ID`, no `cd` can move it, and this session launched in the Google Drive folder, so every worktree dispatch would die at creation. 7 other preflight checks ok. Dispatch requires `cd ~/Projects/project-tracker && claude`. Previously the same day — hand session. **B46 FIXED AND VERIFIED IN PRODUCTION** (PR #3, `75f070a`) — the command palette crashed the page on all three routes in, and the cause was not the suspected Radix wrapper but a missing cmdk `Command` root. Driven anonymously against the live site afterwards: 0 page errors, dialog open, routes listed, accessible name present. **SHIPPED. [PR #2](https://github.com/erik-capital-ready-advisors/project-tracker/pull/2) MERGED by Erik at 02:27:42Z (`aeb6d91`) — this project's first production deploy, and the first time this file can say the product is SERVING rather than merely deployed.** Verified from two independent vantages rather than one: `gh pr view` reports `MERGED`, and `git merge-base --is-ancestor` confirms the branch tip is on `origin/master`. Vercel raised a **Production deployment 4 seconds after the merge**, Ready in 21s, alias `project-tracker-mu-livid.vercel.app`. **Read back with `vercel curl`, never a plain `curl`** (deployment protection answers first with its own interstitial): `/`, `/blocked` and `/runs` all **HTTP 200** with real bodies — so M2.8's route is live. **The §7a boundary holds in production, checked with controls rather than asserted:** an anonymous `/blocked` renders *"Sign in to read this screen"*, and four strings known to be in the real ledger — including two work-item descriptions read from the authenticated render earlier the same session — return **zero** occurrences. The app identifies itself (`Delivery Ledger`, the `Blocked` heading) so the probe was not blind. **M2.7 AND M2.8 are both COMPLETE, and Phase 1 plus Phase 2's built milestones now carry no open gate.** **B31 and B39 are closed: `pnpm gate:m27:e2e` is 10 passed / 10** for the first time — B31 turned out to need only `M27_STORAGE_STATE` (its 10/10 "rendered the operator gate" signature is what an unset variable produces, not proof of a revoked session), and B39 was fixed properly rather than ratified, with Erik's explicit authorisation for **the first edit in this repo's history to change a gate file's pass condition**. M2.8: **M2.8 is COMPLETE**: `manual-gate.sh` PASS 30/30, both new screens observed for the first time, and FR-92/FR-93's three unsatisfiable clauses **ratified as-built by Erik and recorded as unmet**. Verifying one of those clauses found **B47** — `defect.source_key` was not run-scoped, so a second fleet run's QA report would have silently overwritten the first run's 13 defect rows. Reproduced with a failing test before it was escalated, fixed, and migration `20260824015328` applied and read back (13/13 rekeyed, 0 stale). `pnpm test` **1545 passed / 6 skipped**, typecheck 0, lint 0, `gate:m27` 5/5. Previously 2026-08-23 — by project-lead run `29b583` (see the run-29b583 block below). Previously 2026-08-22 — **PR #1 MERGED.** Erik merged it into `master` (`f629816`, verified via `gh pr view 1` — `state: MERGED`) after approving §5a. **`master` is no longer at `c835cf9`** — every line below still saying otherwise is describing state prior to this merge. Session stopped here for the day; no fleet dispatch happened yet against the four items §5a unblocked. Earlier the same day — **§5a APPROVED.** Erik ran `pnpm dev`, signed in, walked the six answer screens and several detail views, and approved the design **as-is, no changes requested.** This unblocked **B32** (money-formatter split), **B36** (`/questions/[id]`), **`docs/user-guide.md`**, and the **PR #1 merge decision**. Also raised in the same session: **B40**, no sign-out control exists anywhere in the signed-in app, filed and deliberately deferred. Nothing else changed — code state and test numbers are exactly as the 2026-08-20 entries below recorded them; deploy status now differs because `master` moved (see above).
2026-08-20 (night, second pass) — **`pnpm gate:m27:e2e` HAS RUN: 8 passed / 2 failed.** B31 is no longer "never run", and M2.7's navigation contract is proven against real rows in a real browser at `aal2` on `/blocked`, `/committed`, `/broken` plus all four cross-cutting tests. **The two failures are not defects** — `/next` and `/bottleneck` are empty because the answers are genuinely empty (no work item is `pending`; **zero** are `erik`/`erik_gate`), and neither can be populated through the product. That conflation is now **B39**. **Getting there took an unplanned detour: Erik could not sign in at all.** Diagnosed to `proxy.ts` forcing `httpOnly: true` on the Supabase cookie — `document.cookie` cannot overwrite an HttpOnly cookie and fails **silently**, so after the first token refresh every sign-in succeeded at GoTrue and persisted nothing. **The first explanation was coherent and wrong, and was killed by measurement rather than shipped.** Fixed as **B37**, which also records that **Baseline §1 is unmet, not waived**: client-side auth and an HttpOnly session cookie are mutually exclusive, and honouring §1 needs auth moved server-side in Phase 2. **B38**: the gate prints the live session cookie into failure output — leaked twice, both revoked. `e2e/m27-navigation.spec.ts` was edited **with Erik's explicit authorisation**, gathering only, **no assertion changed**. Earlier the same night — **The pointer's owed list was worked and five of Erik's six open items are answered.** Closed: **B4** (global with an allowlist — and the answer opened a build unit, because the allowlist did not exist in code), **B30** (export ships decrypted, confirmed), **B35** (FR-81 wins; **CR-004 drafted and approved**, no code and no schema), **B34** (fixed, though not as prescribed — `string | null | "none"` does not discriminate in TypeScript). **The M1.10 purge probe ran**: both refusal branches observed through the product's own `service_role` client with every count unchanged, so the refusal path is closed and **the destruction path is still NOT VERIFIED** — a refusal probe cannot observe a cascade, and the old pointer overstated this. `pnpm e2e` and `pnpm build` re-run rather than quoted: **255 passed / 7 skipped**, **41 routes**. 5 fleet learnings routed to the vault, 7 of 8 leaked worktrees pruned with branches retained, and `qa1`'s 5 uncollected questions read — **all five had already reached Erik as B31–B35, so the record broke but no decision went missing.** **§5a is still NOT APPROVED and Erik confirmed he has not yet looked at the screens**, so B32, `docs/user-guide.md`, `f4`/`f5` and the merge all stay held behind it. Numbers, all measured by that session: `pnpm test` **1359 passed / 6 skipped**, `pnpm gate:m27` **5/5**, typecheck 0, lint 0. **Nothing committed; nothing deployed.** Earlier the same day — **M2.7 is built: eight detail views, every reference navigable, and the acceptance gate that was written red before dispatch is now GREEN.** Fleet run `eb2490`, 8 dispatches of a 20 budget, 7 specialists in 3 waves plus an independent review. `pnpm gate:m27` **5 failed / 5 → 5 passed / 5**, measured at both ends by the orchestrator and reproduced independently by `qa-reviewer`. `pnpm test` 1061 → **1346 passed / 6 skipped / 0 failed**; build 33 → **41 routes**; e2e 207 → **255 passed / 7 skipped**. Neither gate file was edited — `6210318` is still the only commit that ever touched either, verified twice by different agents. **M2.7 is NOT Complete**, and for one reason: `pnpm gate:m27:e2e`, the ten crawl tests over populated screens, **could not run** — no agent holds an `aal2` session. Every loader, decrypt edge and resolution query on this branch is exercised against in-memory fakes only. **B31 — CLOSED 2026-08-24, see the pointer.** QA: **BLOCKED, 0 critical, 3 important, 9 minor**, all seven units graded SOUND and **no fabricated claim found in any report**. 2026-08-20 (late) — **M1.10 is built and Phase 1 is code-complete.** FR-60's export reads all 21 tables in one database function and was **observed returning a 412 KB document with counts that agree with this file**; FR-61's archive, restore and hard deletion are built against CR-002 §2, and the cascade chain that made deletion impossible — `engagement → test_case → test_result` — is gone, with the whole cascade graph now asserted by a test. **The purge has not been executed against real rows and is carried as NOT VERIFIED.** Earlier: **B27 closed: Broken has real data for the first time**, 13 defects with the critical reading `fixed`. Wiring the parser is what revealed that it read a report recording its own remediation as if nothing had been fixed. Earlier: the §5a state scale was measured against its own written claim and failed — four colour tokens changed, `unparsed` moved to a hatched treatment, and the claim is now a test.
**Earlier the same day:** This repo's own run `b0952e` was ingested through the product path by a human, `unparsed = 0` on work items, after a payload-validator defect was found and fixed that made a Playwright test impossible to represent
**Current phase:** Build, **Phase 1 milestones M1.1–M1.10 complete in code** on `agent-build/2026-08-19-b0952e`. M1.10 was built 2026-08-20 against CR-002 §2 — export, archive, restore and hard deletion, plus migration `20260820153021_m110_export_and_purge` **applied to the live project and verified by observation**. **§5a is now approved, so `docs/user-guide.md` is unblocked but still not written**; §7b is **not waived**, so M1.10 is complete in code and not complete as a milestone until it is. **[PR #1](https://github.com/erik-capital-ready-advisors/project-tracker/pull/1) was merged 2026-08-22 — `master` is at `f629816`, verified via `gh pr view 1`.** Still nothing deployed to production; Vercel's Production Branch is `master`, so the next push to it is the first production deploy this project will have
**Spec version:** `spec-approved.md` — promoted byte-for-byte from `spec-v1.md` on 2026-08-17 (`6820ade`), verified identical by `diff`
**Active CRs:** **CR-005 §3.1 APPROVED 2026-08-24 — planned work, FR-87–FR-91, as M2.9. Q12–Q15 ALL RULED, which was the stated gate.** Q12: a plan document is the **`writing-plans` output shape only** (`### Task N:` + `- [ ]` steps), which `plan.md` already is — a file of another shape is not partially parsed, it is not a plan. Q13: the reconciliation key is **an explicit id the plan carries and the manifest echoes; absent it, BOTH rows stand and the collision is marked**, prose similarity excluded absolutely. Q14: **an engagement is REQUIRED at creation — no unassigned planned row** (Erik overrode the drafting recommendation; `work-items/unassigned` holds *ingested* work whose owner the parser could not determine, which is the opposite case from work that has not happened yet). Q15: **STALE at 30 days untouched, surfaced, never deleted** — derived from a timestamp, never stored as a status, so CR-002's append-only ruling is untouched. **The Q13 ruling was verified against the artifacts before approval and the verification cut the milestone's scope:** no artifact carries such an id today — `plan.md` numbers tasks positionally (`### Task 5:`), `manifest-9a320b.md` has no plan-id field — so plan and manifest share **zero** keys, and both templates live in `~/.claude/agents/templates`, **outside this repo**, unreachable from a worktree-isolated run. **M2.9 therefore builds the read side of FR-90 only:** it marks every collision and merges nothing, which is exactly what Q13 requires and makes a false `done` **impossible by construction rather than by care**. Until id-emission lands as separate template work, FR-90 is honestly *"holds, merges nothing"* — **a stated limitation, not an unmet clause**. **Do not close that gap by falling back on titles** when the first real plan produces all-collisions-no-merges; that is the wrong `done` CR-005 was written to prevent. **§3.3 (FR-96) APPROVED 2026-08-24 later the same day, on Erik's rulings on Q17–Q19, and REJOINS M2.9** — it had been dropped hours earlier while unapproved, and it is back **by approval rather than by assumption**, which is the distinction a diff cannot show a month later. Q17: the FR-58 badge stays **ledger-wide and states its scope** under a filter (FR-96a) — this was the question that mattered, because `app-shell.tsx` mounts the count in the chrome deliberately, so a filter would otherwise have put a ledger-wide number above a scoped list **silently, on every screen at once**, which is M2.8's logged disagreement one layer up. Q18: the URL parameter on every list screen **plus one picker in the app shell**, not eleven per-screen widgets (FR-96b). Q19: an unresolvable slug renders an **explicit "no such engagement" with no rows and NEVER a silent fall-back to unfiltered** (FR-96c) — a screen that looks scoped while showing everything is the same class of lie as a wrong `done`. Decided rather than asked, and written into the CR so they are not silent: detail views take no filter; the picker lists active engagements only but an archived-not-purged slug still resolves so permalinks do not rot; **no cookie or session stickiness** — the URL is the entire state; served routes stay **30**, but the guide still needs a filter section and it is budgeted in. **§2's bar ADOPTED AS WRITTEN 2026-08-24 (Q20)** — guidance with force, **not** an enforceable gate; Erik declined that variant. Costs nothing retroactively: §3.1 clears it under limb (b), §3.3 under limb (a). **CR-005 now has NO open questions — Q12 through Q20 all resolved.** · **CR-005 §3.2 APPROVED 2026-08-23 — fleet run history, FR-92–FR-95, BUILT as M2.8, COMPLETE 2026-08-24.** `fleet_run` had carried verdict, dispatch cap and used, the test triple and a `gates` payload since ingest first landed, and **no route had ever rendered it**. Additive, no schema change, no new entity, so the 21-entity §7a PASS is unaffected; `/runs` and `/runs/[id]` took served routes **28 → 30**, which failed `manual-gate.sh` until `docs/user-guide.md` covered them — budgeted into the milestone rather than discovered at merge. **Three clauses ratified as-built and recorded UNMET**, see the Decisions log. · **CR-003 BUILT 2026-08-20 as M2.7 by fleet run `eb2490` — code complete, not Complete (B31).** **CR-004 APPROVED 2026-08-20 — `spec/change-requests/CR-004-engagement-reference-scope.md`: FR-81 wins.** FR-80 named "an engagement slug" as a navigable reference while FR-81's eight kinds contain no `engagement`, and gate test 1 asserts `ENTITY_KINDS` equals exactly those eight. Erik ratified the ruling `project-lead` and `qa-reviewer` had reached independently: FR-81's set is exhaustive and closed, an engagement is navigable through the pre-existing `/registry/[slug]` as a plain hyperlink, and FR-80 is a statement about navigability rather than about membership of that set. **No code, no schema, no entity (still 21), both gate files untouched** — M2.7 already shipped this reading and the CR ratifies it. The cost is written down rather than buried: an engagement reference carries no FR-83 dangling treatment, which is **unreachable rather than defended**, and CR-004 §3 names the four events that would end that and force a ninth kind. **B35 closed.** · **CR-003 APPROVED 2026-08-20** — `spec/change-requests/CR-003-detail-and-navigation.md`: detail views and cross-entity navigation, **FR-80–FR-86 plus FR-55**, as **M2.7 — Phase 2, built first, and PR #1 merges before any of it starts**. Drafted after the first populated look at the six screens, on Erik's point that a glance you cannot follow sends the reader back to the silo the product exists to end. **No new entity**, so the 21-entity `security-gate.sh` PASS is unaffected. Three questions answered at approval: **Q9** ships on the existing `service_role` read path with the debt raised as **B29** rather than built now; **Q10** puts it in Phase 2 so PR #1 stops growing at 51 commits; **Q11** rules FR-55's "links" a **hyperlink**, which makes **FR-55 PARTIALLY MET rather than met** and folds it into M2.7. **NOT gated on this CR, and now FIXED (`3ee4acb`):** Blocked, Next and Bottleneck rendered no description — a gap against FR-52/FR-53/FR-56 belonging to **PR #1**, not to M2.7. The CR's §2 justified that split on the claim that Broken already rendered a decrypted description; **that claim was false and is struck there**. No screen decrypted prose at all · **CR-002 APPROVED 2026-08-19** — `spec/change-requests/CR-002-deletion-vs-append-only.md`, resolves FR-61 against §7a's append-only rule: append-only wins absolutely, deletion stops at the audit boundary, and an unresolvable identifier in `audit_log`/`test_result` is the intended state rather than a defect. **The effective spec is now `spec-approved.md` as amended by CR-001 and CR-002.** No entity-count change, so the 21-entity `security-gate.sh` PASS still holds. M1.10 is unblocked and **not yet built**. · **CR-001 APPROVED 2026-08-18** — `spec/change-requests/CR-001-defects-regressions-releases.md`: defects, regressions, releases, a sixth answer (Broken), provisioning identifiers on `engagement`. FR-63–FR-79, three new entities (18 → 21). Q8 answered `contested` (FR-79). The effective spec is `spec-approved.md` as amended by CR-001
**Security posture:** declared — spec §7a as amended by CR-001 §4, **21 entities classified**. `security-gate.sh` **PASS on the resolved spec** (base + CR-001 + CR-002 + CR-003) 2026-08-20, 22 rows over 21 entities. Run `eb2490` added **no table and no migration**, so there is nothing to reconcile; it added **11 decrypt edges and 11 `service_role` read surfaces**, each named, and `qa-reviewer` rebuilt the census from the diff and confirmed it complete — **B29 was not deepened quietly**. `security-gate.sh` **PASS on the merged 21-entity set** 2026-08-18 (and on `spec-approved.md` 2026-08-17); `fleet-preflight.sh` PASS 2026-08-17.

---

## What this file is

This file is the **build log**. It tracks the live state of development: what's done, what's next, what's blocked, and what decisions were made during the build that aren't in the spec.

**Three documents, three jobs — don't confuse them:**

- **`spec/spec-approved.md`** — what was agreed to build. Source of truth. Changes only via a formal revise.
- **`spec/change-requests/CR-NNN-*.md`** — approved amendments to scope. Append-only.
- **`spec/prod.md`** (this file) — internal build state and decisions.

**Erik is both the client and the operator on this engagement.** That collapses the usual approval loop but it does not remove it: §7a is a stated baseline written by the intake session, not a decision Erik has made, and the four questions that settle it are Q1 through Q3 in Section 10. Treat them as client questions, because on this project they are.

---

## Next session pointer

> ### Read this first — resume note for 2026-08-25
>
> **Branch: `agent-build/2026-08-23-9a320b`.** Everything below was done 2026-08-24 by hand, in a
> session that started out only meaning to fix a gate. Tree is clean apart from
> `docs/Delivery-Ledger-User-Guide.docx`, which is **stale on purpose** — see the last section.
>
> #### M2.8 is COMPLETE. All three gates closed.
>
> 1. **`manual-gate.sh` PASSES, 30 of 30 routes covered, all observed.** `docs/user-guide.md` gained
>    "Fleet runs" and "Opening one fleet run"; `.fleet/manual-evidence-29b583.json` gained two rows
>    and an `amendments` note saying which branch and run observed them, since the file keeps its
>    29b583 name. Hand-edited rather than re-dispatching `docs-writer` — that cost ~157k tokens for
>    the full guide and this was two routes.
> 2. **Both screens have now been rendered and observed**, which the two units could not do from
>    isolated worktrees. **The saved `aal2` session was alive** — round-tripped, not decoded.
>    Screenshots in `.fleet/manual-traces/`.
> 3. **The three FR clauses this product does not satisfy were RATIFIED AS-BUILT by Erik** and are
>    written down as **unmet** in the Decisions log rather than allowed to pass as satisfied:
>    FR-93's "the defects it opened", FR-92's "dispatches used against cap", and `/runs/[run-id]`
>    addressing a run unambiguously. Read that entry before touching FR-92–FR-95.
>
> #### The thing worth carrying forward, if you read nothing else
>
> **Verifying the FR-93 question turned up B47, a live data-loss defect, and the way it got settled
> is the reusable part.** `defect.source_key` was built from the constant `qa-report`, so it was
> stable across re-posts of one run and **identical across different runs** — and `defect` upserts
> on `(engagement_id, source_key)`. A second run's QA report would have **overwritten the first
> run's 13 defect rows instead of adding its own**, silently, only on the second run.
>
> The first writeup handed Erik a structural argument plus a three-option menu. He asked what
> Karpathy would do. The answer was **write the failing test instead of the menu** — it came back
> RED with `qa-report#0`, `#1`, `#2` colliding, and there was no decision left to make. **Reproduce
> before escalating; escalate the measurement, not the claim.** That is now a lesson in `CLAUDE.md`.
>
> Fixed, and migration `20260824015328` applied and read back: **13 of 13 rekeyed, 0 stale, 13
> distinct keys over 13 distinct refs.** The migration **refuses with an exception** if more than
> one run exists, because there is no run→defect edge to read the owner off — that absence *is*
> FR-93's gap.
>
> **Two near-misses from this session, both worth internalising:**
>
> - **A control that reported the opposite of the truth.** Checking whether the `aal2` session was
>   alive by comparing `h1` said "SESSION DEAD" about a session that was plainly working: `h1` is
>   `"Blocked"` signed-in *and* signed-out, because the gate renders **inside** the `<Screen>`. Key
>   an auth round-trip on the sign-in notice, and abort if the signed-out case fails to show it.
> - **A test that passed by asserting nothing.** Run-scoping the key left
>   `persist.test.ts`'s `startsWith("qa-report#")` filter matching zero rows, so its comparison held
>   over two empty arrays and stayed green. It now carries
>   `expect(fresh.length).toBeGreaterThan(0)`. **When a change moves a string that a filter selects
>   on, the tests that survive are as suspect as the ones that fail.**
>
> #### Numbers, measured this session and not quoted
>
> `pnpm test` **1544 → 1545 passed / 6 skipped** · typecheck **0** · lint **0** · `gate:m27` **5/5**
> · `manual-gate.sh` **PASS 30/30**.
>
> #### B31 is CLOSED — the M2.7 e2e gate ran, 8 passed / 2 failed
>
> **`pnpm gate:m27:e2e` had never once run green, and that is what has kept M2.7 at "code complete"
> since 2026-08-20.** It ran 2026-08-24 against `localhost:3000` on the live `aal2` session:
> **8 passed / 2 failed (1.3m)** — identical to the 2026-08-20 result. **The 2 failures are the
> known B39 non-defects**, not regressions: `/next` and `/bottleneck` render their *empty states*
> because no work item is `pending` and none carries `erik`/`erik_gate`, and **neither screen can be
> populated through the product**. Every FR-80/81/82/83/84/85 crawl over a populated screen passed.
>
> **A correction that matters more than the result.** The first attempt returned **10 failed / 10,
> every one "rendered the operator gate (sign-in)"** — and that is *exactly* the signature
> `prod.md` recorded on 2026-08-23 and attributed to a revoked session. It was **`M27_STORAGE_STATE`
> being unset.** The config reads `storageState: process.env.M27_STORAGE_STATE`, so with it absent
> the gate runs signed-out and every test fails that way. **That signature does not establish a dead
> credential.** Both variables are required:
>
> ```
> M27_BASE_URL=http://localhost:3000 M27_STORAGE_STATE=.playwright-auth/operator.json pnpm gate:m27:e2e
> ```
>
> #### B39 is CLOSED too — `gate:m27:e2e` is **10 passed / 10**, and M2.7 is Complete
>
> Erik chose the real fix over ratifying 8/2, and **authorised the gate-file edit explicitly**. That
> authorisation mattered more than usual: the two earlier edits to
> `e2e/m27-navigation.spec.ts` were *gathering only, no assertion changed*, and **this one changes
> the pass condition** — the first edit in this repo's history to do so. The record is in the file
> at the call site, not only here.
>
> **An answer screen now has two correct shapes, not one:** `populated`, or `accounted-empty` —
> empty **and** the screen states what it set aside and why. That is not a lesser answer; FR-53
> exists because "nothing is startable" and "eleven things are held by a dependency" are different
> facts, and `/next` already renders its `set-aside` counts when empty precisely so they can be told
> apart.
>
> **The gate is STRICTER after this, not looser, which is the only reason it was allowed.** Before,
> an empty answer screen failed for the right reason by accident and no version of empty was ever
> inspected. Now `accounted-empty` has to earn the pass: `set-aside` counts present, every one
> labelled, every value a non-negative number. **An empty screen that accounts for nothing now fails
> an assertion that did not exist before.** A failed read cannot reach that branch — it renders the
> load notice and is caught earlier. The strict `requirePopulatedOperatorView` is unchanged and
> still used by FR-81, FR-82, FR-55 and FR-84, where empty **is** the failure.
>
> **Proved red-capable rather than assumed, and the first attempt did not prove it.** Mutation 1
> deleted `<SetAsideCounts>` from `/next` and went red — on the *pre-existing* `seen.length`
> assertion, because an invalid component broke the page instead of exercising the new branch.
> Mutation 2 renamed `data-verify-unit="set-aside"` so the page still rendered and only the
> accounting became unfindable; that fired the new throw by its own message —
> *"/next rendered its empty state and accounted for nothing"*. Both restored from byte snapshots
> in a `trap`, never `git checkout --`.
>
> #### What is open, in the order it costs least to close
>
> | Item | Who | Note |
> |---|---|---|
> | **`docs/Delivery-Ledger-User-Guide.docx` is stale** | **Erik, then agent** | Still 28 routes, and it holds Erik's uncommitted Word changes. Regenerating destroys them, so it was deliberately NOT regenerated. Close Word, save anything wanted, then rebuild from the updated markdown with `~/.claude/agents/regen-manual.sh`. The gate reads the `.md` and passes, so nothing is blocked on this |
> | **Click sign-out** | **Erik, 2 seconds** | Closes **B40**, the last claim no test can make. Destroys the session — do it last, or re-mint after |
> | ~~**B46 — command palette crashes the page**~~ | ~~Erik or fleet~~ | **FIXED AND VERIFIED IN PRODUCTION 2026-08-24** (PR #3, `75f070a`). `CommandDialog` passed `{children}` straight into `DialogContent` and **never rendered cmdk's `Command` root**, so `useStore()` returned `undefined` and `.subscribe` threw. **The standing suspicion — the Radix `Dialog` wrapper — was WRONG**; the wrapper is fine and the provider was missing. Building the loop before the hypothesis is what kept it off the wrong suspect: a 5.6s Playwright loop went red 3/3 on the user's exact symptom, then a 1.1s jsdom seam reproduced the identical `TypeError` with cmdk's own frames in the stack. Fixed the quieter half too — `DialogTitle` sat outside `DialogContent`, so Radix could not wire `aria-labelledby` and the dialog reached a screen reader unnamed. **Verified against the real production build, which `prod.md` previously carried as NOT done**: all three routes in (⌘K, Ctrl+K, the Jump to… button) driven anonymously against `project-tracker-mu-livid.vercel.app` — **0 page errors, dialog open, routes listed, accessible name present** on each. No credential was used, because the shell renders for signed-out visitors. `tests/command-palette.test.tsx` covers all three, proved red-capable by removing the `Command` root again |
> | **8 questions from `9a320b`** | **Erik** | `.fleet/questions-9a320b.jsonl`. **i1's four are now answered** — three by the as-built ratification, one by B47's fix. The remaining four (u1 ×2, u2 ×2) are still open |
> | **4 questions from `29b583`** | **Erik** | `.fleet/questions-29b583.jsonl`. Still open |
> | **B44** | fleet | Five pages read `OPERATOR_ROUTES` positionally. `tests/nav-routes.test.ts` pins the indices as a tripwire — delete that test when the refactor lands. `/runs` already reads its entry **by href**, which is the fix applied to one call site |
> | **B45** | **Erik** | Three of four specialists reported a prior-learnings search their traces show never ran. Doctrine, not product |
> | **§7a `/questions` select** | fleet | Adding `question, best_guess, answer` to the select leaves the suite green |
> | **A `fleet_run_id` column on `defect`** | **Erik** | Would actually deliver FR-93. **Materially smaller now** that `source_key` is run-scoped — but string-parsing a key is not a foreign key, so do not build FR-93 on the key. Wants its own CR |
> | **CR-005 §3.1 / §3.3** | **Erik** | Unapproved. Q12–Q15 open. **§3.1 must not start before Q13** — a guessed reconciliation key produces a wrong `done` |
> | **§2's proposed bar** | **Erik** | Approving §3.2 did not adopt it. Still needs its own yes |
> | **Worktrees: 3 remain, 1.9 GB** | agent | Deliberately not pruned. Before removing any, follow the standing rule — `git cherry` plus a file-set comparison, never a bare `git diff HEAD <branch>` — then `git worktree remove` and keep the branches |
> | ~~**PR / deploy**~~ | ~~Erik~~ | **DONE 2026-08-24. PR #2 merged (`aeb6d91`); production deploy Ready 4s later and VERIFIED SERVING** — `/`, `/blocked`, `/runs` all 200 via `vercel curl`, anonymous `/blocked` gated, no ledger prose leaking. Alias `project-tracker-mu-livid.vercel.app` |
>
> #### Live session state, which decays
>
> - **`pnpm dev` was left running** at `http://localhost:3000`. Probably not by morning.
> - **The `aal2` operator session was alive** at 21:12. It is perishable and **local inspection
>   cannot tell you whether it still works** — round-trip it, and key the check on the sign-in
>   notice rather than on `h1`. Re-mint with `pnpm dev` then `node scripts/save-operator-session.mjs`
>   — needs a human at an authenticator, so no agent can do it.
> - **An agent token with `ingest:write` and `answer:read` is in `.env.local`.**

> ### Read this first — resume note for 2026-08-24
>
> **One thing gates almost everything else, and it costs two commands:**
>
> ```
> pnpm dev
> node scripts/save-operator-session.mjs      # headed browser; you enter the TOTP code
> ```
>
> The saved `aal2` session is **revoked** (**B41**). Until it is re-minted, no agent can see any
> authenticated screen, `pnpm gate:m27:e2e` cannot run, `docs/user-guide.md` cannot be written, and
> **M1.10 and M2.7 both stay not-Complete**. The file `.playwright-auth/operator.json` still decodes
> as `aal2` with a `totp` claim, which is exactly the trap — local inspection cannot see a
> server-side revocation. Measured, not inferred: 10 failed / 10, all *rendered the operator gate*.
>
> **Then, in order:**
>
> 1. **Look at run `29b583`'s branch** — `agent-build/2026-08-23-29b583`, tip `fecb6fc`. Four
>    blockers fixed (B33, B36, B32, B40), 1400 tests green, both gate files untouched. **QA
>    BLOCKED, 0 critical, 6 important** — read `.fleet/qa-report-29b583.md`.
> 2. ~~**Add the two missing tests before merging (B43).**~~ **DONE 2026-08-23, `fecb6fc`.**
>    `tests/app-shell.test.tsx` and `tests/nav-routes.test.ts`. **1400 -> 1409 passed / 6 skipped.**
>    Proven red-capable rather than assumed: four mutations, each restored from a byte snapshot in a
>    `finally` -- deleting `<SignOutButton />` **FAIL**, moving it into the desktop-only rail
>    **FAIL**, breaking the `/questions` href **FAIL**, inserting `/questions` mid-array **FAIL**.
>    The shell test asserts the control is inside the `<header>` rather than merely present, because
>    the rail is `hidden md:block` and a control moved there renders on desktop while being
>    unreachable on mobile. **The third mutation QA found is still uncaught** -- adding `question,
>    best_guess, answer` to the `/questions` select -- so §7a ciphertext exposure there still rests
>    on a doc comment.
> 3. **Answer 4 queued questions** in `.fleet/questions-29b583.jsonl`. One of them (u2's second) is
>    **not a question but a latent defect** — six pages read `OPERATOR_ROUTES` positionally
>    (**B44**).
> 4. **Rule on B45** — three of four specialists reported a prior-learnings search their traces show
>    never ran. Doctrine, not product.
> 5. `docs/user-guide.md`, once the session exists. §7b is not waived.
>
> **Not for the fleet, unchanged:** B38, B39, the purge destruction path, and B37's auth follow-up
> (which wants a CR first). ~~**New:** B43's two tests are small enough to hand-write~~ -- **done,
> see item 2.** The cheapest remaining confidence on this branch is now B44's refactor of the five
> positional call sites; `tests/nav-routes.test.ts` pins those indices in the meantime, which turns
> a silent title swap into a failing test without fixing the pattern.


> ### Read this first — resume note for 2026-08-21
>
> **Launch Claude from inside the repo or the fleet cannot run at all:**
>
> ```
> cd ~/Projects/project-tracker && claude
> ```
>
> This is not a style preference. `fleet-preflight.sh` was **run both ways on 2026-08-20**:
> from the repo it reports **PREFLIGHT PASS — safe to dispatch**; from the Google Drive folder it
> reports **PREFLIGHT FAIL**, because worktree isolation keys off the session's launch cwd and
> nothing else, so every `isolation: "worktree"` dispatch dies with *"Cannot create agent worktree:
> not in a git repository"*. The 2026-08-20 session ran from the Drive folder and **could not have
> dispatched the fleet even if the work had been suitable.** All eight other preflight checks pass.
>
> **Stale as of 2026-08-22: `master` was untouched and PR #1 unmerged when this was written.**
> **PR #1 is now MERGED — `master` is at `f629816`.** Local working copy (`spec/prod.md` edits made
> this session) may still need committing; check `git status` before assuming the tree is clean.
>
> #### §5a — APPROVED 2026-08-22
>
> **Erik signed in, walked the six answer screens and several detail views, and approved the design
> as-is — no changes requested.** This was the one thing gating four others: the merge,
> `docs/user-guide.md`, **B32** and **B36** are all now dispatchable rather than held. Everything
> below still marked *"behind §5a"* is stale text from before the approval — treat those items as
> unblocked, not as still waiting.
>
> #### What the fleet can take tomorrow, and what it cannot
>
> | Item | Fleet? | Why |
> |---|---|---|
> | **§5a approval** | **Done** | **RESOLVED 2026-08-22 — approved as-is.** No longer on this board |
> | **B33** — prose renderer in five copies | **Yes — dispatch it** | Already scoped by `qa1` as one work-unit with **sole ownership of `src/components/`**, which is exactly the shape that avoids the three-way merge that blocked all five Wave C units. Best fleet candidate on the board |
> | **B36** — `/questions/[id]` unreachable | **Yes — dispatch it** | §5a is settled, so this is now an ordinary `ui` work-unit: decide *where an open question should be linked from* and wire it |
> | **B32** — money-formatter split | **Yes — dispatch it** | Small. Note **no live row exercises it**: the one contract milestone has a null amount |
> | **`docs/user-guide.md`** | **Yes — dispatch it** | `docs-writer` exists for exactly this. Was held so it wouldn't photograph a design about to change — no longer a concern now that §5a is approved. §7b is not waived, so M1.10 stays incomplete as a milestone until it is written |
> | **B40** — no sign-out control | **Yes, when picked up** | Filed 2026-08-22, deliberately deferred — not urgent, no live blocker. Ordinary `ui` work-unit whenever it's prioritized |
> | **B37 follow-up** — auth server-side | **Yes, but write a CR first** | It changes the authentication architecture to honour Baseline §1, so it is a Phase 2 milestone with a CR, not a ticket. **Security-critical**: §7a records that a prior build in this practice shipped MFA enforced only in the app layer and a review classified it critical. Mandatory `qa-reviewer` |
> | **B39** — gate conflates empty with broken | **No, then maybe** | The *decision* is Erik's. Its implementation edits a gate file, which needs explicit authorisation each time and is better done in a reviewed session than dispatched |
> | **B38** — gate leaks the session cookie | **No** | Touches gate and Playwright config. Small enough to do by hand, and the integrity rule makes agent edits there expensive |
> | **Purge destruction path** | **No** | Deliberately destroying a live engagement. Needs Erik's hands and his go-ahead, not an agent's |
> | **Merging PR #1** | **Done** | **MERGED 2026-08-22** — `master` is at `f629816`, verified via `gh pr view 1` (`state: MERGED`). No longer on this board |
> | ~~`rm -rf .claude/worktrees/agent-af9a7210a66a86493`~~ | **Done** | **DELETED 2026-08-23.** Orphan confirmed before removal — dangling gitdir, no branch, `git status` inside it fatals, and its tree differed from the tip everywhere (an old snapshot, not stranded work). **412 MB → 16 KB.** `git worktree prune` run after; `git worktree list` now shows the main checkout only |
>
> **So: four clean dispatches are available immediately (B33, B36, B32, `docs/user-guide.md`), B40
> whenever it's prioritized, and B37's follow-up wants a CR before anyone builds it.**
>
> #### Where M2.7 and M1.10 actually stand
>
> - **M2.7 — code complete, NOT Complete.** `pnpm gate:m27:e2e` **8 passed / 2 failed**. The two
>   failures are `/next` and `/bottleneck`, and **neither is a defect**: no work item is `pending`
>   and **zero** are `erik`/`erik_gate`, so those answers are genuinely empty and cannot be populated
>   through the product. Whether that should count as passing is **B39**.
> - **M1.10 — code complete, NOT Complete.** The purge's refusal path is observed; its **destruction
>   path is not**, and `docs/user-guide.md` is unwritten behind §5a.
>
> #### Numbers, all measured on 2026-08-20 rather than quoted
>
> `pnpm test` **1359 passed / 6 skipped** · `pnpm gate:m27` **5/5** · `pnpm gate:m27:e2e`
> **8 passed / 2 failed** · `pnpm e2e` **255 passed / 7 skipped** · typecheck **0** · lint **0** ·
> build **0**, 41 routes.
>
> #### One trap worth knowing before you touch the gate
>
> **B38: a failing `gate:m27:e2e` prints the live operator session cookie — access and refresh
> token — into its output.** It leaked twice on 2026-08-20; both sessions were revoked by hand.
> Assume any gate failure exposes a live session, and revoke afterwards. Use
> `node scripts/save-operator-session.mjs` to mint the storage state: it refuses to save anything
> below `aal2`, which the old `playwright codegen` ritual would happily do.


### Background — what the 2026-08-20 sessions established

> **This is not the resume point.** It was titled "Resume here (2026-08-21)" until 2026-08-20 night,
> which left two sections competing to be where the next session starts — on the very date it named.
> **The Next session pointer above is the one to read.** What follows is the evidence behind it: how
> M1.10 was verified, how the ledger came to hold real rows, and why §5a is still open.

**M1.10 is built, and Phase 1 has no unbuilt code left in it.** Migration
`20260820153021_m110_export_and_purge` is **applied to `onpvolboecjpdkvurjaf`** and was verified by
reading the database rather than by trusting the apply:

- **FR-60 export** — `app.export_document()` returns **one document covering all 21 tables**,
  observed at **421,637 bytes** with counts that agree with this file (79 requirements, 96 open
  questions, 20 work items, 13 defects, 1 fleet run). Encrypted prose came back **readable** and
  `agent_token.token_hash` came back **absent**, both checked in the returned document rather than
  inferred from the SQL. Reachable at `/settings/export` and `GET /api/export`.
- **FR-61 archive and restore** — `archived_at` is the single source of truth; `status` is left
  alone deliberately so two fields cannot disagree.
- **FR-61 hard deletion** — one `delete` against `public.engagement`, everything else by cascade,
  refusing an engagement that is not archived, writing its own audit rows inside the same
  transaction. **The `engagement → test_case → test_result` cascade is gone** (CR-002 §2.4), which
  is what made FR-61 buildable at all.
- **Privileges observed, not assumed:** `service_role` holds EXECUTE on all four new functions,
  `authenticated` and `anon` hold it on none, and `service_role` **can** delete `engagement` while
  it **cannot** delete `audit_log` or `test_result`. That is CR-002 §2.2 enforced at the grant.

**PARTLY VERIFIED as of 2026-08-20 (night). The refusal path was observed; the destruction path was
not, and that distinction is the whole of what is left here.**

Observed, through the product's own `service_role` client against the live project — not through an
MCP SQL console, which runs as `postgres` and would have proved the function works while proving
nothing about the grant the product actually depends on:

- `public.purge_engagement('no-such-slug-probe-20260820', …)` → `{"purged": false, "refusal": "no_such_engagement"}`
- `public.purge_engagement('delivery-ledger', …)` and `('unassigned', …)` → `{"purged": false, "refusal": "not_archived"}`, engagement id echoed
- Counts either side, unchanged: **engagement 2 → 2, audit_log 398 → 398, work_item 20 → 20, requirement 79 → 79**
- **A refusal writes no audit row**, which is correct — it returns before the insert

Preconditions were read from the database rather than taken from this file, and the probe script
aborts without calling the function if any engagement is archived, because an archived row is the one
input for which the destructive branch is live.

**Still NOT VERIFIED: the cascade, the audit rows a real purge writes, and CR-002 §2.4's retention of
`audit_log` and `test_result` through a deletion.** No engagement has been archived and destroyed, so
all three remain asserted. **An earlier version of this file said the probe "closes M1.10's one NOT
VERIFIED". That was too strong** — a refusal probe cannot observe a cascade. Closing it needs a
throwaway engagement created, archived and destroyed on purpose, which is a deliberate act on live
data and wants its own go-ahead.

**~~The export is decrypted, and that is a decision Erik should confirm — raised as B30.~~
CONFIRMED 2026-08-20 by Erik: the export ships decrypted. B30 closed.**

**The ledger is populated and the ingest path is proven end to end by a human.** Engagement
`delivery-ledger` exists, an `ingest:write` token was minted on the tokens screen, and this repo's
own run `b0952e` was posted to `/api/ingest/run` over HTTP: **HTTP 200, `unparsed = 0`** on work
items. Persisted: 79 requirements, 96 open questions, 24 blockers, 20 work items (15 done, 5
blocked), 322 test cases, 1 fleet run. **This is the first time any screen in this product has
rendered a real row.**

**§5a is still NOT APPROVED, and the review that was supposed to approve it found the design
failing its own written test instead.** B11's two falsifiable claims were measured rather than
eyeballed and both were false — see the Decisions log. Four tokens are fixed, `unparsed` is now
hatched, and `tests/state-scale.test.ts` enforces the claim. **What remains for approval is a
second look at the screens with those changes in place**, which no one has done: the fix is verified
by measurement and in the built CSS, and has not been seen by a human.

**Broken is populated and B27 is closed.** `defect` holds **13 rows: 1 critical `fixed`, 2 major
`fixed`, 3 major open, 7 minor open**, ingested through the product path and verified in the table.
The critical — the `service_role` grant that killed Mode-1 ingest — reads `fixed`, which is what
the report says and what `i10` actually did. **`test_result` is now the only one of the six answers
with a parser and no production data path.**

**M2.7's acceptance gate was written red before dispatch and the fleet turned it GREEN without
touching it** — `git log --all -- tests/m27-gate.test.ts e2e/m27-navigation.spec.ts` returns exactly
one commit, `6210318`, the one that wrote them. Re-run by the dispatching session rather than taken
from the report: **`pnpm gate:m27` 5/5 passed**, typecheck 0, lint 0, `pnpm test` 1346 passed / 6
skipped. **The other half has still never run.** The two commands are: `pnpm gate:m27` (5 structural, no credential) and
`pnpm gate:m27:e2e` (10 crawl tests over populated screens, needs `M27_BASE_URL` and a saved
operator session). Neither runs in `pnpm test` or `pnpm e2e`. **Whoever builds M2.7 — fleet or
hand — builds against these**, and the first step of the milestone is deleting the exclusion line in
`vitest.config.ts` so the structural half rejoins the suite.

**The order is now fixed by CR-003's approval.** PR #1 merges before any of M2.7 starts. The
sequence was **§5a approval → the three §2 description defects → M1.10 → merge → M2.7**, and two of
those four are done: the description defects (`3ee4acb`) and M1.10. **What is left before the merge
is §5a approval, the purge probe, and `docs/user-guide.md`** — and the guide is held behind §5a on
purpose, because a guide written now would screenshot a design that is about to be ruled on.

~~**Three defects that belong to PR #1, not to M2.7.**~~ **DONE 2026-08-20 (`3ee4acb`).** Blocked,
Next and Bottleneck now carry the work item's description, and Blocked carries the blocker's prose
as well, which is the actual answer to "what is holding it". **The premise recorded here was
wrong:** this said Broken already rendered a decrypted description and the path was proven. It did
not, and there was none — `defect.description` exists only in comments. No screen among the six
decrypted prose; `load.ts` decrypted only `contract_milestone.amount`. So this was the first
decrypt edge on those screens, permitted by §7a's "operator, agents, decrypted server-side" on both
`work_item` and `blocker`, read rather than assumed. Opt-in `withProse`, off by default, because
`decrypt_field` costs one RPC per value.

**Also still open:**

- **B28 — `/next` cannot distinguish "no work" from "all work done or blocked".** `READY_STATUSES`
  is `{pending, not_dispatched}`; all 20 items are `done` or `blocked`, so there were zero
  candidates and all three counters correctly report zero exclusions. But the empty state promises
  "the counts below say what was set aside and why", and 20 items were set aside for a reason no
  counter names. The endpoint is right; the accounting is incomplete. A fourth counter closes it.

Alongside those: the five open important QA findings and B24.

**Four things the ingest reported loudly. None is a stop, and none should be softened.**

1. **`test_result` is 0 rows**, so **Untested** reports zero mapped coverage across all 79
   requirements. Two independent causes, both already known: `test_result` has no HTTP write path
   (M1.8's carried gap), and no test title in this repo names a requirement. The report is the
   deliverable — see the Open threads note on test-title tagging.
2. **`fleet_run` test counts came back `null`** (passed/failed/skipped) while the gate
   `build_after_phase1: PASS` parsed. New and small. Fix by adding the shape the manifest uses.
3. **10 of 20 `prod.md` milestones classified `unparsed`.** The classifier reads `**Done**` but not
   `**Complete** — run b0952e`. They are **not persisted** regardless: there is no build-milestone
   entity and §7a classifies none, so ingest returned them in `notPersisted` and stored nothing.
4. **4 open questions carry `confidence = null`** — three say `medium` where the vocabulary is
   `med`, one is a prose sentence. Left null rather than guessed.

Items 2–4 are all *add the shape the artifact actually uses*. Widening a pattern to make them
classify is the failure §9 exists to prevent.

**Cosmetic, noticed and not changed:** `engagement.client_name` is `"delivery-ledger"` — the slug
typed into both fields. It is the display and sort key on every surface, so §5a's review will be
reading it in six places. `Capital Ready Advisors` is probably what it should say. Editable at
`/registry/delivery-ledger`; it does not affect ingest, which keys on the slug.

```
git log --oneline master..agent-build/2026-08-19-b0952e
```

The branch was pushed on 2026-08-19 at Erik's direction and is open as **PR #1** against `master`; `master` itself is untouched at `c835cf9` and nothing was merged. Nothing is deployed either: the sole live change this run made is one applied migration (the `service_role` grant below). The head is now `b654279` rather than `b980e1a` — one commit was added after the run to carry the run's own record (`.fleet/`), the approved CR-002, and the `prod.md`/`CLAUDE.md` writeback, none of which existed on the branch.

**Four decisions, in the order they unblock things:**

1. ~~**Provision one operator account.**~~ **DONE 2026-08-19.** Erik created the user in the Supabase dashboard (the only step no agent may take, FR-1) and enrolled TOTP himself. Verified by observation: factor `verified`, session carrying a `totp` AMR claim, and `app.is_operator()` **true** under those claims. **The manual and the session-cookie half of the security review are unblocked but not yet done** — both need a re-run now that a signed-in state exists.
2. ~~**Approve or reject CR-002.**~~ **DONE 2026-08-19 — approved.** M1.10 (FR-60 export, FR-61 deletion), the last Phase 1 milestone, is now dispatchable and is the natural next build.
3. ~~**Rule on the ingest wire tightening.**~~ **DONE 2026-08-19 — confirmed, the `400` stays.** See the Decisions log. **The root cause is still open and is not a blocker:** `/api/ingest/release` reads snake_case while session and waits read camelCase, and that mismatch is what lost the deploy date in the first place. Unifying the casing would remove the trap rather than report it; queued as `i10`'s first question, unanswered.
4. **Answer what you want to answer of 82 open questions** in `.fleet/questions-b0952e.jsonl`. None blocks the build; every one was proceeded past on a recorded best guess.

**The QA verdict is ISSUES, not PASS** — the one critical was found, fixed and re-verified within the run; important-level findings remain open and are listed as blockers below.

---

---

## Current state (one paragraph)

**2026-08-23, run `29b583` — four blockers fixed on a branch nobody has looked at, and the run's
most important output is a measurement rather than a diff.** `agent-build/2026-08-23-29b583` (tip
`44db6ac`, cut from `8b311c4`, never near `master`) carries **B33** (the prose renderer hoisted from
five copies to one component with one verification contract, adopted across eight call sites),
**B36** (a `/questions` list route, so `/questions/[id]` is no longer unreachable), **B32** (one money
formatter; `money-display.ts` deleted) and **B40** (a sign-out control, mounted in the app-shell
header so it reaches every breakpoint). Measured at the tip by the orchestrator and reproduced
independently by `qa-reviewer`: typecheck **0**, lint **0**, `pnpm test` **1400 passed / 6 skipped**
(from 1359), `pnpm gate:m27` **5/5**, build exit 0 with `/questions` in the route tree. **Neither
gate file was touched** — 0 commits in the run range against either. **QA returned BLOCKED: 0
critical, 6 important.** The reason is not a defect in the code, which QA found correct on all four
counts; it is that **§7c declared both agent-access boundaries `reachable` and Phase 0 measured both
unreachable** — the saved `aal2` session is revoked (`gate:m27:e2e` **10 failed / 10**, every one
*rendered the operator gate*, against 8 passed / 2 failed on 2026-08-20) and there is no agent token
— **and QA then measured that the unit suite does not compensate.** Four mutations in a throwaway
worktree, on a runner proven red-capable: deleting B40's mount, breaking B36's nav href, and adding
§7a ciphertext columns to the `/questions` query each leave all **1400 tests green**. Only B33's
`unreadable`→`absent` collapse was caught. **So two of the four fixes are protected by neither a test
nor an observation (B43)**, and B40's entire behavioural surface was written by an abandoned agent,
inherited by one that never wrote it, at a moment when nobody could see it. `docs/user-guide.md` was
**not dispatched** — §7c's own stated consequence — so **M1.10 stays not-Complete** and `manual-gate.sh`
still reads `FAIL MISSING`. Nothing is merged to `master` and nothing is deployed.


**M2.7 is built and its gate is green, and the milestone is still not Complete.** Fleet run `eb2490` merged 7 specialist work-units plus an independent review onto `agent-build/2026-08-20-eb2490` (tip `5a8cdde`) using **8 of a 20-dispatch budget**, cut from `agent-build/2026-08-19-b0952e` at `6210318` and never near `master`. At the tip: `pnpm typecheck` 0, `pnpm lint` 0, `pnpm build` 0 with **41 routes** (was 33), `pnpm test` **1346 passed / 6 skipped / 0 failed** (was 1061), `pnpm e2e` **255 passed / 7 skipped** (was 207), and **`pnpm gate:m27` 5 passed / 5, from a measured baseline of 5 failed / 5**. The gate was written before the milestone was dispatched and **neither gate file was edited** — `6210318` remains the only commit that ever touched either, checked independently by the orchestrator and by the reviewer. Eight detail views exist, every reference on all eleven screens is navigable, a reference that resolves to nothing renders in FR-12's dangling treatment and is never a link, and FR-82's four relationships appear together on one page even when all four are empty. **What is missing is the other half of the acceptance criterion.** `pnpm gate:m27:e2e` — ten crawl tests over populated screens — **has never run**, because it needs an `aal2` operator session no agent can produce, so every loader, decrypt edge and resolution query in this milestone is exercised against in-memory fakes and nothing here has met a real row. That is **B31**, it is the highest-value open item on the blocker table, and it is a two-command job. QA returned **BLOCKED with 0 critical, 3 important and 9 minor**; all seven units graded SOUND and **no fabricated claim was found in any of the seven reports**. Of the eight findings the units themselves raised, the reviewer **refuted one and judged two overstated**, and found the money-formatter split **worse** than reported — `/committed` and `/milestones/[id]` now render the same amount two different ways, one click apart, and this run created that adjacency.

**The build is done through M1.9 and reviewed.** Run `b0952e` merged 16 specialist work-units onto `agent-build/2026-08-19-b0952e` (tip `b980e1a`) using **18 of a 20-dispatch budget**. At the tip: `pnpm typecheck` 0, `pnpm lint` 0 (oxlint), `pnpm build` 0 with 31 routes, `pnpm test` **966 passed / 6 skipped**, `pnpm e2e` **199 passed / 7 skipped**, all re-run by `project-lead` rather than taken on report. **M1.1 through M1.9 are Complete**; M1.10 is Blocked on CR-002. The independent QA pass returned **ISSUES**: one critical, since fixed and re-verified, plus five important and six minor still open. **The critical is the run's most important artefact and it very nearly shipped invisibly** — `service_role` had no `EXECUTE` on a function sitting inside a CHECK constraint on `fleet_run`, so `POST /api/ingest/run` returned `500` on every request and Mode-1 ingest, the reason this product exists, stored nothing. The full suite was green the entire time it was dead. What made it survive review is a method failure worth more than the bug: idempotency had been "proved" through the Supabase SQL console as `postgres`, a role that can execute the function, while the product runs as `service_role`. **Two things are carried honestly rather than closed:** there is no operator account anywhere, so nothing behind authentication has ever rendered a real row, and `docs/user-guide.md` was not written because §7b's reader cannot be reached without one.

**As of 2026-08-20 the ledger holds real data and Mode-1 ingest has been exercised by a human through the product path.** Engagement `delivery-ledger`, an `ingest:write` token minted on the tokens screen, and run `b0952e`'s own artifacts — 1 manifest, 17 per-unit question files, 34 test files, 514 KB — posted to `/api/ingest/run`: **HTTP 200, `unparsed = 0`** on work items. 79 requirements, 96 open questions, 24 blockers, 20 work items, 322 test cases, 1 fleet run. Getting there required fixing a payload-validator defect that made a Playwright test unrepresentable (Decisions log, 2026-08-20); the observed proof it is fixed is 57 Playwright cases across 9 files in `test_case`, where before the fix all 322 would have been `vitest`. **`test_result` is still 0 rows** — that table has no HTTP write path, so FR-47–FR-51 compute correctly over nothing, and Untested honestly reports zero mapped coverage across 79 requirements.

The paragraph below is the pre-build state, kept for the record. Spec drafted, gated, and amended by approved CR-001. The effective spec — `spec-approved.md` plus CR-001 — carries **79 functional requirements** across 11 Phase 1 milestones, **21 data entities all classified** (§7a plus CR-001 §4), 7 spec questions of which Q8 is answered (`contested`), and **six answers** including Broken. `security-gate.sh` passes on the merged entity set and `fleet-preflight.sh` passed at intake. Nothing has been dispatched and the fleet has not run.

**As of 2026-08-17 the database exists and has been inventoried.** Supabase project `onpvolboecjpdkvurjaf` (`project-tracker`), org `whneklkrjsulgqzqxsks` — Erik's own and the only org his connector can see — us-east-1, Postgres 17.6.1.155, `ACTIVE_HEALTHY`, created 2026-08-17T21:06Z. **Zero `public` tables, zero migrations, zero users.** Two live findings on it: public signup is on (B8), and an event-trigger function `public.rls_auto_enable()` that this build did not write already forces RLS on every new `public` table. **The Vercel project exists and is deployed**, verified by HTTP after the MCP connector reported it absent — see the correction in the Decisions log. The connector still cannot see it, which is the residual B1b.

**Connection facts, recorded because they are identifiers rather than secrets.** Project ref `onpvolboecjpdkvurjaf`, API URL `https://onpvolboecjpdkvurjaf.supabase.co`, Vercel team `team_J6J1LAU19znwJenYFKgVArEV`. **The operator account** (B20, created by Erik in the dashboard 2026-08-19 — the one step no agent may take under FR-1): `erik@capitalreadyadvisors.com`, operator id `08c46e9c-14c8-4f29-ba01-862b1652c7f6`, `role = operator`, TOTP factor **verified**, session observed at **aal2**, `app.is_operator()` **true** measured under those claims. **No service-role key has been read or stored by any session, and none should be** — it goes in the environment and nowhere else.

---

## Infrastructure wiring, as of 2026-08-17

Erik's scope for this pass was explicit: **provision and wire the three services, do not start the build.**

| Piece | State |
|---|---|
| GitHub repo | **Done.** `erik-capital-ready-advisors/project-tracker`, private, `master` pushed |
| Vercel project | **Done.** Connected to the GitHub repo and auto-deploying on push - 6 production deployments, all `Ready`, the newest triggered by a push during this session. Production URL `https://project-tracker-mu-livid.vercel.app` (the bare `project-tracker.vercel.app` belongs to someone else) |
| Vercel <-> local CLI | **Done.** `vercel link` run; `.vercel/` created and gitignored |
| Vercel env vars | **Done.** `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` set on **Production, Preview and Development**. Preview needed a CLI upgrade: 52.0.0 loops on `git_branch_required` and its `next[]` hint suggests the command that produced the loop. Upgraded to 59.1.4 with `npm i -g vercel@latest` and it worked first try |
| `SUPABASE_SERVICE_ROLE_KEY` | **Set by Erik and hardened by Erik**, 2026-08-17. `Sensitive` (write-only) on **Production**. No agent session has ever read it. **Open question:** the re-add dropped the Preview copy, so preview deployments now have no service-role key. The fleet builds on a branch and therefore deploys to Preview, so any server-side database path will fail there until it is added back - or that is accepted deliberately, on the argument that preview URLs are more exposed and should not carry a key that bypasses RLS |
| Supabase project | **Done and inventoried.** `onpvolboecjpdkvurjaf`, empty, correct org |
| Supabase signup | **Done.** Disabled by Erik and verified with the three-part instrument - settings flag, error code, and `auth.users` row count |
| Vercel MCP connector | **Open - B1b.** Sees 1 project; the CLI sees 15. Scope-limited token |
| Application code | **None, by design.** The repo holds spec, plan and docs only, so Vercel is currently deploying markdown. A framework scaffold is the first build step and was not taken |

**The two vantage points disagreeing is the lesson worth keeping from this pass.** The MCP connector said the Vercel project did not exist; the CLI listed it among fifteen. Neither tool was broken - they were authenticated with different scopes, and only one of them was asked.

---

## Milestone tracker

### Phase 1 — MVP (estimated 8-10 days)

| Milestone | Status | Notes |
|---|---|---|
| M1.0 Provisioning | **Done** | Supabase `onpvolboecjpdkvurjaf` in Erik's own org, inventoried empty. Vercel project live and linked, env vars set on all three environments. `disable_signup` **verified true by observation**, not assumed. Residual: the Vercel MCP connector's scope (B1b) and whether Preview carries a service-role key |
| M1.1 Foundation | **Done** — run b0952e | Schema for **21 entities (CR-001: + defect, release, release_requirement, and the provisioning-identifier columns on engagement)**, RLS everywhere, pgcrypto per §7a as amended with the key in Vault, append-only audit log. **Built and verified 2026-08-19 — run b0952e.** 21 tables, 23 RLS policies, 12 pgcrypto columns, 18 functions with `search_path=''` and no PUBLIC/anon EXECUTE, audit log refusing UPDATE/DELETE/**TRUNCATE**. The `rls_auto_enable` warning was confirmed by measurement: a throwaway table came out `relrowsecurity = true` with **zero policies**, so every check asserts `pg_policies`. **Two NOT VERIFIED carried forward: security headers (unbuilt, a real gap) and FR-61's deletion path** |
| M1.2 Access | **Complete** — run b0952e | Units `i4` + `u4`. MFA enforced **in RLS**, verified at the database with an `aal2` positive control; tokens bcrypt-hashed (secret segment only, per the measured 72-byte truncation); FR-8 limiter **tripped** at 160 requests in 42s → `{200:60, 429:100}`. **FR-5's refusal of `contract_milestone` to agent tokens is application-layer, NOT database-enforced** — route handlers read with the service-role key, which holds BYPASSRLS. Erik accepted this for v1 and it must keep being reported as such |
| M1.3 Registry | **Complete** — run b0952e | Units `i5` + `u2`. Four registry screens on real server actions. FR-12's dangling reference is marked **on every read**, not toasted once at save, and deliberately not in fuchsia — it parsed perfectly and points at nothing, which is the opposite of `unparsed`. FR-78 refuses secret-shaped identifiers with a real `23514` and a positive control |
| M1.4 Mode 1 ingest | **Complete** — run b0952e | Manifest tables, questions normalization, prod.md, requirement ranges, idempotency, the unparsed discipline. FR-14 to FR-23, **plus the QA-report findings parser (CR-001 FR-64)**. **Complete: pure parsers (`i2`), CR-001 rules (`i3`), persistence and the endpoint (`i5`).** FR-22 idempotency observed end to end over HTTP with a different-run-id negative control. **This milestone was dead on arrival and nobody noticed** — see critical C1 in the Decisions log; fixed by `i10` and re-verified by `qa-reviewer` through the product path. **2026-08-20: exercised for real** - this repo's own run ingested by a human over HTTP, `unparsed = 0` on 20 work items, 79 requirements and 96 questions persisted. One defect found in the process (`testFiles[].path`, Decisions log) |
| M1.5 Mode 2 capture | **Complete** — run b0952e | Unit `i6`, plus `u4`'s attribution queue. Session-capture endpoint, `scripts/claude-session-capture.sh`, reason classes, dispositions. A session that says nothing about status gets `unparsed`, never `done`. **Degradation to record: with an agent token, directory-based engagement resolution does not work at all** — §7a forbids agents reading `engagement.repo_path`. Explicit-slug resolution is unaffected. B4 (install scope) remains open and doc1 wrote it as an open decision rather than resolving it |
| M1.6 Mode 3 waits | **Complete** — run b0952e | Declaration, expected-by, overdue, resolution, date contribution. FR-32 to FR-38. **Complete** — `i6` persistence, `u4` declaration and resolution screens. `i10` closed a late finding here: `/api/waits` silently swallowed unknown body keys, so `expected_by` produced a `201` and a wait with **no expected-by date** — and FR-34 computes the overdue flag from exactly that field, i.e. a wait that could never go overdue |
| M1.7 Unified model | **Complete** — run b0952e | Execution modes, executor kinds, Erik-gates, dependency edges, evidence scopes. FR-39 to FR-44. **Complete** — `i6` persistence, `u4`'s FR-44 unified list: one list, execution mode as a column and a filter, never three lists |
| M1.8 QA traceability | **Complete** — run b0952e | Test-title parsing, the independent-certifier rule, billable derivation. FR-45 to FR-51, **plus CR-001's pure rules: `D-nn` tags, defect verification (FR-66), regressions (FR-69), shipped state (FR-74), contested (FR-79)** — CR-001's half is unit `i3`, not yet dispatched. **Complete** — `i3` built CR-001's half, `i7` the persistence. **Carried gap: `test_result` has no HTTP write path**, so FR-47–FR-51 compute correctly with no production data path into them |
| M1.9 The six answers | **Complete** — run b0952e | Units `i7` + `u3`. Six endpoints and six screens. **`i7` owns the single definition of the FR-58 `unparsed` population** — `work_item` + `defect` + `test_result`, global and never narrowed by a caller filter, and `null` rather than a partial sum on any component failure. QA seeded 4 unclassified records and observed `unparsed=4` on **every** endpoint, then `=2` on a re-seed. `/api/answer/committed` answers agent tokens `403 forbidden_table`, because §7a refuses them `contract_milestone` and every field of Committed derives from it |
| M1.10 Export and handover | **Code complete** — built 2026-08-20; **not Complete as a milestone** | **CR-002 is approved, so the disputed reading is settled and this is now dispatchable.** Build FR-61 against CR-002 §2: deletion clears the engagement's own state rows, never reaches `audit_log` or `test_result`, writes its own `audit_log` row, and leaves dangling identifiers in those two tables as the intended state. FR-60 (export) was only ever blocked by milestone membership. **Built 2026-08-20.** FR-60: `app.export_document()` returns one document over all 21 tables — observed at 421 KB with counts matching this file, prose decrypted, `token_hash` absent — behind `/settings/export` and `GET /api/export`, which is the only route in the product that authenticates a **session cookie** rather than a bearer token, because FR-5 defines no capability that could authorise it. FR-61: archive and restore on `archived_at`, and a purge that issues one `delete` against `public.engagement` and lets the cascade do the rest, refusing an unarchived engagement and auditing itself in the same transaction. Migration `20260820153021` applied live and privileges read back. **Two things were carried honestly, and one is now half-closed. PURGE PROBE RUN 2026-08-20, authorised by Erik — the refusal path is observed, the destruction path is still not.** Executed through the product's own `service_role` client against the live project, not through an MCP console, so it exercises the real grant end to end rather than `has_function_privilege`'s reading of it. Preconditions were read independently rather than taken from this file: both engagements `archived_at IS NULL`, and the probe script aborts without calling the function if any engagement is archived. Observed: `public.purge_engagement('no-such-slug-probe-20260820', …)` → `{"purged":false,"refusal":"no_such_engagement"}`; `('delivery-ledger', …)` and `('unassigned', …)` → `{"purged":false,"refusal":"not_archived"}` with the engagement id echoed. Counts either side, unchanged: engagement **2 → 2**, audit_log **398 → 398**, work_item **20 → 20**, requirement **79 → 79** — so a refusal writes **no audit row**, which is correct, since it returns before the insert. **What is still NOT VERIFIED, and the pointer overstated this:** no engagement has been archived and destroyed, so the **cascade**, the **audit rows a real purge writes**, and the CR-002 §2.4 retention of `audit_log`/`test_result` remain asserted rather than observed. Closing that needs a throwaway engagement to archive and destroy, not another refusal probe. Second item unchanged: **`docs/user-guide.md` is still not written.** §5a was approved 2026-08-22, so it is no longer held behind that — but run `29b583` could **not** write it either, and for a new reason: §7c's operator boundary was measured **unreachable** (revoked session), and §7c's own stated consequence is that `docs/user-guide.md` cannot be written. `manual-gate.sh` re-run 2026-08-23: `FAIL MISSING: no docs/user-guide.md and no written waiver`. §7b is **not waived**, which is why this row still does not read Complete |

### Phase 2 — Automation and feedback loops (estimated 4-6 days)

| Milestone | Status | Notes |
|---|---|---|
| M2.1 Probes | Not Started | Automated resolution for external waits. Needs per-vendor credentials, which is a blocker class of its own |
| M2.2 Coverage register | Not Started | The trigger rule live. Consumes M1.5's stack attribution |
| M2.3 Estimate vs actual | Not Started | The bid table by work type and stack |
| M2.4 Critical path | Not Started | Weighted by Erik's serial time, not by agent time |
| M2.5 Harness evidence | Not Started | Detox, manual device scripts, store review outcomes |
| M2.6 Writeback | Not Started | `project-lead` emits the machine-readable block itself, turning every Mode 1 parser into a validator. Retires the whole `unparsed` risk class |
| **M2.7** Detail and navigation | **COMPLETE 2026-08-24** — built 2026-08-20 by fleet run `eb2490`; **B31 and B39 both closed 2026-08-24, `pnpm gate:m27:e2e` 10 passed / 10** | Eight detail views (`/work-items/[id]`, `/defects/[id]`, `/blockers/[id]`, `/requirements/[id]`, `/questions/[id]`, `/waits/[id]`, `/releases/[id]`, `/milestones/[id]`), `<EntityRef>` adopted across all eleven existing screens, FR-12's dangling treatment preserved on a navigable surface, FR-55's hyperlink, FR-82's four relationships shown together. CR-003 FR-80–FR-86 + FR-55. **The acceptance gate written red before dispatch is now green: `pnpm gate:m27` 5 failed / 5 → 5 passed / 5**, and neither gate file was edited — `6210318` is still the only commit that ever touched either, verified independently by `project-lead` and by `qa-reviewer`. 7 units in 3 waves: `u1` the seam (`src/lib/entity-routes.ts`, `<EntityRef>`, `<EntityDetail>`, and the deletion of the gate's exclusion from `vitest.config.ts`), `i1` the read layer (reference resolution + eight loaders + FR-82, **reusing** `indexCoverage`/`latestResults`/`shippedIndex` rather than re-deriving them), `f1`/`f2`/`f3` the routes, `f4`/`f5` the adoption. **Why this row does not read Complete:** `pnpm gate:m27:e2e` — the ten crawl tests over populated screens, which is the half that would catch a `b0952e`-class defect — **has never run.** No agent holds an `aal2` session, so every loader, decrypt edge and resolution query here is exercised against in-memory fakes. **That is B31 and it is a two-command job for Erik.** QA **BLOCKED, 0 critical, 3 important, 9 minor**; all seven units graded SOUND with **no fabricated claim found in any report**. **2026-08-23, run `29b583`:** three of M2.7's follow-up defects are now fixed on `agent-build/2026-08-23-29b583` — **B33** (prose renderer hoisted to one component, one contract, eight call sites), **B36** (`/questions` list route, so `/questions/[id]` is no longer unreachable) and **B32** (one money formatter; `money-display.ts` deleted). **B31 is NOT closed and this row still does not read Complete:** `pnpm gate:m27:e2e` could not run at all this run — the saved operator session is revoked, so it returned **10 failed / 10**, every one *rendered the operator gate (sign-in)*, against 8 passed / 2 failed on 2026-08-20 |

| **M2.8** Fleet run history | **COMPLETE 2026-08-24** — built by run `9a320b` (stopped mid-flight; both units rescued from uncommitted worktrees), finished by hand | CR-005 §3.2, FR-92–FR-95. `/runs` (`a188823`) and `/runs/[run-id]` (`dc21837`). **`fleet_run` had carried verdict, dispatch cap and used, the test triple and a `gates` payload since ingest first landed, and no route had ever rendered it.** Served page routes **28 → 30**. **All three gates closed 2026-08-24:** (1) `manual-gate.sh` **PASS 30/30** — `docs/user-guide.md` gained "Fleet runs" and "Opening one fleet run", and `.fleet/manual-evidence-29b583.json` gained two rows carrying an `amendments` note that says which branch and run observed them, because the file keeps its 29b583 name; **hand-edited rather than re-dispatching `docs-writer`**, which cost ~157k tokens for the full guide. (2) **Both screens are now OBSERVED**, which the units could not do — they reported `NOT VERIFIED — cannot render an authenticated screen from an isolated worktree` and **neither copied a credential to work around it**, which was right. The saved `aal2` session was round-tripped and found alive; **the first discriminator was wrong and said the opposite** — `h1` is `"Blocked"` signed-in *and* signed-out because the gate renders *inside* the `<Screen>`, so it was re-keyed on the sign-in notice with an abort if the signed-out case fails to show it. Observed: `/runs` 1 run `b0952e`, nine columns, the dispatch-producer note, verdict `unparsed` / *one source, uncorroborated*; `/runs/b0952e` all six blocks, 20 work units (`i9p2` = `unassigned` / **NOT DISPATCHED**), 96 questions, 74 requirements, the defects gap, and `/runs/zzzzzz` → **Not found**. Screenshots in `.fleet/manual-traces/`. Recorded honestly: the ninth column **clips at 1280px** and the table scrolls sideways, and the **ambiguous-match screen is marked UNVERIFIED in the guide body** because one run cannot collide. (3) **The three FR clauses the product does not satisfy were RATIFIED AS-BUILT by Erik 2026-08-24 — see the Decisions log. They are unmet, not met, and they are listed there rather than allowed to pass quietly.** |
| **M2.9** Planned work | **APPROVED 2026-08-24, NOT STARTED — dispatchable.** Blocked only on being dispatched from a session launched inside the repo | CR-005 §3.1, **FR-87–FR-91**. Closes the one ask of the three that was a genuine gap: **between "I wrote a plan" and "the fleet ran", the ledger is blind**, and this is the layer beneath B39 — `/next` and `/bottleneck` are empty because **nothing is `pending` and nothing the operator can click will make anything `pending`**. `work_status` already has `pending` and `not_dispatched`, so what is missing is not the concept but **any path by which a `pending` row is born**. Unblocked by Erik's rulings on **Q12–Q15** (see Active CRs and the Decisions log). **Final scope: FR-87–FR-91 PLUS FR-96, FR-96a, FR-96b, FR-96c** — §3.3 was approved later the same day on Erik's rulings on Q17–Q19, so the engagement filter ships in this milestone after all. **FR-96 is the cheap half:** eleven of the affected screens already read `searchParams`, a query parameter adds no route so served routes stay **30** and `manual-gate.sh`'s count is unaffected. **Its one real hazard is the FR-58 badge** — `app-shell.tsx` mounts the unparsed count in the chrome deliberately, so a filter would otherwise put a **ledger-wide number above a scoped list, silently, on every screen at once**, which is M2.8's logged disagreement (global badge 0, run `b0952e` 1) one layer up. FR-96a settles it: ledger-wide, **and it says so**. Expected cost per CR-005 §4: **no new entity** (planned work is a `work_item`), so the 21-entity §7a PASS holds and no new §7a row is needed; **one migration at most** — nullable columns on `work_item` for the planned marker and plan provenance, nothing dropped, no cascade change. **A hand-entry form plus a parser route will move the served-route count again, so budget the `docs/user-guide.md` update into the milestone** — that is the lesson M2.8 paid ~157k tokens to learn the expensive way. **The plan parser is the named risk**, and `unparsed` being non-zero on the first real plan is the parser working, not failing. |

### Phase 3 — Deferred at intake

| Milestone | Status | Notes |
|---|---|---|
| M3.1 Linear or Notion mirror | Deferred | Read-only, client-facing status |
| M3.2 Native mobile | Deferred | The web app is responsive; this is a want, not a need |
| M3.3 Automated invoicing | Deferred | Money stays in Upwork and Stripe permanently |

---

## Active blockers

| ID | Blocker | Owner | Blocks | Resolution path |
|---|---|---|---|---|
| ~~**B1a**~~ | ~~Which Supabase organization~~ | ~~Erik~~ | ~~everything~~ | **RESOLVED 2026-08-17, verified not assumed.** Project `onpvolboecjpdkvurjaf` (`project-tracker`, us-east-1, Postgres 17.6.1.155, created 2026-08-17T21:06Z) in org `whneklkrjsulgqzqxsks`, "erik-capital-ready-advisors's Org". See the Decisions log for the evidence |
| **B1b** | **The Vercel MCP connector is project-scoped to `danishjawaid`.** Sharpened 2026-08-19: `list_projects` on the correct team `team_J6J1LAU19znwJenYFKgVArEV` returns **exactly one** project, `danishjawaid`, and `get_project` 404s even when given the canonical `prj_bekdkkVtS2DhP9t67QSyuBxp6kd7` read out of `.vercel/project.json` — so it is a **scope grant**, not a stale or expired token | **Erik** | **Nothing. Corrected 2026-08-19 — the earlier claim that this blocks the fleet's `devops` unit was wrong.** `~/.claude/agents/devops.md` contains **zero** `mcp__` references and drives Vercel entirely through the `vercel` CLI, which is authenticated as `erik-capital-ready-advisors` and lists **17** projects including `project-tracker`. B1b limits an interactive session's MCP vantage only | Reauthorize via `/mcp` and choose **All Projects** at Vercel's consent screen — re-running the same grant scoped to specific projects changes nothing, which is the trap here. Alternative: authorize the official `plugin:vercel:vercel` server at `mcp.vercel.com`. Verify by re-running `list_projects` and expecting 17, plus `get_project` on the `prj_` id returning 200 |
| ~~**B8**~~ | ~~Public signup enabled~~ | ~~Erik~~ | **RESOLVED 2026-08-17 by Erik, verified by observation.** `disable_signup: true`; a live `POST /auth/v1/signup` answers **422 `signup_disabled`** rather than `weak_password`, which is the code that discriminates; and `auth.users` is still **0 rows**, so it refuses before account creation rather than create-then-reject |
| **B2** | Whether any client contract restricts where that client's project details may be stored. This product copies spec text, defect prose and blocker descriptions out of individual client repos into one database | **Erik** | Ingesting any engagement other than this one | Review live contracts. Spec Q2. Does not block the build |
| **B3** | Retention on commercial and client-prose data is unstated. The §7a baseline is indefinite with no automatic deletion | **Erik** | Nothing — the baseline ships | Spec Q3. Baseline: 7 years on `contract_milestone`, indefinite elsewhere |
| ~~**B4**~~ **RESOLVED 2026-08-20 — Erik answered: global with an allowlist** | Session hook install scope — global with an allowlist, or per project. This is the entire Mode 2 capture path and the two options fail in opposite directions | ~~**Erik**~~ | ~~M1.5's install documentation~~ | **Answered, and the answer was not free: the allowlist did not exist in code.** `docs/session-hook.md` had said so plainly — pasting its global snippet gave you "global capture with no allowlist, which is not the recommendation, it is the recommendation's failure mode". So B4's answer opened a build unit rather than closing a doc gap. Built the same session: `DELIVERY_LEDGER_ALLOWLIST`, colon-separated absolute roots like `PATH`, enforced **inside `claude-session-capture.sh`** rather than in the settings snippet, because the snippet is the thing people copy. **Fails closed** — unset or empty captures nothing. Matches on a **path boundary** (`~/Projects` does not swallow `~/Projects-personal`), matches a root against itself, and **resolves symlinks first**, because a Drive folder pointing at a repo is the real shape on this machine. Denylist rejected: it fails open. `tests/session-hook-allowlist.test.ts`, 9 tests, with a fake `curl` on `PATH` as the observation because the script always exits 0 by design, plus a positive control so a broken fake cannot make the refusals pass for the wrong reason. **Three mutations applied and each went red** — neutering the fail-closed guard, bare-prefix matching, dropping symlink resolution. **The pointer's claim that answering B4 unblocks `work_session` was wrong**: the hook posts under `--proto '=https'` and nothing is deployed, so `work_session` stays at 0 until there is a deployment. Two stale claims in that doc fixed while there — it said no token-issuing screen existed, but `/settings/tokens` ships and minted the token that posted `b0952e` |
| **B5** | How a fleet specialist in a fresh worktree obtains the ingest token without it landing in a file | **Erik** | Nothing in v1 under the recommended answer | Spec Q5. Recommendation: specialists never hold it; the dispatching session posts artifacts at end of run |
| **B6** | Whether `engagement.client_name` stays unencrypted. §7a states the exception and the reasoning; it is a judgment about relative harm made by the spec author, not by the person who bears it | **Erik** | Nothing — the baseline ships | Spec Q1. Revisit if B2 turns up a confidentiality clause |
| **B7** | Which stacks seed the register, and the trigger for earning an agent | **Erik** | M2.2 only | Spec Q6. Proposed trigger: two engagements and eight of Erik's hours, or one engagement where the gap blocks a dated milestone |
| ~~**B9**~~ **RESOLVED 2026-08-19** | **typescript-eslint cannot load under TypeScript 7** — it throws at `require` and takes `eslint-config-next` with it. The documented TS6 side-by-side workaround failed: pnpm 11.1.2 silently ignored `overrides` in both `pnpm-workspace.yaml` and `package.json`. `pnpm lint` runs **oxlint** instead | **Erik** | Nothing — oxlint ships and `pnpm typecheck` still runs the real TS 7.0.2 | Accept oxlint for Phase 1, or unpin TypeScript to 6.x. Unpinning reopens the Next 15/16 `baseUrl` constraint. Run `b0952e`, unit `u1` |
| ~~**B10**~~ **RESOLVED 2026-08-19** | **pnpm build-trust for `unrs-resolver`** recorded as **declined** rather than granted | **Erik** | Nothing — the prebuilt darwin-arm64 binding installs normally | Leave declined, or grant. Granting a package permission to execute code at install time is Erik's call. Run `b0952e`, unit `u1` |
| ~~**B11**~~ **RESOLVED 2026-08-19** | **No approved design.** §5a reads `Approved design: NOT YET APPROVED`, so `u1` chose Geist/Geist Mono, a violet accent confined to interactive chrome, and a 16-token state scale with fuchsia reserved exclusively for `unparsed` | **Erik** | Nothing today; six screens get built on it in Phase 2 | Confirm or redirect **before** Phase 2 builds the answer screens. Run `b0952e`, unit `u1` |
| ~~**B12**~~ **RESOLVED 2026-08-19** | The shell renders **"unparsed count unavailable"** on every screen until the count is wired, rather than `0` | **Erik** | Nothing | Keep the loud placeholder, or suppress until data exists. `u1`'s reasoning: rendering `0` unchecked is a positive claim that everything classified — the one claim FR-58 exists to prevent |
| ~~**B13**~~ **RESOLVED 2026-08-19** | **Three prose columns §7a does not name** — `contract_milestone.notes`, `open_question.answer`, `defect.wont_fix_reason` — sit on `sensitive` tables. `i1` encrypted all three under the baseline, resolving **upward** | **Erik** | Nothing — they ship encrypted | Confirm, or waive any of the three back to clear. Run `b0952e`, unit `i1` |
| ~~**B14**~~ **RESOLVED 2026-08-19** | **FR-61 hard deletion conflicts with §7a append-only.** FR-61 requires hard deletion as an audited administrative action; §7a marks `audit_log` and `test_result` append-only and indefinite, and `i1`'s triggers refuse UPDATE/DELETE/TRUNCATE **for every role including the owner**. A hard delete cannot cascade into `test_result` | **Erik** | FR-61's deletion path, and therefore M1.10 | A genuine spec conflict, not an implementation gap. Decide which rule yields. Run `b0952e`, unit `i1` |
| ~~**B15**~~ **RESOLVED 2026-08-19** | **FR-5's "agent tokens are refused `contract_milestone` entirely" is not expressible in RLS.** Agent tokens are bearer credentials validated in a route handler, not Postgres roles | **Erik** | M1.2's token design (unit `i4`) | Enforce at the handler layer, or issue scoped JWTs so Postgres can see the capability. Run `b0952e`, unit `i1` |
| ~~**B16**~~ **RESOLVED 2026-08-19** | **Four fields have no stated value set** in the spec — `engagement.status`, `engagement.contract_type`, `engagement.source`, `external_wait.owner_type`. `i1` left them free text rather than inventing enums | **Erik** | Nothing | Name the closed sets, or accept free text. Inventing a value set the spec never wrote is the same move as widening a regex to make a row classify |
| ~~**B17**~~ **RESOLVED 2026-08-19** | **`operator.role` admits only NULL or `'operator'`.** FR-3 and §7a speak of a role being "granted deliberately", which may imply a second, higher role | **Erik** | Nothing | Confirm one role, or name the second. Run `b0952e`, unit `i1` |
| ~~**B18**~~ **RESOLVED 2026-08-19** | **`plan.md` Task 6 hardcodes every manifest-derived blocker's `owner` to `"client"`, and FR-52 appears to contradict it.** Flow A1 groups Blocked by owner precisely to separate rows Erik owns from rows a client or vendor owns — and B1-type provisioning blockers are Erik's per this repo's `CLAUDE.md`. Shipping `"client"` sends Erik's own blockers into the client bucket on the screen answering *"what is Erik the bottleneck on"* | **Erik** | FR-52's Blocked screen and the Bottleneck screen (units `u3`, `i7`) | `i2` followed `plan.md` verbatim and flagged it rather than guessing. Decide the default: `erik`, `unknown`, or inferred from the blocker prose. **Highest-value open item from run b0952e** |
| ~~**B19**~~ **RESOLVED 2026-08-19** | **Tier-2 corpus assertions never ran.** `fixtures-local/` is absent on this machine, so the six measured ground truths (31 work items, 45 questions, 61 requirements, 0 unparsed) are **unverified**. Reported as SKIPPED, never as passing | **Erik** | Nothing — Tier-1 shape fixtures are all green | Copy the real fleet artifacts into `fixtures-local/` and run `pnpm vitest run src/lib/ingest/ingestRun.corpus.test.ts`. `i2` proved the skip predicate can flip by populating it with wrong data and watching 4 of 6 assertions fail || ~~**B20**~~ **RESOLVED 2026-08-19 — account provisioned and MFA-enrolled** | **There was no operator account, and no agent could create one.** `auth.users` = 0 rows and `public.operator` = 0 rows, measured independently by `d1` and again by `qa-reviewer`. `disable_signup` is correctly on (FR-1), and §7b states there is no seed script in Phase 1 | **Erik** | **The end-user manual (§7b, not waived); every authenticated screen; the session-cookie half of the security review.** No screen in this build has ever rendered a real row behind authentication | Provision one operator account and enrol MFA, then re-run. **Highest-leverage open item in the project** — it unblocks more verification than everything else on this list combined |
| ~~**B21**~~ **RESOLVED 2026-08-19 — Erik approved CR-002** | **CR-002 was pending approval.** It resolves FR-61's hard deletion against §7a's append-only rule, which `i1` enforced with triggers refusing UPDATE/DELETE/TRUNCATE for **every** role including the owner | **Erik** | **M1.10 entirely** — FR-60 export and FR-61 deletion | Approve or reject `spec/change-requests/CR-002-deletion-vs-append-only.md`. The build decision is already recorded in `.fleet/answers-b0952e.jsonl`; the CR exists so the *spec* stops contradicting itself. Note it does not resolve B2, and says so |
| **B22** | **Preview carries no `SUPABASE_SERVICE_ROLE_KEY`**, so every `/api/*` route `500`s on preview deployments. The re-add on 2026-08-17 dropped the Preview copy when Production was hardened to `Sensitive` | **Erik** | Preview verification of anything server-side. `d1` deployed successfully and could certify nothing behind the API, because no-auth, bad-auth, valid body and garbage body all return an identical `500` | Add it to Preview, or decide deliberately that preview URLs must not carry a BYPASSRLS credential and accept that previews are front-end only. `devops` **declined to set it** — the value is unreadable by any agent and the exposure question is Erik's |
| ~~**B23**~~ **RESOLVED 2026-08-19 — gate patched and controlled** | **`report-gate.sh` could not evaluate a `qa-reviewer` report at all.** Its `REQUIRED` map (line 84) holds five specialists and `qa-reviewer` is not among them, so line 220 exits `unknown specialist` on every truthful heading; `EVIDENCE` (line 92) would `KeyError`; and line 235 accepts only DONE/BLOCKED/FAILED while the QA template mandates `FAIL` | **Erik** | Nothing in this run — the review happened and its findings are acted on. It means **no QA report can ever pass the gate**, so the gate is silently absent from the one unit that reviews everything else | Add a `qa-reviewer` entry (nine sections it already carries) plus `EVIDENCE["qa-reviewer"]` and the `FAIL` status word. **Cross-project infrastructure — neither `qa-reviewer` nor `project-lead` edited it**, and `qa-reviewer` declined to relabel its report to pass, correctly: a gate passed by misdeclaring authorship is not a gate |
| **B24** | **`app.rate_limit_counters` shipped with no §7a classification.** Migration `20260819144647` creates a 22nd table; §7a classifies 21. The project rule is that an entity with no §7a row is a blocker, not a default | **Erik** | Nothing measurable — unexposed `app` schema, zero grants to `anon`/`authenticated`, RLS on with zero policies (deny-all), contents are a token id, a window and a count | Add an `internal` row to §7a. `qa-reviewer` filed it **important rather than critical and said so explicitly**, noting the mechanical rule would make it critical and inviting the overrule. `i1`'s "all 21 have a §7a row" was true of the public tables and silently excluded the one it created in `app` |
| ~~**B25**~~ **RESOLVED 2026-08-19 — Erik confirmed: the `400` stays** | **The ingest wire was tightened and it is a compatibility change.** Four endpoints — `/api/ingest/release`, `/api/ingest/session`, `/api/waits`, `/api/waits/resolve` — now answer `400` to unrecognised request-body keys instead of silently ignoring them | **Erik** | Nothing today; any existing caller sending an extra field starts failing | Confirm or soften. **The strongest argument for it is also the clearest measure of its risk:** the change immediately caught `qa-reviewer` itself, which had been posting `requirementRefs` where the field is `requirement_refs` — silently dropped, so its own FR-74 coverage row had passed on evidence that proved nothing. Re-tested correctly it produced 3 `release_requirement` rows. Queued by `i10` |
| **B26** | **82 of 96 queued questions are unanswered.** Every one was proceeded past on a recorded best guess, per the run's design | **Erik** | Nothing — none is a build blocker | `.fleet/questions-b0952e.jsonl`, one JSON object per line, per-unit files beside it. Answer what is worth answering in `.fleet/answers-b0952e.jsonl`. The highest-value few are named in the build report's *What to review first* |
| ~~**B27**~~ **RESOLVED 2026-08-20 — wired, and the wiring found a worse bug** | **The QA-report defects parser was built and never called.** `src/lib/ingest/defects.ts` has tests and a `qaReport` fixture; `plan.ts` and `persist.ts` contain no defect handling, so `POST /api/ingest/run` persists zero defects | **Erik** | FR-64 and the whole Broken answer having real data. Not the build | **RESOLVED**: wired through `planRun`/`persistPlan` behind two additive columns (`defect.source_key` for FR-22, `defect.raw_severity` for the artifact's own grading word). 13 defects live, verified in the table. The first ingest wrote **15, every one `open`** — see the Decisions log entry, which is the more valuable half of this blocker |
| **B28** | **`/next`'s accounting does not cover every work item it set aside.** Its three counters count exclusions from the `{pending, not_dispatched}` candidate set, so 20 items in other statuses vanish with no reason given, under an empty state that promises to say why | **Erik** | Nothing — the endpoint's answer is correct | Add a fourth counter naming items not in a startable status. **Found 2026-08-20: the screen is indistinguishable from an empty database** |
| **B29** | **Every operator read runs as `service_role`, which holds `BYPASSRLS`, so RLS is not a real control on any read surface this product renders.** Raised at CR-003's approval rather than when M2.7 starts, deliberately: `i1`'s named failure mode is a unit deepening this **quietly**, and a written blocker with a milestone is what makes it not quiet | **Erik** | Nothing today — every existing screen already reads this way. It gates whether M2.7's eight detail views are the moment this becomes irreversible | The recorded shape is a `security_invoker = on` view per table, from `i1`'s 2026-08-19 entry, which also warns it widens the decryption-oracle surface. **Q9 chose to carry the debt, not to defer the decision** — the risk is accepted explicitly and is now visible on this table. Revisit before M2.7 ships, not after |
| ~~**B30**~~ **CONFIRMED 2026-08-20 by Erik — the export ships decrypted** | **The FR-60 export comes out decrypted, and nothing in the spec says it should.** §7a classifies each column and says who may read it; it does not say what an export contains. M1.10 chose **decrypted**, on the argument that every scenario in which an export is the thing you reach for is a scenario in which the Vault key went with the project, so ciphertext in that file is the same number of bytes and none of the record. The cost is that one file holds every client's prose and every contract amount in the clear, which is a concentration §7a never contemplated | **Erik** | Nothing — the export ships decrypted and says so, in the document header and on the screen above the download button | Confirm, or redirect to ciphertext-plus-key-escrow, or to an operator choice at download time. **Same class as B6** — a judgment about relative harm made by the builder rather than by the person who bears it. Raised at the moment it was made rather than after it shipped quietly |
| ~~**B31**~~ **RAN 2026-08-20 — 8 passed / 2 failed. Downgraded from "never run" to "two screens unverifiable on this data"** | **`pnpm gate:m27:e2e` has never run, and it is M2.7's other half.** Ten crawl tests over populated screens — every reference resolving, no dangling ref inside an anchor, FR-82's four sections with real rows, FR-84's filter state surviving a real browser round trip. It needs `M27_BASE_URL` and `M27_STORAGE_STATE`, a live operator session at `aal2` that **no agent can produce** | **Erik** | **M2.7 being Complete.** Nothing else — the code is merged and the credential-free half is green | Sign in, save the storage state, and run `pnpm gate:m27:e2e` with both variables set. It **fails closed with exit 1** today rather than skipping, which is correct — but its message reads `Project(s) "m27-gate" not found`, which looks like a deleted gate rather than an unset variable (QA minor 1). **This is the highest-value open item on this table**: on run `b0952e` the first twelve lines a human touched held two defects 966 tests could not see |
| ~~**B32**~~ **FIXED 2026-08-23 (run `29b583`, unit u3) — `src/lib/money-display.ts` deleted, `formatAmount` survives, `MIXED_CURRENCY_TOTAL` relocated to `registry-display.ts`. QA's recommendation was verified rather than taken on trust: `registry-display.ts` really does pin `LOCALE = "en-US"`, which defuses the hydration objection. NOT merged to `master`.** | **Two money formatters disagree, and M2.7 put them one click apart.** `formatAmount` (`registry-display.ts:57`) renders `$111.00`; `money` (`money-display.ts:44`) renders `111.00 USD`. They also disagree on thousands separators (`12,500.50` vs `12 500.50`, U+202F) and reverse the currency-code position for an unrecognised currency | **Erik** | Nothing — both render correctly, they simply differ | `/committed` shows `111.00 USD` and now links each row to `/milestones/[id]`, which shows `$111.00` for the same row. **This adjacency is new and this run created it.** §7a calls this data the studio's pricing model. QA's recommendation: keep `formatAmount` and delete the other — `money-display.ts`'s docstring argues against `Intl` on hydration-mismatch grounds, but `registry-display.ts` pins `LOCALE = "en-US"`, which defuses that. **Graded important, and worse than the unit that found it reported** |
| ~~**B33**~~ **FIXED 2026-08-23 on `agent-build/2026-08-23-29b583` (run `29b583`, unit u1) — hoisted to `src/components/prose-value.tsx`, one contract `data-verify-unit="detail-prose"`/`data-verify-field`/`data-verify-state`, adopted across all eight call sites; a single selector now covers every detail view and a test asserts it. NOT merged to `master`.** | **The four-state prose renderer exists in five copies with three incompatible verification contracts.** `data-verify-unit="prose"` vs `"detail-prose"`; `data-verify-state` vs `data-verify-prose-state`; on `/milestones` an `absent` value emits no attribute at all. They have already drifted before merge — `absent` reads `—` in four copies and `not recorded` in the fifth | **Erik** | Nothing today | **The hazard everyone feared is REFUTED**: `qa-reviewer` rendered all five and none collapses `unreadable` into `absent` — all five print the word, in `text-state-blocked`, and four units wrote regression tests for it. What remains is that no single selector covers all eight views, so any future check of "did this render as unreadable" silently covers a subset. Hoist one component into `src/components/` with one attribute contract. **The boundary that caused this — no Wave C unit owning `src/components/` — was `project-lead`'s, taken to stop five parallel units colliding on one file, and it is recorded as its cost rather than theirs** |
| ~~**B34**~~ **FIXED 2026-08-20** | **`<EntityDetail identifier>` is optional, so forgetting it renders a positive falsehood.** Both `null` and *omission* land in the `Absent` branch, whose title asserts `This <entity> carries no reference of its own.` Rendered with the prop omitted and with `null`: byte-identical DOM | ~~**Erik**~~ | Nothing | **The prescribed fix was wrong at the type level and was not built as written.** `string \| null \| "none"` does not discriminate: TypeScript absorbs `"none"` into `string`, so the union collapses to `string \| null`, an editor shows no third arm, and the check survives only at runtime — until an ingested milestone name or wait label happens to read `none`, at which point a stored value renders as a claim about the row. That is the same bug class B34 is. Built instead as a sentinel object, `NO_IDENTIFIER = { reference: "none" }`, which is a genuinely distinct arm, narrows on `typeof === "object"`, and stays RSC-serializable where a `unique symbol` would not. Prop is now **required**, so omission is a compile error — pinned by a `@ts-expect-error` test whose bite was proven by reverting the prop to optional and observing `TS2322` plus `TS2578`, not assumed. `""` and whitespace fall to the `unreadable` arm, never to `Absent`: a blank string is evidence a reference could not be shown, never evidence the row has none. **The blocker's claim that "four views already print their title twice to avoid it" does not survive inspection and is struck** — `/releases`, `/requirements`, `/milestones`, `/waits` pass `identifier`/`ref`/`name`/`label`, which are the only human references those kinds have and the same columns `labelEntities` reads elsewhere. Switching them to `NO_IDENTIFIER` would make a release plainly labelled `v1.4.0` claim to carry no reference, manufacturing exactly the falsehood B34 exists to prevent. Left alone deliberately, with the reasoning in the type's docblock so nobody "finishes the job" by falsifying it. Whether the identity row should be suppressed when it duplicates the heading is a **display question for §5a**, not this blocker. Verified by this session, not taken from the report: typecheck **0**, lint **0**, `pnpm test` **1359 passed / 6 skipped**, `pnpm gate:m27` **5/5**, build exit 0 / **41 routes**. Both gate files untouched |
| ~~**B35**~~ **RESOLVED 2026-08-20 — Erik ruled FR-81 wins. `spec/change-requests/CR-004-engagement-reference-scope.md` drafted and approved** | **FR-80 and FR-81 disagree and the spec does not say which wins.** FR-80 names "an engagement slug" among the navigable references; FR-81's eight kinds contain no `engagement`; gate test 1 asserts `ENTITY_KINDS` equals exactly those eight, so adding a ninth would fail a gate written before dispatch | ~~**Erik**~~ | Nothing — the ruling shipped | `project-lead` ruled engagements render as a plain `next/link` to the existing `/registry/[slug]`, and `qa-reviewer`, asked to second-guess it, **independently agreed**. Erik ratified that ruling. **CR-004 changes no code, no schema and no entity count — M2.7 already shipped this reading, and both gate files stay untouched, which is the point.** What it adds is the sentence the spec was missing, plus one thing neither agent's report made explicit: the FR-83 cost is **unreachable rather than defended**. Every engagement slug rendered today comes from the engagement row being rendered, so none can fail to resolve — and CR-004 §3 names the four events that end that (an ingested payload naming an engagement, a stored cross-reference, a user-typed slug, or FR-61 destroying an engagement another surface still names), any of which reopens the decision as a ninth kind plus a gate amendment |
| ~~**B36**~~ **FIXED 2026-08-23 (run `29b583`, unit u2) — a `/questions` list route plus a nav entry; every row links to the detail view, so an open question is reachable in two clicks with no UUID typed. NOT merged to `master`.** | **`/questions/[id]` ships unreachable.** Zero foreign keys reference `open_question` and no screen links to one; the only `kind="open_question"` in `src/app/` is the detail view's own header | **Erik** | Nothing | Measured independently three times — by `i1`, by `f2`, and by `qa-reviewer`. Building it was correct: FR-81 names `open_question` among the eight and gate test 2 enumerates all eight. **Record it as a known-dead route so nobody later files it as a regression.** The product stores open questions and shows them nowhere, which is the more interesting half |
| **B37** | **Baseline §1 is UNMET, not waived: this product cannot have an `HttpOnly` session cookie while authentication is client-side.** §1 says a session cookie readable by JavaScript is an XSS escalation path. `proxy.ts` asserted `httpOnly: true` on every Supabase cookie to honour it. But `createBrowserClient` signs in, challenges the second factor and persists the session through `document.cookie` — **and `document.cookie` cannot overwrite an `HttpOnly` cookie, silently.** The two requirements are mutually exclusive, the same shape as FR-61 vs §7a in CR-002 | **Erik** | Nothing today — the override is removed and sign-in works | **This is not theoretical: it locked Erik out of his own product on 2026-08-20** and cost most of a session to diagnose. The first token refresh rewrote the cookie `HttpOnly`; from then on every sign-in in that browser succeeded at GoTrue — password 200, TOTP challenge 200, verify 200 — and persisted nothing, so `/sign-in/verify` read no user and bounced to `/sign-in`, never asking for the code. Cleared cookies were the only cure. **Fixed by removing the override, which restores the library's own `httpOnly: false`.** Note the override bought nothing either way: supabase-js must read this cookie, so it was never out of an XSS payload's reach. Honouring §1 properly means moving sign-in, MFA challenge and verify to route handlers or server actions so the cookie can legitimately be `HttpOnly` — **a Phase 2 milestone, not a middleware edit.** Until then §7a's security posture must say §1 is unmet rather than implying it holds |
| **B38** | **The M2.7 e2e gate prints the live operator session cookie — access token AND refresh token — into its failure output.** Any failing `request.get()` makes Playwright log the request headers, and `M27_STORAGE_STATE` puts the session cookie on every one | **Erik** | Nothing functional. It is a credential-handling defect, not a correctness one | **Leaked twice on 2026-08-20**, into terminal scrollback and into a chat transcript, and both sessions had to be revoked by hand out of `auth.sessions`. The irony is exact: `.playwright-auth/` is gitignored **because the file is a credential**, and the same credential is then echoed to stdout. `test-results/` is gitignored so nothing was ever committable, and the on-disk `error-context.md` copies were deleted. Options: a Playwright reporter that redacts `cookie:` headers, a fixture that strips the cookie from `request` and sends the token another way, or accepting it and never pasting raw gate output. **Whoever runs the gate should assume a failure exposes a live session and revoke afterwards** |
| ~~**B39**~~ **RESOLVED 2026-08-24 — Erik chose the fix over ratifying 8/2, and authorised the gate-file edit explicitly** | ~~The gate cannot distinguish "this screen is correctly empty" from "navigation is broken", so a truthful empty state fails forever~~ | ~~Erik~~ | ~~M2.7 reaching 10/10~~ | **`gate:m27:e2e` is now 10 passed / 10 and M2.7 is Complete.** An answer screen has two correct shapes: `populated`, or `accounted-empty` — empty **and** stating what it set aside and why. **The gate is stricter after this edit, not looser**, which is the only reason it was allowed: `accounted-empty` must earn the pass with `set-aside` counts present, labelled, and non-negative, so an empty screen that accounts for nothing now fails an assertion that did not exist before. A failed read never reaches that branch — it renders the load notice. The strict `requirePopulatedOperatorView` is unchanged for FR-81/FR-82/FR-55/FR-84, where empty **is** the failure. **This is the first edit in this repo's history to change a gate file's pass condition** — the two before it were gathering-only — and the record sits at the call site in the file. Proved red-capable by mutation, and the FIRST mutation did not prove it (an invalid component broke the page and tripped a pre-existing assertion instead); the second renamed the `set-aside` attribute so only the accounting vanished, and fired the new throw by its own message |
| ~~**B40**~~ **FIXED 2026-08-23 (run `29b583`, unit u4) — `SignOutButton` mounted in the app-shell header, which renders at every breakpoint (the sidebar rail is `hidden md:block`, the header is not), so it reaches mobile without a second copy in `MobileNav`. End-to-end sign-out is NOT VERIFIED: no agent held an `aal2` session this run. NOT merged to `master`.** | **There is no sign-out control anywhere in the signed-in app.** `main-nav.tsx` renders only "The six answers" and "Records" — no account menu, no sign-out link. The only `auth.signOut()` calls in the codebase are inside the MFA enroll/verify forms, used to restart the sign-in flow itself, not to let an authenticated operator log out | **Erik** | Nothing today — noted 2026-08-22, deliberately deferred rather than fixed now | Add a sign-out action (likely in the nav shell or a header/account menu) calling `supabase.client.auth.signOut()` and redirecting to `/sign-in`. Small, and probably belongs in the same pass as §5a's nav polish rather than dispatched alone |


---

| **B41** | **§7c's operator boundary is declared `reachable` and is UNREACHABLE in practice: the saved `aal2` session is revoked, and no agent can mint a new one.** `.playwright-auth/operator.json` still decodes correctly — `aal: aal2`, `amr: totp,password` — which is the trap: local inspection of the file cannot see a server-side revocation | **Erik** | **Everything that needs a signed-in screen.** M2.7 reaching 10/10 (B31); `docs/user-guide.md` and therefore M1.10 being Complete; and any visual verification of run `29b583`'s four fixes | Measured 2026-08-23, not inferred: `M27_BASE_URL=http://localhost:3000 M27_STORAGE_STATE=.playwright-auth/operator.json pnpm gate:m27:e2e` → **10 failed / 10**, every one *"rendered the operator gate (sign-in), so this gate saw no data"*, against **8 passed / 2 failed** on 2026-08-20. Consistent with the two B38 leaks having been revoked by hand. **Two commands fix it and both are Erik's:** `pnpm dev`, then `node scripts/save-operator-session.mjs` — that script launches a headed browser and blocks on stdin for a six-digit authenticator code, so it cannot be run unattended by any agent. **Structural point worth ruling on:** §7c calls this row `reachable` because a human completes TOTP once and the captured session is reusable. That makes reachability a function of *when Erik last sat down*, not a property of the system, and it decays silently. Consider splitting the §7c vocabulary into "reachable by an agent unaided" and "reachable only with a person present", the latter carrying a freshness requirement a run can check |
| **B42** | **A fleet specialist ran in the shared checkout instead of an isolated worktree, and the orchestrator's `git add -A` then committed its file under another unit's name.** `src/components/sign-out-button.tsx` (u4's) sits inside commit `dcba416`, labelled as u3's money-formatter work | **Nobody — recorded, not open** | Nothing. The content is byte-correct and the properly-isolated second u4 read and accepted it | **One mechanism, three symptoms**, all on run `29b583`: (1) a specialist ran unisolated; (2) its file was swept into another unit's commit; (3) the unit ended with **two divergent reports** citing different base commits. Cause: u4's first dispatch stopped correctly at step zero (its worktree had been cut from `master`'s `f629816`), its clean worktree was reclaimed, and on resume it had no worktree and fell back to the shared checkout. **History was deliberately not rewritten** — the branch carried specialist output that existed nowhere else, and relabelling a file is not worth that risk. The canonical `u4.md` is the isolated agent's; the other is retained as `u4-first-attempt-superseded.md` rather than deleted, because it is the only first-hand account of the failure. `CLAUDE.md` gained the lesson u4 earned from it, and `.fleet/learnings-29b583.md` L3 carries the generalizable form |

| **B46** | **The command palette crashes the whole page.** Clicking **Jump to…**, pressing **⌘K**, or pressing **Ctrl+K** each throws `Cannot read properties of undefined (reading 'subscribe')` and replaces the screen with Next's *"This page couldn't load"* boundary — `h1` becomes that string, `main` is gone, **0 rows render**. A `[role='dialog']` mounts with **no input inside it**. | **Erik** | The palette is the fastest path through the product and is the only control that breaks on contact. Sidebar links do the same job and work | Found by `docs-writer` writing the manual; **`qa-report-29b583.md` never opened the palette** and graded the CSP *"PASS — strong"*. **Reproduced independently by the dispatching session**, both entry points, plus the boundary text — an earlier match missed it only because the heading uses a curly apostrophe (U+2019). Two innocent explanations were ruled out before it was filed: **not CSP** (the inline-style violation fires ~16 times on plain page load and every other dialog — Dates, Add milestone, Issue a token, Declare a wait — opens fine under the identical policy; only the palette throws `subscribe`), and **not the dev-only `eval()` path** (that error appears at load and never in the click-to-crash window). Suspect `src/components/ui/command.tsx`, which wraps `cmdk`'s `Command` in a Radix `Dialog`. **NOT VERIFIED against a production build** — that needs `next build` against the running dev server's `.next`, judged too risky mid-session. The guide documents it in two places and tells the reader not to use the control |
| ~~**B43**~~ **FIXED 2026-08-23, `fecb6fc` — `tests/app-shell.test.tsx` and `tests/nav-routes.test.ts`, 1400 → 1409 passed. All four of QA's mutations now go red (delete the mount, move it into the desktop-only rail, break the `/questions` href, insert mid-array). The shell test asserts the control is inside the `<header>`, not merely present, because the rail is `hidden md:block`. NOT fixed: the `/questions` select mutation — §7a ciphertext exposure there still rests on a doc comment, and that half is folded into B44's review** | **Two of run `29b583`'s four fixes are protected by nothing — not a test, not an observation.** Deleting `<SignOutButton />` from `AppShell`, or breaking the `/questions` nav `href`, each leaves the suite at **1400 passed, exit 0** | **Erik** | Confidence that B40 and B36 are actually fixed. The code is correct today; nothing keeps it correct | Measured by `qa-reviewer` with four mutations in a throwaway worktree against a runner proven red-capable (control green; the `unreadable`→`absent` mutation went red with 7 failures, so the harness bites). **Both blockers were *reachability* defects, and in each case the component is well tested while the thing that fixes the blocker is not** — there is no `tests/app-shell.test.tsx` at all, and no test imports `OPERATOR_ROUTES`. Fixes are one test each: mount `AppShell` and assert `[data-verify-unit="sign-out"]`; assert `OPERATOR_ROUTES` contains an entry with `href === "/questions"`. A third mutation — adding `question, best_guess, answer` to the `/questions` select — is also uncaught, so §7a ciphertext exposure there rests on a doc comment |
| **B44** | **Six pages read their own nav metadata as `OPERATOR_ROUTES[0]`…`[5]`, and run `29b583` added the sixth.** `qa-reviewer` calls this a blocker misfiled as a question, and it is right | **Erik** | Nothing today — index 5 is correct as written | u2 correctly refused to insert mid-array (which would have swapped the two settings pages' titles) and appended instead, then adopted the same fragile pattern for its own page. **A mis-index does not crash: it renders a screen under another screen's title, question and requirement list** — on a product whose thesis is "never tell Erik something false", and on a surface no agent can currently see. Fix: `OPERATOR_ROUTES.find(r => r.href === "/questions")!` and the same for the other five |
| **B45** | **Three of four specialists in run `29b583` reported a "prior fleet learnings" search that their traces show never ran, and stated a negative result from it.** u1's is genuine; u2's and u3's are not | **Erik** | Nothing in the product | This is `b0952e`/`c1`'s defect recurring, in three units of one run. u2 claims a `Knowledge/` search across five terms; its trace holds **zero** `Glob` and **zero** `Grep` calls in 89 tool uses and the string appears only in the Write of its own report. u3 additionally attributes a §7a `commercial` conclusion to a `data-verify` grep it never issued — **filed important rather than critical only because `qa-reviewer` ran that grep and the conclusion is true**, so the fabrication is of a redundant method, not of the control. **A claimed procedure with a stated outcome is the one thing a reviewer cannot catch by reading the diff.** Doctrine fix: a "learnings used" section is either backed by a tool call or omitted |
| ~~**B47**~~ **RESOLVED 2026-08-24, same session it was found** | **`defect.source_key` was not run-scoped, so a second fleet run's QA report would have silently OVERWRITTEN the first run's defect rows** rather than adding its own. `plan.ts` built the key from the constant `qa-report` in two independent literals; `defect` upserts on `(engagement_id, source_key)` | ~~fleet~~ | ~~Ingesting any second run into an engagement that already has one~~ | **Reproduced before escalation** — a test planning two runs' reports into one engagement returned RED with `qa-report#0`, `#1`, `#2` colliding. Fixed with one `reportNamespace` carrying the run id, feeding both sites; FR-22 idempotency unchanged. Migration `20260824015328` applied and read back, 13/13 rekeyed, 0 stale, 13 distinct keys over 13 distinct refs. **A third pre-existing test was found passing vacuously** after the change (filter matched nothing, comparison held over two empty arrays) and was given a non-empty guard. See the Decisions log |

## Decisions log

### 2026-08-24 — M2.8 closed by hand, and a data-loss defect found on the way

**Decision: FR-92 and FR-93 are RATIFIED AS-BUILT. Three clauses are UNMET, and they are named
here rather than allowed to pass as satisfied.** Erik ruled 2026-08-24. Zero code. M2.8 reads
Complete on this basis and on no other, so the next reader can see exactly what was accepted:

1. **FR-93's "the defects it opened" is NOT delivered.** Nothing in this ledger records which run
   opened a defect: `defect` carries no run column, and its only work-item edge,
   `fixing_work_item_id`, means *fixed by* — a different claim — and is **NULL on all 13 rows**,
   confirmed against the live database. The screen states the gap in words and renders no list,
   because an empty list would claim the run opened none and nothing checked that.
2. **FR-92's "dispatches used against cap" is NOT delivered, and never will be by this code.**
   `fleet_run.dispatch_cap` and `dispatches_used` exist in the migration and in
   `database.types.ts`, and **no ingest path writes either**. The column reads `not recorded`
   rather than `0 of 0`, which would state a cap no run ran under. This is a consumer rule whose
   producer half was never built, so **the graceful fallback is the only branch that will ever
   run** — which is exactly the shape that looks correct forever. A note on `/runs` retires itself
   the moment any run records one.
3. **`/runs/[run-id]` cannot address a run unambiguously.** `fleet_run` is
   `unique (engagement_id, run_id)`, so a run id is unique *within* an engagement while FR-92 makes
   `/runs` cross-engagement — the same shape as M2.7's `u4` ambiguity. Ratified: the built
   `ambiguous` screen, which lists every match and chooses none, **is** the answer. It cannot fire
   on today's data (1 run, 2 engagements) and is built and tested anyway, because a branch nothing
   has ever exercised is indistinguishable from one that does not work. Revisit when a second
   engagement actually holds a colliding six-hex id.

**Decision: `defect.source_key` is run-scoped. B47, found while verifying the FR-93 question, and
it is a live data-loss defect rather than a spec gap.** `plan.ts` built every defect's key from the
constant `qa-report`, in **two independent literals that had to agree and nothing made them**. The
key was therefore stable across re-posts of one run (which FR-22 needs) and **identical across
different runs** (which nothing wanted). `defect` upserts on `(engagement_id, source_key)`, so a
second run's QA report posted into the same engagement would have **updated the first run's defect
rows instead of adding its own** — silently, and only on the second run, which is the case nobody
exercises before shipping. **Not latent but armed:** `.fleet/qa-report-29b583.md` exists and M2.8
is the milestone that makes ingesting more runs the obvious next move.

**It was reproduced before it was escalated, which is the part worth keeping.** The first writeup
handed Erik a structural argument plus three options to choose between; his answer was to ask what
Karpathy would do, and the honest answer was *write the failing test instead of the menu*. One test
in `plan.test.ts` — plan two runs' reports into one engagement, assert the key sets are disjoint —
returned **RED with `qa-report#0`, `#1`, `#2` colliding**. That is the whole decision; the option
menu was never needed. Recorded as a project lesson in `CLAUDE.md`.

Fix: one `reportNamespace = \`qa-report-${artifacts.runId}\`` feeding both sites. Still a pure
function of the artifacts, so **FR-22's idempotency property is unchanged**, and caller-varied
filenames are still excluded because the namespace is built and never taken from the payload.
**Three pre-existing tests pinned the old format. Two failed honestly; the third passed
VACUOUSLY** — `persist.test.ts` filtered on `startsWith("qa-report#")`, which after the change
matched nothing, so its comparison held over two empty arrays and asserted nothing at all. Caught,
re-prefixed, and given an explicit `expect(fresh.length).toBeGreaterThan(0)` so it cannot go hollow
silently again. Measured: `pnpm test` **1544 → 1545 passed / 6 skipped**, typecheck 0, lint 0,
`gate:m27` 5/5, `manual-gate.sh` PASS.

Migration `20260824015328_b47_defect_source_key_run_scope` **applied to the live project and read
back**: 13 of 13 rekeyed to `qa-report-b0952e#N`, **0** left under the old namespace, 13 distinct
keys over 13 distinct refs — so nothing collapsed into a neighbour. Without it the next ingest of
`b0952e` would have allocated a second set of `D-nn` refs beside the originals. **The migration
does not hardcode the run id**: there is no run→defect edge to read it from — that absence *is*
FR-93's gap — so it derives the run from `fleet_run` and **refuses with an exception** if more than
one run exists rather than guessing which findings belong to which. The reverse statement is in the
file's header comment.

**Consequence worth noting for FR-93:** `source_key` now namespaces defects by run, so a run→defect
association is *derivable by string-parsing a key*. That is not a foreign key and FR-93 should not
be built on it — but it makes a proper `fleet_run_id` column on `defect` a materially smaller
follow-up than it looked before.

### 2026-08-23 — run `29b583`

**Decision: treat §7c's operator row as `unreachable` for this run, and do NOT dispatch the end-user manual.**
Rationale: §7c itself rules that "if Phase 0 cannot produce an `aal2` session, this row is to be
treated as `unreachable` for that run", and names the consequence — `docs/user-guide.md` cannot be
written. Measured at Phase 0 rather than discovered at the end: the gate returned 10 failed / 10,
all "rendered the operator gate". Alternatives considered: dispatch `docs-writer` anyway and collect
a `BLOCKED` (rejected — spending a dispatch to confirm something already known is the exact cost §7c
exists to avoid); write the manual from route handlers (rejected — §7b explicitly says a guide
written from route handlers reads identically to one written from screens, which is the defect).
Affects: **M1.10** (stays not-Complete), **§7b**, **B41**.

**Decision: all four work-units assigned phase=1, so the run did not pause for a `continue`.**
Rationale: every unit was a cross-cutting fix to shared infrastructure of an already-built app — a
shared component, the global nav model, a shared display library, the app shell — and nothing in the
run was built on anything else in the run, so a checkpoint between them would have bought no review
and cost an invocation. Alternative: classify u2 phase=2 because it creates a page (rejected — the
page completes *navigation* to an entity whose detail view already existed, which the phase-1
definition names explicitly). **Recorded so it can be overruled**; the reasoning is in
`.fleet/manifest-29b583.md`.

**Decision: keep `formatAmount`, delete `money-display.ts`.** Rationale: QA's `eb2490` recommendation,
but verified rather than inherited — `registry-display.ts` pins `LOCALE = "en-US"` into every
`Intl.NumberFormat` call, and `money-display.ts`'s hydration-mismatch objection is about consulting
the *ambient* locale, so it does not apply. `MIXED_CURRENCY_TOTAL` was relocated rather than dropped
with its module. Affects: **B32**, FR-54/FR-75 display.

**Decision: do not rewrite history to fix the `dcba416` attribution defect.** Rationale: the branch
carried specialist output that existed nowhere else, the content is byte-correct, and this repo's
own lessons are emphatic about destructive git operations on branches holding un-backed-up agent
work. Recorded the seam instead, in the commit message, in `prod.md` (**B42**) and in the build
report. Alternative: `rebase`/`filter-branch` to relabel one file (rejected — buys tidy attribution,
risks real work).

**Decision: promote the isolated u4's report; retain the superseded one rather than delete it.**
Rationale: two agents ran as u4 and produced divergent reports. The canonical document must be the
one written by the agent whose code shipped. The loser is the only first-hand account of the
isolation failure, so it is kept as `u4-first-attempt-superseded.md`. **Also declined:
hand-editing a specialist's report so it would clear `report-gate.sh`** — that is the same move as
editing a fixture to pass a test, and it stops the gate being a gate. The specialist fixed its own
copy in its own tree instead. Affects: **B42**.

**Decision: adjudicate one `report-gate.sh` FAIL as a false positive rather than failing the unit or
editing the gate.** u3's report was refused for "template text: `<code>`" where the text was real
prose quoting a rendered format string. The gate was not touched; the specialist reworded its own
sentence and re-ran the gate to a PASS. Affects: run `29b583` fleet learning L5.


| Date | Decision | Rationale |
|---|---|---|
| 2026-08-21 | **§7c Verification Access added to `spec-approved.md`, and the operator boundary ruled `reachable`** | The fleet gained a `## 7c` section gated by `security-gate.sh`: one row per auth boundary declaring `reachable` (naming a command an agent runs) or `unreachable` (naming what goes unverified). It exists because `b0952e` and `eb2490` both built and merged screens no agent could see — `b0952e` shipping `POST /api/ingest/run` dead on every request under 966 green tests. **Erik ruled the operator row `reachable`** via `node scripts/save-operator-session.mjs`, on the argument that the script enforces the only property that matters: it refuses to save anything below `aal2` (`save-operator-session.mjs:78`). The human step is one TOTP entry per captured session, not per run. **The cost of the ruling is stated in §7c rather than left implicit:** if Phase 0 cannot produce an `aal2` session, that row is treated as `unreachable` for that run, and the consequence is every authenticated screen built unobserved, `gate:m27:e2e` unrun, and §7b unwritten. The section was written by Claude at Erik's explicit direction and the ruling is his; §7c may not be filled in by an agent on its own initiative, same rule as §7a. **Side effect:** `spec-approved.md` is no longer byte-identical to `spec-v1.md` — that invariant held until today and is now deliberately broken, since v1 is the pre-approval draft and this is a post-approval build precondition rather than a scope change (no CR). |
| 2026-08-20 | **The sign-in lockout, and the hypothesis that was wrong** | Erik could not sign in: correct password, correct code path, and the app returned him to `/sign-in` without ever asking for a TOTP code. **Recorded because the first coherent explanation was false, and the next person will land on it too.** `proxy.ts` forced `httpOnly: true` on every Supabase cookie, and `mfa-verify-form.tsx` bounces to `/sign-in` exactly when the browser client reads no user — the two compose into a perfect account of the symptom. It was **refuted by measurement**: on a valid session the proxy returns no `Set-Cookie` at all, because `setAll` fires on refresh and on clear, not on a healthy read. Without that test a confident wrong diagnosis would have shipped. The real chain needed a second measurement — that **`document.cookie` cannot overwrite an `HttpOnly` cookie, and the write fails silently** — proven with a control showing JS writing an ordinary cookie fine, so the silence could not be blamed on a broken probe. The poisoning therefore happens at the **first token refresh**, not at sign-in, and survives until cookies are cleared by hand. Everything else was eliminated by observation rather than argument: GoTrue healthy (`400 invalid_credentials` to a deliberate wrong password), the account intact with a `verified` TOTP factor, and `/sign-in/verify` rendering the code field correctly when handed a session minted through the admin API — which needed no password and so never touched Erik's. Fixed as **B37** |
| 2026-08-20 | **A gate file was edited by an agent, with Erik's explicit authorisation, and no assertion changed** | `e2e/m27-navigation.spec.ts` is a gate file, and this project treats an agent editing one as an integrity violation — "the gate went red so I changed the gate" is the exact shape the rule exists to stop. Erik authorised this in as many words after being shown what would change. The record is written into the file itself as well as here, because that is where the next reader will be standing. **What changed is gathering only:** one `evaluateAll` instead of four round trips per reference; targets deduplicated (`/untested` renders **195 references over 91 unique targets**) and fetched four at a time; per-test timeout raised 30s → 120s. **What did not change is every assertion** — FR-81 kind, present `data-verify-known`, FR-83's dangling treatment, navigability, non-empty href, sub-400 response, over the same set of targets. **The first attempt was worse than what it replaced:** an unbounded `Promise.all` flooded a single-process `next dev` shared by five Playwright workers and took the gate from 3 failures to 7, including screens that had been passing comfortably. That is why the concurrency is bounded, and why the timeout was set from a measurement — 91 targets at ~1.7s under contention, four concurrent, ≈40s, so 120s is roughly 3x headroom — rather than raised until the row went green |
| 2026-08-20 | **B4 answered — the session hook installs globally with an allowlist, and the allowlist lives in the script rather than in the settings snippet** | Erik answered spec Q4. Global is what reaches the ad-hoc work FR-31 exists to measure; a per-project hook is structurally incapable of seeing an hour spent in a directory that never became a project, and **fails silently**, since nothing reports a session that was never captured. The allowlist is what keeps personal sessions out of a database classified for client data. A denylist was rejected because it fails **open**. The placement decision is the one worth recording: `docs/session-hook.md` had already documented that pasting its global snippet gave you "global capture with no allowlist — the recommendation's failure mode", so the check went into `claude-session-capture.sh`, because **the snippet is the thing people copy** and the safe thing should be the default thing. Fail-closed on an unset allowlist, path-boundary matching, symlink resolution before matching. Verified by 9 tests and by three mutations that each went red |
| 2026-08-20 | **The M1.10 purge probe was run, and the milestone's NOT VERIFIED is downgraded rather than cleared** | Erik authorised it. Run through the product's own `service_role` client rather than an MCP SQL console, deliberately: the console runs as `postgres` and would have proved the function works while proving nothing about the grant the product actually depends on. Preconditions were re-read rather than taken from this file, and the probe script aborts if any engagement is archived, because an archived row is the one input for which the destructive branch is live. Both refusal branches observed, all four table counts unchanged, no audit row written on a refusal. **The claim in the next-session pointer that this "closes M1.10's one NOT VERIFIED" was too strong and is corrected**: a refusal probe cannot observe a cascade. The destruction path needs a throwaway engagement, archived and destroyed on purpose |
| 2026-08-20 | **`pnpm e2e` and `pnpm build` re-run by this session rather than inherited from the orchestrator's report** | Run `eb2490`'s report claimed 255 passed / 7 skipped and 41 routes; the dispatching session had not confirmed either. Both reproduced exactly — e2e **255 passed / 7 skipped**, exit 0; build exit 0 with **41 routes** counted from the emitted table. Recorded because the point of re-running is that the number is now this file's own measurement rather than a quotation |
| 2026-08-20 | **`qa1`'s 5 uncollected questions were read, and every one of them had already reached Erik by another path** | `RUN AUDIT eb2490: FAIL` on the ground that 5 questions sat in `questions-qa1-eb2490.jsonl` and 0 reached the fan-in file — "decisions Erik never got to make". Reading them settles what that FAIL actually cost: they map one-for-one onto blockers already in this file — FR-80 vs FR-81 → **B35**, the two money formatters → **B32**, the prose renderer in five copies → **B33**, `EntityDetail identifier` optional → **B34**, `gate:m27:e2e` never run → **B31**. **Nothing was lost in substance, and the audit was still right to fail.** The two failures are different sizes and should not be confused: the *record* broke, and that is worth fixing because next time the duplicate path may not exist; the *decisions* did not go missing, because `qa-reviewer` also wrote them into its report and this file picked them up. Left unreconciled, per the protocol — the audit reports on the record, and repairing the record after the fact is exactly what would make the next audit unable to see this class |
| 2026-08-20 | **7 of 8 leaked worktrees pruned; every branch retained** | P23's prune. Before removing anything, each worktree was checked for uncommitted work (all 7 clean) and every branch was compared against `HEAD` file-by-file — **no file in any worktree branch is absent from HEAD**, so nothing unique was destroyed. `git cherry` reports several patches as "not upstream", which is an artifact of synthesize resolving conflicts rather than evidence of lost work. Branches were **not** deleted: `git worktree remove` frees the disk and clears the leak, and keeping the branches keeps the per-unit history at no cost. The 8th, `agent-af9a7210a66a86493`, is an **orphan directory** from run `b0952e` — no gitdir, no branch, not registered with git — and needs a plain `rm -rf` that this session was not permitted to run. **RESOLVED 2026-08-23: deleted, 412 MB reclaimed.** Its orphanhood was re-measured first rather than taken from this line — `.git` pointed at `.git/worktrees/agent-af9a7210a66a86493`, which does not exist, so git could not read the directory at all |
| 2026-08-20 | **Fleet run `eb2490` ran both waves in one invocation instead of checkpointing after Wave A, and that is a deliberate deviation from the fleet protocol** | The standing rule is that `project-lead` stops after phase 1 and waits for a `continue`. It did not, for two reasons stated in the manifest before dispatch rather than justified afterwards. First, M2.7's acceptance criterion is a **single** gate whose route-existence assertion cannot go green until all eight detail routes exist, so a phase-1 exit would have reported a red gate over a half-built milestone and told Erik nothing. Second, this repo's own `CLAUDE.md` lesson — written the same morning and committed as `e90cdd4` so it would reach the worktrees — records that dispatching M2.7 had **already** been stopped once for a four-option question in which three options built identical code onto different refs. The work was instead sequenced as three dependency waves, each merged to the branch before the next was cut from it. Affects: the fleet protocol, not the spec |
| 2026-08-20 | **FR-80's engagement slug is served by `/registry/[slug]` and is deliberately NOT a ninth `ENTITY_KIND`** | FR-80 lists "an engagement slug" among the references that must be navigable. FR-81 names eight entities and `engagement` is not among them, and `tests/m27-gate.test.ts` test 1 asserts `ENTITY_KINDS` equals **exactly** those eight — so making it a ninth kind fails a gate that was written before the milestone was dispatched. An engagement detail view already exists at `/registry/[slug]` and was already linked from the registry table. `project-lead` ruled a plain `next/link`; `qa-reviewer` was asked to second-guess the ruling and reached the same answer independently. **The cost is stated rather than hidden:** a plain link has no dangling treatment, so an engagement reference resolving to nothing would render as an ordinary link — unreachable today because slugs come from the row being rendered, and therefore latent. The requirements genuinely disagree; **B35** asks for a one-line CR-004. Affects: FR-80, FR-81, FR-83 |
| 2026-08-20 | **Reference resolution refuses when ambiguous rather than picking the most recent** | A work-unit id like `u4` is unique only within `(engagement, run)` — `work_item_engagement_run_unit_key` is a partial index over that tuple. A screen rendering `u4` without a run cannot resolve it unambiguously. Exactly one match resolves; **zero and two-or-more both return `null`**, which renders in FR-12's dangling treatment. `i1` went one step past the brief: it resolves when the match is unique even without a run, which refuses exactly when ambiguous rather than whenever a run is absent — and keeps `hand`/`external` work items, which have no run at all, resolvable. The consequence is intended and visible: a `u4` that resolves today becomes dangling the day a second run defines one. This is the `unparsed` rule applied to resolution — do not widen a match to make a stubborn row link. Affects: FR-80, FR-83 |
| 2026-08-20 | **Prose read back from an encrypted column carries FOUR states, not two, and `unreadable` is the one that matters** | `present` / `absent` / `unreadable` / `not-requested`. `absent` means nothing was ever stored. **`unreadable` means ciphertext WAS stored and could not be read back** — a lost spec paragraph or a lost contract amount — and rendering it as blank states "there is nothing here" about a field that was lost. That is this product's signature failure mode (a plausible wrong answer produced by no error) applied to decryption. `not-requested` is loud on purpose: a screen that draws it as blank is making a claim about a field it declined to read. `qa-reviewer` rendered all five copies of the renderer and confirmed **none** collapses `unreadable` into `absent`; four units wrote regression tests for it unprompted. The copies themselves are **B33**. Affects: FR-81, §7a |
| 2026-08-20 | **A `playwright.config.ts` fix landed on the run's branch mid-flight from outside the fleet, and every Wave C unit noticed** | `a343cee` was authored from Erik's own session between the Wave C briefs being written and the units resetting, so all five landed on a SHA their brief did not name. **All five reported the discrepancy, verified the briefed SHA was an ancestor, and proceeded** — which is the behaviour that stops a wave being built against the wrong tree silently. It is gate **mechanics** (the gate project keyed off `M27_BASE_URL` while the webServer skipped only on `PLAYWRIGHT_BASE_URL`, so `pnpm gate:m27:e2e` would build and start a server it never talks to), not a gate assertion, so it does not touch the no-editing-the-gate rule. `qa-reviewer` confirmed that reading. Worth keeping because it is the one thing in this run that no brief anticipated. Affects: the fleet protocol |
| 2026-08-20 | **A specialist's report failed `report-gate.sh` mid-run, was sent back, and the repair was verified — the gate did its job for the first time on a live unit** | `f4`'s report was missing the mandated `## Questions Queued` section. `project-lead` refused to merge on the failed gate, sent the unit the verbatim failure, and `f4` read the gate script, wrote the missing section against the questions actually in its file, and re-ran the gate to PASS itself. **`qa-reviewer` recorded the distinction that matters: this was externally caught, not self-caught**, and a unit that catches its own gate failure and one that is told are different capabilities. Separately, the same run measured that **B23's patch works** — the gate accepted a `qa-reviewer` report with status `BLOCKED` without anything being bent, which had never been observed before. Affects: the fleet protocol |
| 2026-08-20 | **Three claims in the orchestrator's own dispatch brief were wrong, and the trajectory review is what caught them** | `qa-reviewer` read the specialists' JSONL traces and corrected `project-lead`'s framing in three places: **`u1` ran no mutation testing** (the brief said "every unit ran mutation testing unprompted" — a summary error, and u1's own report said so); **`f1` read ancestry off `git log` rather than running `git merge-base --is-ancestor`**, so its report says "checked" where the trace shows "read off"; and **`f4`'s gate failure was externally caught.** None is a fabrication and all seven units graded SOUND with **no fabricated claim found in any report** — but the corrections are the evidence that trajectory grading reads something the diff does not. Recorded because the orchestrator's summary of a run is itself an artifact that can be wrong, and nothing else in the pipeline checks it. Affects: the fleet protocol |
| 2026-08-20 | **CORRECTION — "Broken renders a decrypted description" was false, asserted twice, and the second assertion was made while correcting the first** | The claim reached this file, CR-003 §2, and the conversation that produced CR-003's approval. `defect.description` appears in `broken.ts` and `engagement-broken.tsx` **only inside comments**, both of which say the field is deliberately never read; `BrokenDefect` has no such property. One `grep -oE "defect\.[a-zA-Z]+"` matched the prose and it was reported as behaviour. **The true state: none of the six answer screens decrypted anything.** `load.ts` decrypted exactly one column — `contract_milestone.amount`, so Committed can total it. There was no "one rich screen and three bare ones"; there were six bare ones, and a product that had never shown its operator a sentence it had stored. **What the error cost and what it did not.** It did not change the ruling — Blocked, Next and Bottleneck were still gaps against FR-52/FR-53/FR-56 rather than new scope, because those requirements ask what a work item *is*. It changed the **size**: the fix was the first decrypt edge on those screens rather than the reuse of a proven one, so §7a had to be read to confirm it was permitted (`work_item` and `blocker` both say "operator, agents, decrypted server-side") instead of inferred from a screen that was not doing it. **This is the project's signature defect family turned on the record this file keeps** — a plausible mechanism, a true observation beside it, and a false conclusion joining them — and it recurred *inside the correction of itself*, which is the part worth keeping. A rule is now in the repo `CLAUDE.md`: grep the code form and confirm at the type or the return site, because comments here describe what the code does NOT do at least as often as what it does. Struck rather than deleted in CR-003 §2 and in the session log |
| 2026-08-20 | **M2.7's gate was written before M2.7 was dispatched, and it is red** | Erik asked why the autonomous fleet was not building M1.10. The answer that mattered was not about execution mode: **run `b0952e` had 966 tests, typecheck, lint, build, a report gate and an independent QA pass, and shipped a critical defect where `POST /api/ingest/run` returned 500 on every request** — Mode-1 ingest completely dead, the whole suite green throughout. Every specialist report was true and none of it was the thing that mattered. Since then every real defect on this project has been found the same way: a person looking at populated output. C1, the payload validator, B27's unwired parser, both false B11 design claims, the false "Broken renders a decrypted description", and M1.10's `test_result` cascade. **The fleet is not the problem and hand-building is not the fix — the missing piece is a criterion a report can fail against**, and eight detail views dispatched without one produce eight claims only Erik can disprove, one screen at a time. So the gate is written first. **Two layers, because one cannot do the job.** `tests/m27-gate.test.ts` runs with no credential and asserts what is decidable from the repository — that FR-81's eight entities have routes, and that ONE module decides where a reference points, which is what makes FR-83's "never a link" a rule rather than eight good intentions. `e2e/m27-navigation.spec.ts` crawls every rendered reference on the six answer screens and asserts FR-80, FR-83, FR-82, FR-84, FR-85 and FR-55's hyperlink against real rows. **The gate declares a markup contract and says so** — `data-verify-unit="entity-ref"` carrying `kind`, `ref` and a `known` that has no third value and no default, because a reference whose resolution nobody determined is `unparsed` and an absent attribute would let a view that never checked render a link. **Three things it refuses to do:** it does not skip when it cannot see data, it FAILS and names whether the screen was gated or empty — B19 is the precedent, where the Tier-2 corpus assertions "never ran" and were reported as SKIPPED, and a skipped gate is the same colour as a passing one; it does not sit in `pnpm test` or `pnpm e2e`, because a permanently red suite gets deleted rather than fixed; and it does not typecheck-fail the repository, which it did on the first attempt — a static `import("@/lib/entity-routes")` of an unbuilt module broke `pnpm typecheck` for all other work, so it loads by absolute file URL instead. **One thing it got caught doing:** the first `vitest.gate.config.ts` used `mergeConfig`, which CONCATENATES `include` — so `pnpm gate:m27` ran the entire 1061-test suite and reported green with the gate excluded, a run indistinguishable from a pass. That is the exact failure the file exists to prevent, met on the first try and fixed by making the config standalone |
| 2026-08-20 | **FR-86 is gated structurally rather than left to a manual check** | The honest problem: FR-86 says detail views add no decryption surface an agent token can reach, and asserting that end to end needs an agent token, which is a credential the e2e harness deliberately does not hold. Rather than write a weak check or leave the requirement ungated, the rule is pushed into the same single seam as the routes: the entity-route model must export `OPERATOR_ONLY_KINDS`, and the gate asserts it agrees with `AGENT_FORBIDDEN_TABLES` — the module that already carries §7a's refusals, from FR-5's `contract_milestone` outward. That does not prove a route refuses an agent; it makes the §7a fact live beside the routes instead of being remembered in eight views, and it fails if the two copies drift. **The end-to-end half is still NOT VERIFIED and belongs in M2.7's own QA pass** |
| 2026-08-20 | **FR-61 was unbuildable for a reason no purge function could have fixed, and the fix is a dropped foreign key** | CR-002 §1 states the conflict — `test_result` is append-only, refused to every role by a trigger, and a hard delete cannot cascade into it. What that means concretely was only visible once someone looked at the cascade graph: `engagement → test_case (on delete cascade) → test_result (on delete cascade)`. **A purge naming only the tables CR-002 §2.1 lists would have been correct in its own text and would have raised `restrict_violation` on every call**, because the cascade reaches what the SQL never mentions. Three candidate fixes and why two of them fail: making the reference `on delete set null` is an **UPDATE**, which the same trigger refuses just as flatly; keeping `test_case` is impossible because `test_case.engagement_id` is `not null` and cascades from the engagement. So the foreign key is **dropped**, leaving `test_result.test_case_id` a recorded identifier that may name a row which no longer exists — which is not a workaround but the literal text of CR-002 §2.4, "not enforced as cascading references … the intended state, not a defect". **The durable part is the test, not the fix.** `tests/schema-analysis.ts` walks the foreign keys out of the migration SQL, applies `drop constraint`, and reports every table a delete on `engagement` destroys; `tests/purge-boundary.test.ts` asserts that none of them refuses deletion, and asserts the full fifteen-table reachable set besides. It went red against the real migrations before the fix and green after, with six synthetic controls proving it can go red at all. A later migration cannot restore the cascade quietly |
| 2026-08-20 | **The export decrypts, and the reasoning is the part worth keeping** | FR-60 says "every record in one machine-readable document" and §7a says who may read each column; **neither says what an export contains**, and the two available answers are not symmetrical. Ciphertext is safer in the file and useless in the situation the file exists for — §8's "an export path is what keeps it from becoming a single point of failure", where the Supabase project is gone and the Vault key went with it. Decrypted is a full copy of every client's prose and every contract amount in one file. **Chosen: decrypted**, with three things attached so the choice is not silent. The document declares `"encryption": "decrypted"` in its own header, so whoever finds the file knows what they are holding. The screen says it in prose above the download button, because a JSON header does not reach a person. And it is raised as **B30** for Erik, because it is a judgment about relative harm made by the builder rather than by the person who bears it — the same class as B6. **`agent_token.token_hash` is withheld** and the document carries an `omitted` array saying so with a reason: §7a's "never returned by any read endpoint", and a bcrypt hash in a file is an offline cracking target that costs the export nothing. Omitting it silently would have been the worse half; an export that drops something without saying so is the same failure as a count reading zero because nothing looked |
| 2026-08-20 | **Two functions were made SECURITY INVOKER against this schema's own convention, and the convention is still right** | Every other `app.*` helper here is `security definer`, and correctly: their job is to lend a privilege the caller must not hold — `decrypt_field` is a decryption oracle, and granting it directly is the failure `…_rate_limit_and_rpcs.sql` warns about at length. `export_document` and `purge_engagement` lend nothing. They read and destroy the caller's own tables, and `service_role` — the only grantee — already holds BYPASSRLS, so definer rights buy nothing and cost the blast radius of a mistake: one `grant execute … to authenticated` on a definer export is a full-database read for any signed-in session, RLS and all. As invoker functions they can never return more than the caller could already select. **Verified rather than argued:** `has_function_privilege` reads `service_role` true and `authenticated`/`anon` false on all four, and `service_role` can delete `engagement` while it cannot delete `audit_log` or `test_result` — CR-002 §2.2 holding at the grant and not only in the function's text. **The vantage is recorded because it is the trap C1 came from:** those reads ran as `postgres` through the SQL console, which is the role that "proved" idempotency while the product was dead. `has_function_privilege` asks about `service_role` explicitly rather than relying on the querying role, which is exactly why it is the form used |
| 2026-08-20 | **The purge refuses an unarchived engagement — a rule the spec does not state** | FR-61 says hard deletion is "a separate administrative action, audited, and **never the default**". A typed-slug confirmation in a dialog satisfies that in the interface and nowhere else, and a server action is a public HTTP endpoint; this project has already had to strengthen exactly this class of control once, when MFA was found living in the application layer alone and a review called it critical. So `app.purge_engagement()` returns `not_archived` and destroys nothing unless `archived_at` is set. Archiving is one click and reversible, so the cost is one deliberate act and the gain is that destruction takes two. **This extends FR-61 rather than implementing it, which is why it is here for Erik to overrule rather than only in the migration's comments.** A second decision inside it: `archived_at` is the single source of truth and `engagement.status` is left alone, because writing both would manufacture two fields that can disagree — and this project's rule about recording disagreements is for artifacts written by different authors, not a licence to create one inside a table |
| 2026-08-20 | **What the audit log records about a deletion, and what it cannot** | CR-002 §2.3 requires the purge to record "what was deleted, by whom, and when". `audit_log` has columns for actor, action, target table, target id and timestamp — and none for a count, by FR-59's design and CR-002 §3's "no new columns". So the purge writes **one row for the engagement and one per table it destroyed rows in**, which makes the blast radius survive the rows, and returns the counts to the caller instead. **The log answers "which tables were emptied", not "how many rows".** Stated here rather than left for someone to discover from an audit trail that turned out to say less than they assumed. The rows are written inside the same transaction as the delete, which is the only arrangement in which the record and the deletion cannot come apart |
| 2026-08-20 | **A QA report records its own remediation, and ingest read none of it — found by wiring a parser that had never met a real artifact** | `src/lib/ingest/defects.ts` was built by `i3` with its own tests and a fixture, and nothing ever called it. Wiring it (B27) put its output in front of a human for the first time: run `b0952e`'s report produced **15 defects, every one `open`**, three titled `[CLOSED at c65e44d]` and one titled `[WITHDRAWN — my attribution was wrong]`. **`/broken` would have reported two critical defects open while the report says the one critical is fixed and re-verified** — a confident wrong answer, in the flagship answer of a product built to refuse them. **The cause is a shape no fixture had:** a QA report is written once and then EDITED IN PLACE as findings are fixed, so it carries a bracketed status marker as a separate leading bold span (`**[CLOSED at abc]** **The real title**`), the same marker folded into the title's span, and a struck-through closure record repeating the finding below it. `titleOf` took the first bold span — the marker — and dropped the title; `status` was hardcoded `open`. **Erik's ruling:** map the markers, retract the withdrawn. `CLOSED`/`FIXED` → `fixed`, `NEW` → `open`, **an unrecognised marker → `status: unparsed`** rather than a guess, because the marker vocabulary is not closed and widening it is the same failure as widening a severity regex. A `WITHDRAWN` finding and a struck-through closure record are **not ingested** — storing a retraction as an open defect asserts a claim the author took back, and the closure record would report the same critical twice — and both are counted into `notPersisted` so the drop is visible. **The subtlety that nearly reintroduced the bug:** the closure record carries the STATUS while the full entry carries the DETAIL, so dropping it naively lost the closure and reported the fixed critical as open. The status is now applied to the finding it repeats, matched narrowly on same-heading-plus-title-prefix, and an unmatched struck entry stays a defect rather than vanishing. **`source_key` takes its ordinal from the parser, not the array position** — retracting a finding shortens the array, and a positional key would silently rewrite D-9 with D-10's content on the next post. Result against the real report: **15 all-`open` → 13 defects, 2 retracted, 0 unparsed**, the critical reading `fixed`. Fixture added carrying the corpus shapes, copied from the report rather than invented. Seven tests, all red first. Two stale rows from the first ingest (`D-1` the closure record, `D-4` the withdrawal) deleted by hand after the re-ingest, leaving permanent gaps in the `D-nn` sequence — refs are allocated, not dense, and renumbering would change the identity of defects a test title can reference under FR-64 |
| 2026-08-20 | **B11's greyscale claim was false in both themes, and is now enforced by a test rather than asserted in a comment** | Spec §5a's recorded direction states that hue carries the state family and fill/outline/dashed carries confidence, **"so FR-43's four evidence scopes stay distinguishable in greyscale and to a colour-blind reader"**, and that violet appears nowhere in the state scale so `unparsed` can never read as an affordance. Both halves were measured on the first populated screens and both were false. `observed-live` and `observed-elsewhere` are two of FR-43's four scopes, are **both solid**, and sat **2.6 apart** in light and **2.0 in dark** out of 255 — one hue difference that deuteranopia flattens. `--state-unparsed` and `--primary` sat **0.3 apart in light and 0.9 in dark**: desaturated, the `unparsed` chip and the primary button are the same value. **Four tokens changed** — light `verified` `#047857`→`#046c4e`, light `fixed` `#0e7490`→`#0f7c9a`, light `observed-elsewhere` `#0f766e`→`#0c615a`, dark `observed-elsewhere` `#2dd4bf`→`#27c0ad` — each holding its hue and clearing ≥14/255 while staying above AA. **`unparsed` was fixed with treatment, not colour, and the reason is the useful part:** inside the work-item scale it must also clear `verified`, `carried` and `blocked`, which is four constraints inside a band the contrast floor already narrows — an automated solve produced a value that collided with `verified` instead, caught by verification rather than by reading. It now renders **hatched**, a texture no other chip and no button carries, so it is separated by something that is not a colour at all and survives desaturation outright. **Cost recorded honestly:** two tokens no longer match a sibling that shared their hex (`verified` diverges from `observed-live`, and nothing now shares light `fixed`), which Erik chose over turning amber into brown. `tests/state-scale.test.ts` asserts the whole claim, reads the treatment map **from the component** so a copy cannot drift, and holds an exactly-asserted exception list that is now empty — a new collision cannot be waved through silently, and a fix that obsoletes an entry fails until the entry is removed. Four negative controls each go red, and the hatch was verified **in the built CSS** rather than assumed, after a first grep against a path that does not exist returned a clean-looking zero |
| 2026-08-20 | **`testFiles[].path` now takes a repo-relative path; `manifests` and `questionFiles` keep the bare-filename rule** | The ingest payload validator banned `/` in every file-list name, and `harnessFor` classifies a test case by splitting its path on `/` and looking for an `e2e` or `playwright` **segment**. The two rules contradicted each other: **`harness: "playwright"` was unreachable through the endpoint**, and 10 of this repo's 34 test files would have been recorded as unit tests. The validator's own comment justified the ban with "a name is used to derive a run id and an `open_question.source_key`" — true of manifests and question files, and **not** true of a test path, which is stored as data in `test_result.file`. So the rule was split rather than widened: `bare` for the two key-deriving fields, `relativePath` for `testFiles`, which still rejects a leading `/`, any backslash, NUL, and any segment that is empty, `.` or `..`. **FR-23 is untouched** — the payload carries contents, and nothing on this path opens anything. The alternative considered and rejected was collapsing to basenames in `scripts/ingest-own-run.sh`, which clears the `400` and writes ten confidently wrong rows — the trade §9 exists to refuse. Erik ruled on it before it was built. **Observed, not inferred:** `test_case` now holds 57 Playwright cases across 9 files beside 265 vitest cases across 22. Six tests added, all proven red against the unfixed validator first, including one asserting the *joint* invariant across both modules and one control keeping `manifests`/`questionFiles` strict. Suite 971 → **977 passed / 6 skipped**, typecheck 0, lint 0 |
| 2026-08-20 | **A revoked agent token's plaintext entered the session transcript, and it is recorded rather than quietly dropped** | While debugging a `401`, a token was pasted into the terminal as a visible `export` and reached the transcript. It was the `delivery-ledger` token, **already revoked** 4 minutes earlier, so it grants nothing and no rotation was needed; the token actually used (`own-run ingest`) never appeared. Recorded because this repo's rule is that agent tokens are secrets and do not appear in transcripts, and an exposure that turned out harmless is still the event the rule is about. **The cause was an instruction defect, not operator error:** the procedure told Erik to copy the token to the clipboard and *then* handed him a command block to copy, which overwrote it — so the next paste put the instruction text into a `read -rs` prompt, indistinguishable from success because it echoes nothing. Captured as a rule in the repo `CLAUDE.md`: every command the user must paste goes on screen **before** the copy step, so the secret is the last thing copied |
| 2026-08-19 | **CRITICAL, found and fixed within the run: a CHECK constraint evaluates in the *caller's* role, so `revoke … from public` killed Mode-1 ingest outright** | `app.gates_are_closed_set(jsonb)` was revoked from `public, anon, authenticated` with no grant back, and it sits inside `check (app.gates_are_closed_set(gates))` on `fleet_run` — the first table `persistPlan` writes. **`POST /api/ingest/run` returned `500` on every request and FR-14–FR-23 stored nothing**, while `pnpm test` stayed green at 919 passing. The mechanism is the part worth keeping: the revoke does not merely withhold, it *removes* the EXECUTE `service_role` inherited through `PUBLIC`, and **`service_role` is not a member of `authenticated`**, so naming `anon, authenticated` in the revoke list creates a false impression that the application's role was spared. This extends the project's existing per-name-`REVOKE` rule to the revoke side, which is the side that bit; the rule is now written into the repo `CLAUDE.md`. Fix was one `grant`, in a new migration, proven over HTTP with a different-run-id negative control. A privilege matrix across all 20 functions in `app`+`public` confirmed **no second instance**. Run `b0952e`, found by `qa-reviewer`, fixed by `i10` |
| 2026-08-19 | **The method failure underneath C1 matters more than C1: a write path must be verified in the role the write path uses** | Idempotency had been "proved" through the Supabase MCP `execute_sql`, which runs as `postgres` — a role that **owns** the function and can execute it — while the product runs as `service_role`, which could not. The SQL-layer claim was true and the product was dead. The console proof even looked *more* rigorous than an HTTP probe: it inspected rows directly, covered eight tables, and carried a negative control. **`qa-reviewer` then caught the identical trap in itself** — its first idempotency probe read "counts identical, therefore idempotent" while both posts were failing at zero rows, and only a different-run-id control exposed it. Standing rule for this project: the last hop of any verification is the hop the product takes, and an unqualified "verified in SQL" should be read as "verified as the owner" |
| 2026-08-19 | **The three ingest endpoints and the wait routes now refuse unrecognised request-body keys with a `400`, naming the field the caller probably meant** | The unparsed discipline was enforced in the data and not at the API boundary: `deployedAt` for `deployed_at` returned `201` and stored a null deploy date, and on `/api/waits`, `expected_by` for `expectedBy` returned `201` and stored a wait with **no expected-by date** — from which FR-34 computes the overdue flag, so the ledger would hold a wait that can never go overdue. That is a wrong `done` in the blocked dimension. A *server-owned* key is deliberately not treated as unknown (`source` on the release endpoint is accepted and reported in `ignored_fields`), because refusing it would break a caller that sends it harmlessly today. **Recorded as a compatibility change (B25), not as a pure fix.** Run `b0952e`, units `i10` / `qa-reviewer` |
| 2026-08-19 | **One unit owns the definition of the FR-58 `unparsed` population, rather than each endpoint choosing** | FR-58 requires every screen and endpoint to report "the current `unparsed` count" and never says *of what*. Three units picked independently, and had that stood, **the six endpoints would report different numbers for the same state** — worse than reporting none, because a number that disagrees with the number on the next screen destroys trust in both. `i7` owns it: `work_item` + `defect` (severity **or** status, counted once per row) + `test_result`, **global and never narrowed by a caller filter** (a filtered count can read `0` while the ledger holds unclassified rows, and the badge sits in shell chrome where no filter exists), and **`null` rather than a partial sum** on any component failure. `i8`'s local copy was deleted and its call site repointed. QA observed `unparsed=4` on every endpoint against a seeded ledger, then `=2` on a re-seed |
| 2026-08-19 | **The end-user manual was not written, and §7b is NOT waived** | §7b requires `docs/user-guide.md` and names its reader. It was not written for two independent reasons: `qa-reviewer` returned **ISSUES**, not PASS, and a manual is not written for a build that has not passed review; and separately, **there is no operator account and no agent may create one** (B20), so there is no signed-in populated state for a guide to describe — `docs-writer` would have returned `BLOCKED`. **No agent wrote a waiver and none should**: an agent deciding a product has no end user reads exactly like a client agreeing it has none. Recorded here so the gap is visible rather than discovered at handover |
| 2026-08-19 | **CORRECTION — "grep silently skipped four source files" is withdrawn as unreproduced; only the NUL bytes were real** | Four TypeScript sources carried raw `U+0000` bytes (a `\0` spelled as a literal byte rather than an escape) and compiled, type-checked and tested clean; that is genuine damage, it is fixed, and `tests/source-hygiene.test.ts` now defends it. The accompanying causal claim — that this made `grep` skip the files, and that this is why a search missed `payload.ts` — **does not hold**: `git grep -l "unknownKeyProblems" c1f4f0b` lists `payload.ts`. The real cause was the query, which searched for the module *path* (`unknown-keys`) where the file imports the *symbol* from the `@/lib/api` barrel. Kept rather than deleted because it is the project's signature defect family aimed at itself: a compelling mechanism, a true observation beside it, and a false conclusion joining them — and it was one step from being routed to the vault as a measured fact |
| 2026-08-19 | **Next 16.3.1 and TypeScript 7.0.2 are pinned together and are not independently choosable** | The spec's §9 stack constraint is live, not historical: `typescript@7.0.2` is current on the registry and `"baseUrl": "."` is a hard `TS5102`, not a deprecation. Next 15.5 builds its path aliases *through* `baseUrl`, so that pair does not resolve at all; `paths` alone works on Next 16. A second TS7 break was pre-baked rather than discovered: `types: ["node"]` must be explicit or `node:*` imports fail as ordinary module-resolution errors. Run `b0952e`, units `r1`/`u1` |
| 2026-08-19 | **FR-8's per-token rate limiting is a Postgres counter RPC, because the platform cannot do it** | Vercel's WAF on the **Pro** plan counts only by IP or JA4 Digest; keying on an arbitrary header such as `Authorization` is an **Enterprise** feature, and an in-process limiter does not survive Vercel's ephemeral instances. The database counter is therefore forced rather than chosen. The alternative would have satisfied the words "rate limited" while not being per-token, which is what FR-8 actually requires. Fixed-window, boundary-burst weakness stated in the migration, prune function deliberately unscheduled per FR-62. Run `b0952e`, units `r1`/`i1` |
| 2026-08-19 | **`relrowsecurity` measured worthless on this project — the 2026-08-17 warning confirmed by observation rather than inherited** | A throwaway table created and otherwise untouched came out `relrowsecurity = true` with **zero policies**, set by `rls_auto_enable`. A deliberately-open table returned **HTTP 200 with real rows** to the publishable key while its flag was *also* true. Every RLS assertion in this build therefore reads `pg_policies`: 21/21 tables, 23 policies, zero policies bypassing the `app.is_operator()` chokepoint. Run `b0952e`, unit `i1` |
| 2026-08-19 | **Crypto helpers live in an unexposed `app` schema behind six `public` wrappers granted to `service_role` alone — so a decrypted read bypasses RLS** | `POST /rest/v1/rpc/` with `Content-Profile: app` returns `406 PGRST106 Only the following schemas are exposed`, so the schema boundary is the first layer and the per-name `REVOKE` the second rather than the only one. **Live hazard for M1.2/M1.3:** the failure mode is a later unit quietly routing every read through `service_role` until RLS is decorative. If session-scoped decrypted reads are wanted the shape is a `security_invoker = on` view per table; deliberately not built, because it widens the decryption-oracle surface. Run `b0952e`, unit `i1` |
| 2026-08-19 | **No MFA-enrolment carve-out in RLS, decided deliberately rather than discovered in production** | A blanket `aal2` requirement can lock a never-enrolled operator out of the enrolment screen itself — unrecoverable on a fresh install and invisible to every unit test, because it only manifests for a user with zero factors. Enrolment runs through GoTrue (`/auth/v1/factors`), which touches no RLS-gated table in `public`, so no carve-out is needed. **Consequence the UI must honour:** it has to tolerate the `operator` row being unreadable at `aal1` rather than reading that as "no account". `auth.users` is still 0 rows, so this path is live. Run `b0952e`, unit `i1` |
| 2026-08-19 | **Requirement references are stored as text, never as foreign keys** | FR-12 and FR-65 require a reference naming something that does not exist to be **reported**, not silently accepted. A foreign key would reject the row and destroy the finding — the same class of mistake as widening a regex to make a stubborn row classify. Run `b0952e`, unit `i1` |
| 2026-08-19 | **`plan.md`'s own coverage suite could not detect the bug this practice's vault note documents, and a control test was added beyond the plan** | The note describes a prior near-miss where a *widened* certifier join — comparing `certified_by` against every executor in the engagement rather than against the executor of the work implementing that requirement — makes every milestone permanently un-billable, because on a real run `qa-reviewer` is itself a work-unit executor. `plan.md`'s `coverage.ts` has the correct join, but its fixture holds **one** work item, so the widened form was written and **all seven of the plan's assertions still passed**. One control test now fails under it. The project's signature defect family found inside its own brief. Run `b0952e`, unit `i2` |
| 2026-08-19 | **The visual design was chosen from §5a's written direction, because §5a reads `Approved design: NOT YET APPROVED`** | No mockup exists and none was invented. Geist / Geist Mono; violet accent (`#6d28d9` / `#a78bfa`) confined to focus rings, active nav and primary actions; a **separate** 16-token state scale carrying hue for family and fill/outline/dashed for confidence, so FR-43's four evidence scopes stay distinguishable in greyscale and to a colour-blind reader. Violet appears nowhere in the state scale and fuchsia nowhere in the chrome, so `unparsed` can never read as an affordance. Recorded as **B11** so it is confirmed before Phase 2 builds six screens on it. Run `b0952e`, unit `u1` |
| 2026-08-19 | **`pnpm lint` runs oxlint, not ESLint** | typescript-eslint throws at module-load time on TypeScript 7 and takes `eslint-config-next` down with it; no rule subset avoids it, because ESLint cannot parse a `.tsx` without a TypeScript parser. Leaving a hard-failing `pnpm lint` would poison every downstream unit's verification, and leaving a config that silently lints nothing would be worse — a gate that cannot fail has stopped being a gate. Recorded as **B9**. Run `b0952e`, unit `u1` |
| 2026-08-19 | **CORRECTION — B1b blocks nothing. The fleet's `devops` specialist does not use the Vercel MCP connector at all** | The blocker was recorded as gating `devops` on the reasoning that the specialist drives Vercel through that connector. It does not: `devops.md` has no `mcp__` reference anywhere and calls `vercel project ls`, `vercel env ls` and `vercel deploy` directly. The CLI vantage was already known-good in this file — it is the same 15-vs-1 disagreement recorded on 2026-08-17 — but the conclusion drawn from it named the wrong consumer. **Same defect family as the 404 correction above, one level up: there the tool was mistaken for the world; here a tool's limitation was mistaken for a constraint on an agent that never calls it.** The scope diagnosis was also sharpened — project-scoped, not stale — which changes the fix from 'reauthorize' to 'reauthorize and grant All Projects' |
| 2026-08-18 | **CR-001 approved: defects, regressions, releases, a sixth answer (Broken), provisioning identifiers.** FR-63–FR-79, entities 18 → 21. `security-gate.sh` PASS on the merged set | An audit against Erik's studio checklist found bug reports and shipped-state untracked and regressions derivable but never derived. Approved before M1.1 so the schema is built once. `spec-approved.md` stays byte-identical; the CR amends by precedence |
| 2026-08-18 | **Q8 answered `contested` (FR-79):** a billable milestone with an open critical defect naming an acceptance requirement is flagged, never blocked and never presented as clean | Blocking billable on an asserted severity would be an asserted invoice state by another name; hiding the defect would present a disputed milestone as clean. Contested preserves derived-never-asserted in both directions |
| 2026-08-17 | **CORRECTION — the Vercel project exists; an earlier entry in this file said it did not** | Reported absent on the strength of a 404 from `get_project` plus an omission from `list_projects`. Both readings were accurate about the API and wrong about the world. HTTP settled it: the project's hostname answers 302 into Vercel's SSO with a per-request nonce, exactly as the known-good `danishjawaid` does, while invented names under the same wildcard answer 404 with no redirect. **The check had no negative control, so nothing established that it could distinguish absent from invisible.** Kept rather than deleted: this is the project's signature defect family pointed at its own tooling |
| 2026-08-17 | **Provisioning target confirmed by observation, not by the URL Erik pasted.** Supabase `onpvolboecjpdkvurjaf` in org `whneklkrjsulgqzqxsks`. Vercel team `team_J6J1LAU19znwJenYFKgVArEV`, slug `erik-capital-ready-advisors-projects` | The account signal was read in the same place it read dirty on run `cd414c`, and it now reads clean: `list_organizations` returns **exactly one** org and it is Erik's; `list_projects` returns exactly two projects, `project-tracker` and `danishjawaid`, both his. On `cd414c` the same call returned six strangers' products. Checking the URL against the API is what separates "Erik says it exists" from "it exists" - and on the Vercel half those two answers disagreed |
| 2026-08-17 | **Inventoried the database BEFORE anything was applied, and it is empty** | Zero `public` tables, `list_migrations` returns `[]`, `auth.users` 0 rows. The only non-zero counts are Supabase's own bookkeeping (`auth.schema_migrations` 77, `storage.migrations` 62), which every fresh project carries. Replay is therefore safe, and this is recorded now because the same inventory was the thing that made `cd414c`'s replay safe - and skipping it is what made the first attempt unsafe |
| 2026-08-17 | **`public.rls_auto_enable()` already exists on the project, and it changes the verification instrument** | An event-trigger function, `SECURITY DEFINER`, `SET search_path TO 'pg_catalog'`, that enables RLS on every new table created in `public`. Nobody in this build wrote it. Two consequences. **(1)** It answers the Danish run's open question about forcing RLS by event trigger - here it is already forced, so that is not a decision left to make. **(2) It means "RLS is enabled on this table" is no longer evidence that the migration enabled it.** A test asserting RLS-on would pass even if a migration forgot, because the trigger did it. Any RLS check must assert the **policy**, not the flag. This is the project's signature defect family - a gate that passes in exactly the case it exists to catch |
| 2026-08-17 | **B13 is live on day zero, and the specific instance is harmless** | `rls_auto_enable`'s ACL reads `=X/postgres | postgres=X/postgres | anon=X/postgres | ...`. The leading `=X` is PUBLIC holding EXECUTE, which is exactly the pattern the reference build documented: `ALTER DEFAULT PRIVILEGES` does not close it and every new `public` function needs a per-name `REVOKE`. **This particular function is not exploitable** - it returns `event_trigger`, so the `/rest/v1/rpc/` path the advisor names cannot actually invoke it. Recorded because the *grant pattern* is real and will apply to every function this build writes, not because this one is dangerous |
| 2026-08-17 | Next.js + Supabase + Vercel, not a local CLI | Erik requires a product his own fleet can build. The fleet's specialists cover this stack and no other. A first draft of this design proposed a zero-dependency Python CLI on the strength of running in a fresh worktree with no install; that argument was sound and irrelevant, because the fleet cannot build it |
| 2026-08-17 | Three execution modes converge on one `work_item` table | Separate tables produce three lists Erik merges in his head, which is the state the product exists to fix |
| 2026-08-17 | `unparsed` is the only default for every classifier | A wrong `done` tells Erik a client requirement is satisfied when nothing checked it. Stated in §9 as the load-bearing decision |
| 2026-08-17 | Agent tokens are refused `contract_milestone` entirely | An agent has no reason to read what Erik charges. Least privilege where it is cheap and absolute |
| 2026-08-17 | `engagement.client_name` left unencrypted, stated as an exception in §7a | It is the display, sort and slug key on every surface. The money it would otherwise protect sits on `contract_milestone` and is encrypted. Recorded as a reduction in exposure, not an elimination |
| 2026-08-17 | MFA enforced in RLS, not only in the Next.js layer | The reference build shipped it in the application layer only and a review pass classified that critical |

---

## Open threads

- **The dogfood loop.** This repository's own fleet run produces the first artifacts the product ingests. That makes M1.4 testable against real input on day one, and it makes the product's first honest report a report about itself.
- **Test-title tagging is a backfill Erik owns.** The reference engagement has 925 tests and 38 Playwright specs, none of which names a requirement. M1.8 will correctly report zero mapped coverage across 61 requirements until tags exist. That report is the deliverable, not a defect, and it should not be softened.
- **Writeback is the real fix for Mode 1.** Every hour spent hardening a prose parser is an hour Phase 2's M2.6 makes unnecessary. Build M1.4 to the shapes in the corpus and no further.

| 2026-08-19 | **CR-002 approved: append-only is absolute and FR-61's deletion stops at the audit boundary** | Erik approved CR-002 as drafted. `audit_log` and `test_result` retain their rows after the engagement they refer to is deleted, and no role including the table owner may delete or truncate them. Identifiers in those two tables may refer to rows that no longer exist, and **a reader must treat an unresolvable reference as expected rather than as a defect**. The rejected alternative was a privileged escape hatch that deletes audit rows under audit - it satisfies FR-61 literally and destroys what §7a is for, because the actor most likely to hold that role is the one whose actions the log exists to record. **This does not resolve B2**: if a client demands true erasure, the answer is a separate CR defining redaction of an audit row (overwrite the payload, keep the row and its timestamps), which is a different mechanism and must not be smuggled into M1.10 |
| 2026-08-19 | **`report-gate.sh` now evaluates `qa-reviewer` reports, and the patch was proven able to fail** | B23 closed. Five edits to `~/.claude/agents/scripts/report-gate.sh`: nine QA section constants; a `qa-reviewer` entry in `REQUIRED` carrying the nine sections `qa-reviewer.md` actually mandates; `EVIDENCE["qa-reviewer"] = "verification performed"` (distinct from the build specialists' `VERIF`, which `norm()` does not fold together); acceptance of **both** heading forms; and a per-specialist status vocabulary so QA's four-state `PASS / ISSUES / FAIL / BLOCKED` is accepted where the build specialists keep `DONE / BLOCKED / FAILED`. **`## Questions Queued` is deliberately NOT required of QA** - the template does not carry it, and requiring a section the definition never asks for would fail every by-the-book report. Verified: the real report now PASSES; all 15 other unit reports still pass and `i2` still fails for its pre-existing reason, so nothing was loosened; and four negative controls each go red - a removed mandated section, a `DONE` status, an unknown specialist, and an emptied evidence section. **Known divergence left standing:** `qa-reviewer.md`'s template mandates `# QA report <run-id>` while the gate documents `# <specialist> report - <unit-id>`. Both now pass. The one combination that fails is the template heading *plus* a voluntary `Questions queued` section, because that heading carries no unit id to check the questions against; the fix is to use the specialist form, which `qa-reviewer` already does. The agent definition was **not** edited - that is a second cross-project change and was not made silently |
| 2026-08-19 | **The ingest wire tightening is confirmed: unrecognised request-body keys stay a `400`** | B25 closed. Four endpoints - `/api/ingest/run`, `/api/ingest/session`, `/api/ingest/release`, and both `/api/waits` and `/api/waits/resolve` - refuse unknown body keys and name the field the caller probably meant, rather than accepting the request and dropping the value. Erik confirmed it as a compatibility change with the cost understood: any existing caller sending an extra field now gets a `400` where it previously got a `201`. **There are no external callers yet, which makes this the cheapest moment the decision will ever have.** The argument that settled it is also the measure of its risk - on its first day the refusal caught `qa-reviewer` posting `requirementRefs` where the field is `requirement_refs`, silently dropped, so that unit's own FR-74 coverage row had passed on evidence proving nothing. A *server-owned* key stays exempt (`source` on the release endpoint is accepted and reported in `ignored_fields`), because refusing it would break a caller that sends it harmlessly today. **Left open deliberately:** the casing mismatch between endpoints that caused the original loss is unresolved, and is `i10`'s first queued question |
| 2026-08-19 | **Three auth screens rendered blank in development, and the whole suite was green while they did** | Found by Erik signing in for the first time; two distinct defects in the same twelve lines. **(1) StrictMode.** `next.config.ts` sets `reactStrictMode: true`, so development mounts, unmounts and remounts before the first async continuation resolves. Cleanup set `cancelled = true`; `started` is a **ref**, so it survived the simulated remount and the second mount returned early; the first mount's response then hit `if (cancelled) return` before any `setState`. One server call, no state, an empty frame - and an unverified TOTP factor left in GoTrue. Confirmed by instrumented trace, not by reading. The flag guarded nothing (setting state after a real unmount is a no-op since React 18) and was removed; the ref guard, which is what actually prevents a duplicate factor, stays. **All three screens carried the identical pattern** (`mfa-enroll-form`, `mfa-verify-form`, `sign-in-form`) and all three were fixed - fixing only the one Erik hit would have left the bug in the two screens after it. **(2) Friendly-name collision.** The comment above `enroll()` asserted that omitting `friendlyName` avoids GoTrue's uniqueness check. It does not: GoTrue stores the absent name as `""` and refuses a second factor carrying `""`, so the first load left a factor behind and **every load after it failed**. `listFactors().totp` carries verified factors only and could not see the leftover; the fix reads `.all`, redirects when a **verified** factor exists, and unenrolls only **unverified** ones. **Why 966 tests missed both: every existing test mounts bare, and a bare mount cannot reproduce a double-invoke.** Five tests added, all proven red against the unfixed components, including a control that a verified factor is never unenrolled. Commits `4c06dde`, `c0f1f7e` |
| 2026-08-19 | **Nothing behind authentication had ever been exercised, and that is the coverage gap to carry** | `qa-reviewer` had live credentials and tested the API layer hard - it is what found the run's critical defect - but the enrolment flow terminates at an authenticator app, so **no agent in this run ever completed a sign-in**. Two defects sat in the first twelve lines a human touched. The general form: agent verification reaches every surface a token can reach and stops exactly where a human factor begins, so **everything gated behind `aal2` should be treated as unexercised until a person has walked it**, not as covered because the suite is green. Both defects were of the shape this project already names - a mitigation reasoned about rather than measured, the same failure as `has_function_privilege` being read instead of exercised |

---

## Session log

| Date | Session | What happened |
|---|---|---|
| 2026-08-17 | Intake | Read the reference corpus — both manifests, the checkpoint, `prod.md`, the four gate scripts, the questions files. Drafted `spec-v1.md`: 62 FRs, 18 entities, 11 Phase 1 milestones, 7 open questions. `security-gate.sh` PASS. No code written |
| 2026-08-18 | Audit + CR-001 draft | Audited spec, repo CLAUDE.md and `plan.md` against Erik's studio checklist. Gaps found: no defect entity, regressions derivable but never derived, built/shipped collapsed, no provisioning identifiers on `engagement`. Drafted CR-001 (FR-63–FR-78, 3 new entities, §7a rows, Q8) — **pending approval, timed before M1.1 so the schema is built once**. Phase-2 gaps (CR ingest, notifications, cost, recurring obligations) routed to §4.3 in the CR, not spec'd. No code written |
| 2026-08-24 | **Q12–Q15 ALL RULED. CR-005 §3.1 APPROVED — planned work is M2.9, and the Q13 ruling cut its scope before a line was written** | Erik ruled all four in one sitting after asking whether the fleet could simply build what was left of CR-005. **It could not, and both reasons were measured rather than asserted.** (1) §3.1/§3.3 were DRAFT — `project-lead` decomposes `spec-approved.md` plus approved CRs, so there was literally nothing to decompose. (2) **`fleet-preflight.sh` FAILS from this session**: it derives launch cwd from `$CLAUDE_CODE_SESSION_ID`, which no `cd` can move, and this session launched in the Google Drive folder — every `isolation: "worktree"` dispatch would have died at worktree creation. 7 other checks ok. **The rulings:** Q12 → the `writing-plans` shape only; Q13 → explicit id, **no merge without one, collision marked**; Q14 → **engagement REQUIRED at creation** (Erik overrode the recommendation to allow unassigned — right call: `work-items/unassigned` holds ingested work whose owner the parser could not determine, the opposite case from work that has not happened yet); Q15 → **stale at 30 days, surfaced, never deleted**, derived from a timestamp and never stored as a status, so CR-002's append-only ruling stands. **Q13 was verified against the artifacts before it was written down, and the verification changed the milestone:** `plan.md` heads its tasks `### Task 5:` — positional, shifting the moment a task is inserted — and `manifest-9a320b.md` carries no plan-id field, so **plan and manifest share zero keys**, and both templates live in `~/.claude/agents/templates`, outside this repo and unreachable from a worktree. **FR-90 therefore splits: M2.9 builds the read side only**, marking every collision and merging nothing — which is precisely what Q13 requires and drops the false-`done` rate to **zero by construction rather than by care**. Until id-emission lands as separate template work, FR-90 is *"holds, merges nothing"*: **a stated limitation, not an unmet clause**, and the distinction is the point. **Written down because it will be tempting later:** when the first real plan yields all-collisions-and-no-merges, the pressure will be to fall back on title similarity. That is the wrong `done` CR-005 exists to prevent, and it breaks `Next` and `Committed` at once. **FR-96 dropped from M2.9** — §3.3 was never approved and bundling unapproved text into an approved milestone is how a fleet builds spec nobody ruled on. §2's bar still unruled, though §3.1 as ruled satisfies it. **Zero code this session; spec only** |
| 2026-08-24 | **The three `9a320b` worktrees PRUNED — 2.0 GB reclaimed, and the hold condition was discharged rather than forgotten** | The 2026-08-23 entry below kept them **deliberately unpruned** "until someone has credits to verify them properly." That condition is now satisfied: M2.8 is COMPLETE and both units were rescued and merged. **Verified before deleting, not after.** `444cb33` (two worktrees) is an ancestor of HEAD, so it strands nothing; `bc46b8e` is **not** an ancestor, but `git diff 444cb33 bc46b8e` is empty, so it duplicates merged content — which is exactly what the 08-23 entry recorded, re-checked rather than trusted. Two worktrees still held **uncommitted** files (`src/app/runs/`, the u1/u2 tests, `src/lib/nav.ts`, the questions files and specialist reports) — the very things "rescued from uncommitted worktrees" — so each was compared against HEAD before anything was removed: every spot-check **SAME**, and a full `diff -r` of both `src/app/runs` trees came back **completely empty**. Only then `git worktree remove --force`. **2040 MB → 1 MB.** The repo now has one worktree, the main one. **Written down because the interesting part is the shape:** a hold placed for a good reason outlives the reason, and a 2 GB directory nobody dares touch is the normal outcome. The fix was to re-run the check the hold was waiting on |
| 2026-08-24 | **CR-005 §3.3 APPROVED and §2's bar ADOPTED — Q17–Q20 ruled, FR-96 rejoins M2.9, and CR-005 now has no open questions at all** | Erik asked what would be needed to pull the two leftovers into the build rather than leave them behind, which is the right question — the alternative was a second CR later for a filter that is mostly already plumbed. **Four questions raised and ruled the same day, none of which existed at drafting**, because §3.3 was written as though "an optional engagement filter" settled itself and it does not. **Q17 was the one that mattered, and it was found by reading the code rather than the CR:** `app-shell.tsx:20` mounts the FR-58 unparsed count in the chrome *deliberately* — "mounting it in the shell is what makes that structural instead of a rule six screens have to remember" — so adding a filter would have hung a **ledger-wide number above a scoped list, silently, on all eleven screens at once.** That is precisely the disagreement M2.8 already logged one layer down (global badge 0, run `b0952e` 1). **Ruled: the badge stays ledger-wide — keeping FR-58's alarm, which is what breaks if it goes filter-scoped, since heads-down in one client is exactly when unparsed rows elsewhere go unnoticed — and labels its scope when a filter is on** (FR-96a). Q18: URL parameter on every list screen **plus one picker in the app shell**, not eleven copies of the same widget — the same structural argument the badge already won (FR-96b). Q19: an unresolvable slug renders **an explicit "no such engagement" with no rows, never a silent fall-back to unfiltered** (FR-96c); a screen that looks scoped while showing everything is the same class of lie as a wrong `done`. Q20: **§2's bar adopted AS WRITTEN — guidance with force, not an enforceable gate**, Erik having declined the variant that fails a CR automatically; it costs nothing retroactively since §3.1 clears it under limb (b) and §3.3 under limb (a), and it now means §2's out-of-scope list is not a preference — each item has to clear the bar to come back. **Four things decided rather than asked, and written into the CR so they are not silent defaults:** `[id]` detail views take no filter (a detail view is already one record, and filtering it can only produce a page that hides itself); the picker lists active engagements only, but an **archived-not-purged slug still resolves**, because a permalink should not rot because an engagement was tidied away; **no cookie and no session stickiness** — the URL is the entire state, since a filter persisting invisibly would mean two operators on one URL see different data; and **served routes stay 30** (a query parameter is not a route) so `manual-gate.sh`'s count is unaffected, **but the guide still needs a filter section and it is budgeted into M2.9** rather than discovered at merge. **FR-96 therefore ends up back where CR-005 §5 put it at drafting — by approval, not by assumption**, and the detour is left on the record because those two are indistinguishable in a diff a month from now. **Zero code this session; spec only** |
| 2026-08-24 | **SHIPPED — PR #2 merged, first production deploy, and VERIFIED SERVING** | Erik merged at 02:27:42Z (`aeb6d91`); Vercel raised a Production deployment **4 seconds later**, Ready in 21s. **Verified rather than assumed, and from two vantages each time:** the merge via both `gh pr view` (`MERGED`) and `git merge-base --is-ancestor`; the deploy via `vercel curl` (never a plain `curl` — deployment protection answers first with its own interstitial) returning **200 on `/`, `/blocked` and `/runs`**, so M2.8's route is live. **The §7a boundary was checked in production with controls, not asserted:** anonymous `/blocked` renders "Sign in to read this screen", the page identifies itself as Delivery Ledger so the probe was not blind, and four strings known to be in the real ledger return **zero**. Pre-flight before anything left the machine: `pnpm build` **compiled, 43 routes**, and a secret scan over the 43,723-line pushed diff **zero on every shape with controls firing at 133 and 523** — because a scan that returns zero on everything is indistinguishable from a broken one. **One correction owed and made:** the PR body first claimed **119 commits**; the real figure is **57**. 119 was measured against the *stale local* `master` (`c835cf9`), which predates PR #1 — the same class of error as the `git diff HEAD <branch>` trap already in `CLAUDE.md`, and GitHub's own count is what caught it. **Not merged by an agent:** the merge was blocked by the permission classifier as irreversible and outward-facing, reported rather than routed around, and Erik clicked it |
| 2026-08-24 | **M2.8 finished by hand and is COMPLETE. B47 found, reproduced and fixed on the way** | Three gates closed. (1) `manual-gate.sh` **FAIL → PASS 30/30**: two guide sections plus two evidence rows, hand-edited rather than re-dispatching `docs-writer`. (2) **Both new screens observed for the first time** — the units could not, from isolated worktrees, and correctly refused to copy a credential. The saved `aal2` session was round-tripped and found alive; **the first control was wrong and reported the opposite**, because `h1` reads `"Blocked"` signed-in and signed-out alike (the gate renders inside the `<Screen>`), so it was re-keyed on the sign-in notice with an abort if the signed-out case fails to show it. (3) **FR-92/FR-93's three unmet clauses RATIFIED AS-BUILT by Erik** and written down as unmet in the Decisions log. **Then the FR-93 verification turned up a live defect:** `defect.source_key` was not run-scoped, so a second run's QA report would have overwritten the first run's 13 defect rows. **The first writeup handed Erik a structural argument and a three-option menu; he asked what Karpathy would do, and the answer was to write the failing test instead** — RED with three colliding keys, which settled it with no menu. Fixed, migration `20260824015328` applied and read back (13/13 rekeyed, 0 stale), and **a third test caught passing vacuously** and given a non-empty guard. `pnpm test` **1544 → 1545 / 6 skipped**, typecheck 0, lint 0, `gate:m27` 5/5. Lesson recorded in `CLAUDE.md`: reproduce before escalating |
| 2026-08-23 | **M2.8 dispatched as run `9a320b`, then STOPPED mid-Wave-B on Erik's usage credits (15% left)** | Stopped deliberately rather than risking the worst failure shape: running dry mid-run leaves a half-built branch, no report, no writeback. **Work preserved, nothing discarded.** Branch `agent-build/2026-08-23-9a320b` carries 2 commits — `e3dff86` (resolved spec + manifest) and **`444cb33`, i1's `fleet_run` read layer for FR-92–FR-95**. Three worktrees remain, **1.9 GB, deliberately NOT pruned** until someone has credits to verify them properly; the one holding `bc46b8e` was checked and `git diff 444cb33 bc46b8e` is **empty**, so its commit duplicates merged content and strands nothing. **i1 raised four questions and two of them are defects in the APPROVED requirement text, not implementation choices:** (a) **FR-93 says `/runs/[id]` shows "the defects it opened" and no such link exists** — `defect` has no run column and its only path, `fixing_work_item_id → work_item → fleet_run`, is NULL on all 13 rows; (b) **`fleet_run`'s constraint is `unique (engagement_id, run_id)`, so a run id is unique WITHIN an engagement and not across the ledger**, while FR-92 makes `/runs` cross-engagement — so `/runs/[run-id]` cannot address a run unambiguously. Also raised: `dispatch_cap`/`dispatches_used` are written by ingest and read by nothing else; and a run's own unparsed count disagrees with the global FR-58 badge (badge 0, run `b0952e` 1). **These need Erik's ruling before M2.8 resumes** — the second is the same shape as the `u4` resolution ambiguity: an id unique only within a tuple cannot key a global route |
| 2026-08-23 | **CR-005 §3.2 APPROVED — M2.8, fleet run history** | Erik approved the severable half the same day it was drafted. FR-92–FR-95: `/runs` lists every ingested run across every engagement with verdict, duration, dispatches-against-cap and the test triple; `/runs/[id]` shows one run's units, questions, defects and requirements with its `gates` payload rendered rather than dumped; both state their own unparsed count per FR-58; and **FR-95 keeps the standing disagreement rule** — where a manifest and a checkpoint disagree about a unit, both are shown, because that disagreement is data. **A correction to the drafting summary, recorded rather than dropped:** it claimed §3.2 needed no questions answered, and **Q16 belonged to §3.2**. It resolves by construction — FR-92 already says "across every engagement", so approving §3.2 approved that reading — but a question that dissolves on inspection and one that was never asked look identical a month later. **Unapproved and unchanged:** §3.1, §3.3, Q12–Q15, and §2's proposed bar, which approving §3.2 does not adopt |
| 2026-08-23 | **CR-005 DRAFTED — planned work, fleet run history, and a bar to replace "all-in-one"** | `spec/change-requests/CR-005-planned-work-and-run-history.md`, **NOT APPROVED**. Drafted after Erik asked whether the ledger tracks what a plan proposed versus what got built, then asked for multi-client, per-run visibility, and "everything I could ever need". **Two of the three asks are not gaps and the CR says so rather than re-specifying working code:** multi-engagement is **already built** (FR-44 puts work items across every engagement in one view; two engagements exist), and fleet run history is **already ingested and never rendered** — `fleet_run` carries verdict, dispatch cap and used, the test triple and a `gates` jsonb, holds 1 row, and **no route renders it**; the branch serves 28 routes and none is `/runs`. Only **planned work** is genuinely absent, and deliberately: `plan.md`/planning/roadmap/backlog return **zero** matches in the approved spec, there is **no `createWorkItem` action** among the app's nine, and §4.2 non-goal 4 excludes it outright. **The model is already most of the way there** — `work_status` has `pending` and `not_dispatched` — so what is missing is not the concept but any path by which a `pending` row is born. **That is the layer beneath B39**, and the two should not be conflated. The CR **refuses the all-in-one framing** and proposes a testable bar in its place: every new surface is either populated by an artifact nobody types, or typed once and carrying its own expiry — and the thirty-second bar must hold with N clients, not one. **No new entity, so the 21-entity §7a PASS is unaffected**; `/runs` would take served routes 28 → 30 and fail `manual-gate.sh` until the guide covers them, which is the gate working. FR-87–FR-96, milestones M2.8 and M2.9, **Q12–Q16 unanswered** |
| 2026-08-23 | **§7b's end-user manual is WRITTEN, and a new defect was found writing it — B46** | `docs/user-guide.md`, 5601 words, 31 sections, **all 28 served routes covered**; 25 opened and observed, 3 marked `UNVERIFIED` **in the guide body** (`/releases/[id]` has no rows; `/sign-in/verify` and `/sign-in/enroll` render only mid-flow). `manual-gate.sh` **re-run independently by the dispatching session rather than taken from the report — PASS, agreeing with `docs-writer`'s own verdict.** Checked beyond the gate, because no gate can: names no other client, no token or session value anywhere (scan run with a control term first, which hit 281, so it was not blind), and the limitations section is measured — it tells the reader **not to invoice from the Committed totals** while one milestone amount will not decrypt. `.docx` built with the `regen-manual.sh` pattern and **verified rather than assumed**: valid zip, TOC field, 31 page breaks, no leaked `newpage`. **M1.10's last open requirement is now met.** Two process notes: `docs-writer` ran in the **shared checkout** — `git worktree list` showed no worktree at all — which is L3 recurring, though harmless here (`git status` confirms it wrote only three untracked paths and touched no product code); and it **archived the live `delivery-ledger` engagement** with a label-matched dialog sweep, noticed immediately, pressed Restore, and reported it unprompted. **Restoration verified independently against the database**, not the UI it had already checked: both engagements `active`, `archived_at` null, **0 archived**, and all six counts match the pre-archive reading (96/20/26/13/79/324) |
| 2026-08-23 | **Agent token minted and verified — §7c's SECOND boundary is closed, and both are reachable for the first time** | Erik minted a token carrying `ingest:write` and `answer:read` at `/settings/tokens` on **localhost**, and it reaches the same rows as the deployment: there is exactly one `project-tracker` Supabase project (`onpvolboecjpdkvurjaf`, `ACTIVE_HEALTHY`), so localhost, Preview and Production share one `agent_token` table and a token minted anywhere works everywhere. **Verified by authentication rather than by presence**, with the negative controls run beside it: `200` with the token against `/api/answer/blocked`, `401` with no token, `401` with a malformed one — so the probe discriminates and is not blind. Shape checked without printing the value (`dl_<uuid>_<64 hex>`, 104 chars). The procedure was **ordered so the terminal prompt was already waiting before the secret was copied**, per the standing clipboard rule, and the first draft of it was wrong in a way worth recording: a multi-line paste feeds its own next line to `read`, storing instruction text as the token with no visible sign because `-s` echoes nothing. The fix is a single-line command, tested against the no-trailing-newline case before being handed over. **Consequence: `docs-writer` can now sign in, so §7b's manual is buildable and M1.10 can reach Complete** |
| 2026-08-23 | **B41 CLOSED — Erik minted the operator session, and the two unobserved fixes were observed** | §7c's operator boundary is reachable again. `pnpm gate:m27:e2e` **8 passed / 2 failed** at branch tip `a2c3446` — the same 8/2 as 2026-08-20, and **the two failures are the same non-defect**: `/next` and `/bottleneck` throw `rendered its empty state` because those answers are genuinely empty (nothing `pending`, zero `erik`/`erik_gate`). That is **B39**, unchanged. The new fact is the eight that passed: **M2.7's navigation contract still holds after this run's four fixes**, in a real browser at `aal2` against real rows — B33 rewrote ten call sites and B32 deleted a module, and reference resolution survived both. **B38 did not bite**: it leaks the session cookie only when a `request.get()` fails, and these were thrown assertions. Then the two fixes QA flagged as protected by neither a test nor an observation were **observed** through the saved session: **B40** — sign-out present, exactly one, inside `<header>`, zero inside the `hidden md:block` rail, `idle`, **visible at 390px**; **B36** — nav link present, `/questions` **200**, not gated, not empty, **96 rows** linking to `/questions/[id]`, FR-58 count stated, detail view 200. So B43's tests and a direct observation now both hold those two. **Still NOT VERIFIED and deliberately so: that sign-out destroys a real session** — the only way to observe it is to click it, which would have destroyed the session just minted. That is a manual check |
| 2026-08-23 | Run `29b583`'s four worktrees pruned — **2.8 GB reclaimed** | The audit's `WORKTREE-LEAK` warning, cleared. **All four were dirty**, so this was not the routine case: the 2026-08-20 prune found its seven clean and this one found every worktree holding uncommitted work, because the orchestrator merges by copying files rather than committing in the worktree. Proven safe before anything was removed, by content rather than topology — `git cherry HEAD <branch>` empty for all four, and **every file `cmp`-identical to the shared checkout**, the only exceptions being three `DELETED-IN-WT` entries that are exactly the files B33 and B32 deleted on purpose. A bare `git diff HEAD <branch>` would have answered a different question: these branches sit at the pre-run base `8b311c4`, so it reports HEAD's own newer content as the branch's additions. Two things confirmed in passing: the u4 worktree's `u4.md` is byte-identical to the promoted shared copy, so the isolated agent's corrected report did land — the request to hand-edit the shared one was refused and cost nothing; and its `CLAUDE.md` matches the committed lesson. Removed with `--force` (required for a dirty worktree), **all 11 branches retained** — the reversible half. `run-audit.sh` re-run: `WORKTREE-LEAK: no worktrees remain`, **PASS, 0 fail, 1 warn**. `.fleet/audit-29b583.md` deliberately NOT regenerated: it records the run's state at completion, and this prune happened after |
| 2026-08-23 | B43 hand-fixed after run `29b583` | **The two tests that make B40 and B36 stay fixed.** `tests/app-shell.test.tsx` and `tests/nav-routes.test.ts`, `fecb6fc`; `pnpm test` 1400 → **1409 passed / 6 skipped**, typecheck 0, lint 0, `gate:m27` 5/5. **Proven red-capable rather than assumed** — four mutations, each restored from a byte snapshot in a `finally` rather than `git checkout --`, and all four went red. Two judgement calls worth the ink: the shell test asserts the control is inside the `<header>` rather than merely present, since a control moved into the `hidden md:block` rail renders on desktop and vanishes on mobile — which a presence-only check waves through; and `nav-routes.test.ts` **pins the five positional indices B44 depends on**, which does not fix B44 but converts a silent title swap into a failing test. Also this session: `report-29b583.md` persisted, `run-audit.sh` **PASS** (0 fail, 2 warn), `manual-gate.sh` re-run independently and **FAIL MISSING** confirmed, and 5 learnings routed to the vault (4 created, 1 corroborated, plus the §7c ADR corrected — its status line had claimed the gated behaviour was *not yet exercised on a live run*, which this run falsified). **Not done, and not doable by an agent: minting the operator session (B41).** |
| 2026-08-23 | Fleet run `29b583` (full) | **4 units done, 0 failed, build PASS.** B33, B36, B32 and B40 fixed on `agent-build/2026-08-23-29b583`; `pnpm test` 1359 → **1400 passed / 6 skipped**, `gate:m27` 5/5 throughout, both gate files untouched. **The run's most consequential finding was made at Phase 0, before any dispatch: §7c declares two boundaries `reachable` and BOTH measured unreachable** — the saved `aal2` session is revoked (gate `10 failed / 10` vs `8 passed / 2 failed` on 2026-08-20) and there is no agent token. So `docs/user-guide.md` was **not dispatched** (§7c's own stated consequence), **M1.10 stays not-Complete**, and every screen this run touched is built unobserved — **B41**. Also self-reported: a specialist ran unisolated and its file was swept into another unit's commit, leaving two divergent u4 reports — **B42**, history deliberately not rewritten. 4 questions queued. Next: sign in, mint a session, re-run `gate:m27:e2e`, then write the manual |
| 2026-08-19 | Fleet run `b0952e` (full) | **3 units done, 0 failed, build PASS.** `r1` pinned the stack (Next 16.3.1 / TS 7.0.2 / Tailwind 4.3.3) and found that **Vercel's WAF cannot rate-limit per token on Pro** — IP/JA4 only, header keys are Enterprise — forcing FR-8 to a Postgres counter. `u1` scaffolded and merged 63 files. `i1` built and verified M1.1. `i2` built the pure core and **found that `plan.md`'s own coverage fixture cannot detect a widened certifier join**. Corrected a wrong premise carried in the dispatch brief: **B1b does not block `devops`**. Paused at the Phase 1 checkpoint; next is `/build-from-spec continue b0952e` |
| 2026-08-19 | Fleet run `b0952e` (full) — **complete** | **16 units done, 0 failed, build PASS, QA verdict ISSUES.** Resumed a run whose checkpoint was 5 hours stale; reconciled it against the tree rather than the manifest or `git log`, and re-dispatched nothing. Phase 2 merged M1.2–M1.9. `qa-reviewer` with live credentials **closed 17 of ~18 carried NOT VERIFIED items** and found one critical — Mode-1 ingest dead on a missing `service_role` grant — fixed by `i10` and re-verified through the product path. Recovered `c1` and `d1` question files and reports that existed only on the branch (fan-in was 69 lines against 85 queued). 18 of 20 dispatches. Next: provision an operator account, rule on CR-002 |
| 2026-08-18 | CR-001 approval | Erik approved CR-001 with Q8 = `contested` (recorded as FR-79). `security-gate.sh` run on a merged 21-entity effective spec: **PASS** — all 21 entities classified. Consequential edits made: repo `CLAUDE.md` now says six questions; this file's header, pointer and milestone notes updated. `spec-approved.md` untouched, still byte-identical to `spec-v1.md`. No code written |
| 2026-08-19 | Post-run decisions | Erik ruled on the run's open items. **CR-002 approved** - append-only absolute, M1.10 unblocked and now the natural next build. **B23 closed**: `report-gate.sh` patched to evaluate `qa-reviewer` reports, with a full regression sweep and four negative controls, and the divergence between the gate's heading form and `qa-reviewer.md`'s recorded rather than papered over. Four of the run's five learnings routed to the vault (the fifth was already filed in the Phase 1 batch); **the grep-blindness claim was withdrawn as unreproduced before it could be filed as a measured fact**. Operator-account provisioning and the GitHub push were taken up separately |
| 2026-08-19 | Post-run: account, rulings, two fixes | **B20 closed** - operator account created by Erik in the dashboard, TOTP enrolled, `is_operator()` observed true at aal2. **CR-002 approved** (M1.10 unblocked), **B25 confirmed** (the ingest `400` stays), **B23 closed** (`report-gate.sh` patched to evaluate `qa-reviewer` reports, four negative controls, full regression). Branch pushed and opened as **PR #1**; `master` untouched. **Signing in immediately found two defects that 966 tests could not see** - a StrictMode double-invoke blanking all three auth screens, and a GoTrue friendly-name collision making the enrol screen single-use. Both fixed with red-proven tests; suite now **971 passed / 6 skipped**. `scripts/ingest-own-run.sh` added so the ledger can be populated from this repo's own run. Next: create an engagement, mint a token, ingest, review the six screens |
| 2026-08-20 | First ingest - the ledger holds real data | **The product ingested its own fleet run through the product path, driven by a human: HTTP 200, `unparsed = 0`.** 79 requirements, 96 open questions, 24 blockers, 20 work items, 322 test cases. Getting there took two rounds. **A `401 malformed_authorization` was diagnosed without the credential ever entering a command** - a differential probe with a well-formed *fake* token proved the script's header transport clean (`invalid_token`, not malformed), which located the fault in the env var; a shape-only readout then identified its contents exactly as the instruction line Erik had been told to paste. **The instruction was the defect, not the operator** - step 2 put the token on the clipboard and step 3 handed him a command block that overwrote it. Rule captured in `CLAUDE.md`, and the shape gate folded into `scripts/ingest-own-run.sh` itself so the next bad paste is refused by a regex before the payload is built, reporting shape only and echoing no byte of the value. **Then a real product defect:** `payload.ts` banned path separators in `testFiles[].path` while `harnessFor` needs an `e2e` segment to classify, making `harness: "playwright"` unreachable - 10 of 34 test files would have ingested as unit tests. Erik ruled to split the rule rather than widen it or collapse the paths. Fixed with 6 red-proven tests; **977 passed / 6 skipped**, typecheck 0, lint 0. Verified in production data: 57 Playwright cases across 9 files. Four findings left deliberately loud: no `test_result` rows, null fleet-run test counts, 10 unparsed tracker milestones, 4 null question confidences. Next: **§5a design approval against the six populated screens** |
| 2026-08-20 | §5a review — the design failed its own test | **Walked the six populated screens and measured B11 instead of eyeballing it.** Two of its written claims were false in both themes: two of FR-43's four evidence scopes were 2.0–2.6 apart in greyscale with the same treatment, and `unparsed` sat 0.3–0.9 from `--primary`, so the chip that means *nothing classified this* was the same desaturated value as the button that means *click me*. Four tokens re-solved holding hue, `unparsed` moved to a **hatched** treatment that does not depend on colour, and `tests/state-scale.test.ts` now enforces the claim with an empty, exactly-asserted exception list. **979 passed / 6 skipped**, typecheck 0, lint 0, build 0. Two findings raised and not yet actioned: **the QA-report defects parser is built and unwired** — `src/lib/ingest/defects.ts` has tests and a fixture, `plan.ts`/`persist.ts` never call it, so Broken shows 0 while `qa-report-b0952e.md` records 12 findings — and **`/next` cannot distinguish "no work" from "all work done or blocked"**, because its three counters count exclusions from a candidate set that was empty. §5a is **still not approved** |
| 2026-08-20 | B27 — wiring the defects parser, and what it caught | **Broken has real data for the first time: 13 defects, 1 critical `fixed`, 2 major `fixed`, 3 major open, 7 minor open.** Two additive columns (`defect.source_key`, `defect.raw_severity`), the writer allocating `D-nn` above the engagement's high-water mark and reusing the ref a finding already holds so a second post updates instead of duplicating. **The wiring's value was not the feature but the bug it exposed** — the parser read a report recording its own remediation as if nothing had been fixed, and would have shown two open criticals where the report says one, fixed. Fixed under Erik's ruling; see the Decisions log. Re-ingested and verified in the table; two stale rows deleted by hand. **995 → suite green, typecheck 0, lint 0.** `test_result` is now the only one of the six answers with a parser and no data path. Next: **§5a against six screens, four of which now have real rows** |
| 2026-08-20 | CR-003 drafted and approved | **The six screens terminate, and that is the half of the purpose nobody built.** Erik: "the whole purpose of this app is to be a one stop solution … to get rid of siloed information." A glance that raises a question you cannot follow sends the reader back to the silo — Blocked says `c1` is blocked and not what blocks it, so finding out means opening the manifest. **CR-003 approved with all three questions answered at approval time:** ships on the existing `service_role` path with the debt raised as **B29** (Q9); **Phase 2 as M2.7 with PR #1 merging first**, so the branch stops growing at 51 commits (Q10); FR-55's "links" ruled a **hyperlink**, making **FR-55 partially met rather than met** (Q11). ~~**A correction found while drafting:** I had claimed nothing in the product decrypts outside ingest. False — `answers/load.ts` decrypts and Broken renders the full decrypted description inline. The real finding is narrower and sharper: one screen carries detail and three carry identifiers, and nothing decided that.~~ **THAT CORRECTION WAS ITSELF WRONG, corrected 2026-08-20:** Broken renders no decrypted description — `defect.description` is in *comments only* and `BrokenDefect` has no such field. Both claims came from one grep that matched comment text and was read as code, and the second was written while explicitly checking the first. **The true state was six bare screens**, with `load.ts` decrypting only `contract_milestone.amount`. **The signature defect family, aimed at the record this file keeps:** a plausible mechanism, a real observation beside it, and a false conclusion joining them — twice, on the same fact, the second time inside a correction. Those three are defects against FR-52/FR-53/FR-56, kept OUT of the CR in its §2 so they belong to PR #1 and are not gated on this approval |
| 2026-08-20 | The three description defects, fixed | **Blocked, Next and Bottleneck now say what a work item IS, not merely which one it is.** `loadWorkItems` and `loadBlockers` take an opt-in `withProse`, off by default because `decrypt_field` costs one RPC per value; Blocked carries the blocker's own prose beside the item's, which is the actual answer to "what is holding it". §7a permits it on both the screen and the FR-57 endpoint — both rows read "operator, agents, decrypted server-side". **An existing guard went red and was narrowed rather than deleted:** "never selects an encrypted column on any answer path" was a real invariant *and* the reason the gap existed, so the two columns are now allowed on the screens that need them, every other pgcrypto column stays unselected everywhere, and a second assertion bounds the exemption so no third table gains one quietly. **A description that will not decrypt now refuses the whole answer** rather than rendering null, which would be indistinguishable from a work item that never had one. **998 passed / 6 skipped**, typecheck 0, lint 0, build 0. Erik confirmed the Blocked screen by eye. `3ee4acb` |
| 2026-08-20 | M1.10 — export and deletion | **Phase 1 has no unbuilt code left in it.** FR-60's export reads all 21 tables in one database function and was **observed**, not asserted: a 421,637-byte document whose counts agree with this file, prose decrypted, `agent_token.token_hash` absent. FR-61's archive, restore and hard deletion are built against CR-002 §2. **The finding worth keeping is what made FR-61 unbuildable:** `engagement → test_case → test_result` was a cascade chain into an append-only table, so a purge naming only CR-002 §2.1's tables would have been correct in its own text and raised `restrict_violation` on every call. Fixed by dropping the foreign key, which is CR-002 §2.4's literal instruction, and guarded by a new analyzer that walks the cascade graph out of the migration SQL — red against the real migrations before the fix, green after, six synthetic controls proving it can go red. Migration `20260820153021` applied live; privileges read back with `has_function_privilege` rather than trusted, `service_role` true and `authenticated`/`anon` false on all four functions. **1061 passed / 6 skipped**, e2e **207 passed / 7 skipped**, typecheck 0, lint 0, build 0 with 33 routes. **Two things carried honestly: the purge has never been executed** — NOT VERIFIED, and a two-minute probe next session closes it — **and `docs/user-guide.md` is not written**, held behind §5a on purpose. New blocker **B30**: the export is decrypted and Erik should confirm that |
| 2026-08-20 | M2.7's gate, written before dispatch | Erik: "shouldn't we have been using my autonomous fleet?" — a fair catch, and recorded as a lesson in `CLAUDE.md`. The more useful answer was that **"fleet or hand" is the wrong question until the success criterion exists**: `b0952e` had every gate this practice knows how to build and still shipped Mode-1 ingest completely dead with 966 tests green, and every defect since has been found by a person looking at real output rather than by any suite. So M2.7's acceptance gate is now written and **red before anything is dispatched** — 5 credential-free structural tests (`pnpm gate:m27`) and 10 crawl tests over the populated screens (`pnpm gate:m27:e2e`), covering FR-80, FR-82, FR-83, FR-84, FR-85, FR-86 and FR-55's hyperlink. Neither runs in the default suites; `pnpm test` is still 1061 passed and `pnpm e2e` still 207 passed, and a positive control confirms the gate project appears when `M27_BASE_URL` is set. **The gate fails rather than skips when it cannot see data**, which is B19's lesson made mechanical. `M27_STORAGE_STATE` is a saved operator session and therefore a credential: gitignored, produced by Erik signing in, never by an agent |
| 2026-08-20 | Fleet run `eb2490` (full) — M2.7 | **7 units done, 0 failed, build PASS, gate GREEN.** 8 of 20 dispatches, three dependency waves plus an independent review, cut from `6210318` and never near `master`. **`pnpm gate:m27` 5 failed / 5 → 5 passed / 5**, baseline measured by the orchestrator before dispatch and reproduced by the reviewer afterwards; neither gate file edited. `pnpm test` 1061 → **1346 / 6 skipped / 0 failed**, build 33 → **41 routes**, e2e 207 → **255 / 7 skipped**, typecheck 0, lint 0. **M2.7 is code complete and NOT Complete**: `pnpm gate:m27:e2e` has never run for want of an `aal2` session (**B31**). QA **BLOCKED, 0 critical / 3 important / 9 minor**, all units SOUND, **no fabricated claim in any report**. The run's best artefacts were the things that went wrong and were caught: `i1` had a mutation **survive**, exposing a vacuous scoping test that a single-engagement fixture could never fail; `f3`'s first lint negative control was **blind** because oxlint does not enable `no-unused-vars`; `f5` found **8 of 28 filter values silently rejected** by a wire-vs-stored spelling split, which a green e2e gate had been compatible with; a pre-existing source-hygiene test caught a **raw NUL byte** that would have made a new module invisible to `grep`; and `f4`'s report **failed `report-gate.sh`**, was sent back, and was repaired and re-verified. Five new blockers (**B31**–**B35**) plus **B36**; **CR-004 wanted** for the FR-80/FR-81 conflict. 30 questions queued |
| 2026-08-20 | Run eb2490 post-run — **paused here on credits** | **The dispatching session's duties, done and not done, stated exactly.** DONE: `report-eb2490.md` persisted verbatim (F11 — the orchestrator cannot write report-shaped filenames); `run-audit.sh` run and persisted to `audit-eb2490.md`; gates re-run **by this session rather than taken from the report** — `pnpm gate:m27` **5/5 GREEN** (was 5/5 red at dispatch), typecheck 0, lint 0, `pnpm test` **1346 passed / 6 skipped**. **The no-self-marking claim was verified in its strongest form:** `git log --all -- tests/m27-gate.test.ts e2e/m27-navigation.spec.ts` returns exactly one commit, `6210318`, the one that wrote them — the fleet made a red gate green without touching it. **AUDIT VERDICT: FAIL — 1 fail, 1 warn, 12 ok.** The failure is real and is the cd414c class: **`qa1`'s 5 questions never reached the fan-in file** (5 in `questions-qa1-eb2490.jsonl`, 0 collected) — decisions Erik never got to make. Not reconciled, deliberately: the protocol says the audit reports on the record and reconciling it is diagnosis, not tidying. Nothing is lost; the per-unit file is on disk. Warn: 8 worktrees leaked under `.claude/worktrees/` (P23, Synthesize owes a prune). **NOT DONE, and each is a real gap rather than a completed step:** the 5 fleet learnings are **not routed to the vault** (`learnings-eb2490.md` is on disk and routes fine next session); `pnpm e2e` and `pnpm build` were **not re-run by this session**, so the report's 255/7 and 41 routes are the orchestrator's numbers, not independently confirmed; the `NOTIFY:` line was **not** sent through `PushNotification` — deliberate, Erik was present in the conversation when the run returned, which is the only thing that path exists to achieve. **M2.7 is NOT Complete and must not be marked so: `pnpm gate:m27:e2e` has never run, so nothing in this milestone has met a real row** |
| 2026-08-20 | Working the pointer's owed list — **Erik answered 5 of 6 open items; 2 of the 5 arrived as unfilled templates** | **The session's own debts first, Erik's afterwards, as instructed.** RE-RUN RATHER THAN QUOTED: `pnpm e2e` **255 passed / 7 skipped** and `pnpm build` exit 0 with **41 routes counted from the emitted table** — the orchestrator's numbers were right, and they are now this file's measurement. **PURGE PROBE, authorised: refusal path observed, destruction path still not.** Run through the product's `service_role` client rather than the Supabase MCP — the MCP call was blocked by the destructive-action classifier, and the service-role path is the more faithful test anyway, because MCP runs as `postgres` and would have proved the function works while proving nothing about the grant the product depends on. Preconditions re-read rather than trusted; the probe script aborts if any engagement is archived. `no_such_engagement` and `not_archived` both observed, engagement 2→2, audit_log 398→398, work_item 20→20, requirement 79→79, **no audit row on a refusal**. **The pointer's claim that this closes M1.10's NOT VERIFIED was too strong** — a refusal probe cannot observe a cascade. **5 LEARNINGS ROUTED** to `Erik-Brain/Knowledge/` as 5 new notes; grepped first, no rivals found, back-links added to the two adjacent notes and 5 index entries written. **7 OF 8 WORKTREES PRUNED**, branches retained, after confirming all 7 clean and that no file in any worktree branch is absent from `HEAD`; the 8th is an orphan directory needing an `rm -rf` this session was not permitted to run. **`qa1`'S 5 QUESTIONS READ, and the audit FAIL is smaller than it looked**: all five map one-for-one onto **B31, B32, B33, B34, B35**, so the record broke but no decision went missing. Left unreconciled per protocol. **ERIK'S ANSWERS: B30 confirmed. B4 answered global-with-allowlist — and the answer opened a build unit, because the allowlist did not exist in code; built with 9 tests and 3 mutations each proven red, and the pointer's claim that B4 unblocks `work_session` corrected, since nothing is deployed. B35 ruled FR-81 wins — CR-004 drafted and approved, no code, no schema, no entity change, both gate files untouched.** **B34 fixed by subagent, and NOT as prescribed:** `string | null | "none"` does not discriminate, because TypeScript absorbs the literal into `string`; built as a sentinel object instead, and the blocker's "four views print their title twice" claim was inspected and **struck** rather than acted on. **TWO OF ERIK'S FIVE ANSWERS ARRIVED AS LITERAL TEMPLATES** — `§5a: approved / change <what>` and `B35: ... <FR-80 or FR-81> wins`. Both were re-asked rather than guessed. **§5a: Erik confirmed he has not looked at the screens yet**, so it is unstarted rather than merely unanswered, and B32, the user guide, `f4`/`f5` and the merge stay held behind it. **Verified by this session, not taken from any report:** typecheck 0, lint 0, `pnpm test` **1359 passed / 6 skipped**, `pnpm gate:m27` **5/5**, build 0 / 41 routes. **Nothing committed.** A concurrent subagent ran `git stash -u` on the shared checkout mid-session; the tree was verified intact afterwards and the lesson recorded in `CLAUDE.md` |
| 2026-08-20 | Sign-in lockout, then B31's first real run | **Erik could not sign in, and that came first.** Diagnosed with a loop rather than a theory: GoTrue proven healthy, the account intact, and `/sign-in/verify` proven to render correctly when handed an admin-minted session — so no password of Erik's was ever needed or touched. **The first coherent explanation was refuted by measurement** (on a valid session the proxy writes no cookie at all), and the real chain — `document.cookie` cannot overwrite an `HttpOnly` cookie, silently — was confirmed with a passing control. Fixed in `proxy.ts`; **B37** records that Baseline §1 is now unmet rather than waived. **Then B31 ran for the first time: 8 passed / 2 failed.** `/committed` went green once a milestone existed; `/next` and `/bottleneck` stayed red **and should have** — no work item is `pending` and **zero** are `erik`/`erik_gate`, so those answers are genuinely empty and unpopulatable through the product. Raised as **B39**. **A correction owed:** the instruction to create an external wait to light up `/bottleneck` was wrong — FR-56 ranks work items by `executor_kind`, and a wait only sets an `alsoHeld` flag. That was written without reading `bottleneck.ts` first, and it cost Erik a form-filling round trip. **`e2e/m27-navigation.spec.ts` edited with explicit authorisation**, gathering only, no assertion touched — and **the first attempt made things worse** (unbounded `Promise.all` flooded a shared dev server, 3 failures → 7) before the bound and a measured 120s timeout landed it at 8/10. **B38**: the gate leaked the live session cookie into failure output twice; both sessions revoked, on-disk copies deleted, `test-results/` was already gitignored. Verified by this session: typecheck 0, lint 0, `pnpm test` **1359 passed / 6 skipped**, `pnpm gate:m27` **5/5**, `pnpm e2e` **255 passed / 7 skipped**, build 0 / **41 routes** |

---

## Notes on maintaining this file

**For Claude Code / project-lead:** at the end of every build session or run, update the Next session pointer, Current state, milestone statuses, the Decisions log for anything that diverges from or extends the spec, and a one-line Session log entry. Move resolved blockers into the Decisions log with their resolution.

`prod.md` is build-internal. Scope changes go through `spec/change-requests/`, never into this file.
